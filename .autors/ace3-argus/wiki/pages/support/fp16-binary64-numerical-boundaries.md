---
title: FP16 representation and independent trajectory boundaries
description: Distinct FP16 and binary64 profiles, opt-in decoder v2/v3 reference boundaries, and limits on causal claims.
---

# FP16 representation and independent trajectory boundaries

## Middle-pair stage13 full-vector suffix intervention

The bounded full-vector stage13 sufficiency hypothesis is **REJECTED**, pending
normal independent Host review. All nine controls passed the unchanged
stage14-stage17 gates and independent final RMSNorm/two-row arithmetic checks.
All 18 directional contrasts agreed: the FP16 pair margin changed from `1/64`
to `17/128`, a `+15/128` movement. Nevertheless, all nine unchanged S18
original-input binary64 excess gates failed. The stage13 full-vector lane
therefore terminates without establishing sufficiency or admission.

`diagnose_q24_s16_final_head_final_rmsnorm_unselected_direct_hidden_mlp_stage17_stage13_rmsnorm_full_vector_suffix_intervention_v1`
substitutes all 896 working L23/P0 stage13 words from the authenticated retained
original-input FP16 archive. It does not require ranked stage13 source-bridge
accounts or reconstruct their unretained stdout. A missing archive member is an
explicit UNKNOWN binding boundary, not permission to replay its producer.

Only stage14-stage18, final RMSNorm and head rows `(34319,13)` may execute for
the nine retained controls. Prefix operands, Q24 input/scratch and FP16 KV bytes
are protected. Existing local/global numerical gates and independently propagated
original-input FP16/binary64 references are unchanged. The directional prediction
is fixed before execution from the negative full retained stage17 weighted error;
both recomputed stage17 and final pair-margin movements must have its strict
nonzero sign. No magnitude equality is assumed across nonlinear boundaries.
There is no new final-head admission threshold: final arithmetic has independent
scalar checks, followed by the directional contrasts.

Any numerical gate failure or zero/opposite contrast rejects full-vector
sufficiency. Binding, lineage or capture defects remain UNKNOWN. Either outcome
terminates this lane without replay. Even a supported outcome would establish
only bounded CPU sufficiency with wider-than-FP16 Q24 residual state, unchanged
native-S16-RTZ, INT4 weights and FP16 operator/KV boundaries, not strict-FP16-state
W4A16, new-token or full-model admission. Normal independent Host review is required.

## L21-from-L20 coordinate-62 margin sensitivity

`q24_s16_l21_from_l20_s18_coordinate62_margin_sensitivity_v1` is a distinct
nine-control diagnostic, not the legacy single-trajectory
`q24_s16_l21_s18_margin_sensitivity_v1`. It authenticates the reviewed census
implementation, contract, tests and terminal review, then uses only its
read-only authenticator for the terminal L21 result/validation/review, all
37 retained L21 artifacts and original L20/L21 references. The census itself
is stdout-only; no standalone census result/validation file or reconstructed
stdout is substituted. The complete ancestral input-binding manifest remains
pinned, not replayed. Source/operand/state/KV/RTZ PASS facts remain explicitly
authenticated retained evidence.

The narrow coordinate-62 scalar sensitivity is **SUPPORTED**, pending normal
independent Host Reviewer reproduction. All nine baseline FP16 words are
`55ed`, the nearest finite word is `55fb`, and the fixed original-input
binary64 reference is `0x1.7ecf94456fd10p+6`. Baseline excess is `7/8` and margin
is `-3/4` under the unchanged `1/8` budget. Their Q24 integers differ: each
requires its own minimal adjustment, ranging from `11885071` to `11921844`
Q24 units. This is not a claim that their original states differ from a
passing state by only one Q24 unit.

The exhaustive passing FP16 interval is `55f9` through `55fd`. Every minimum
cut reaches Q24 integer `1602748417`, yielding `55f9` with exact margin `0`.
That odd word's RNE cell has endpoints `3057/32` and `3059/32`, both excluded:
the ties belong to `55f8` and `55fa`. At integer `1602748416`, the one-unit-less
negative control yields `55f8`, excess `3/16` and margin `-1/16`. The exact
passing Q24 interval ends at `1607991295`; monotone FP16 cells and the complete
fixed-radius passing interval establish minimality, rather than a sampled
search.

All nine in-memory 896-coordinate minimum projections pass S18, while all
nine one-unit-less projections fail only at coordinate 62. Both projections
change only coordinate 62, keep every original reference and non-62 metric
fixed, and publish no replacement residual state. Native, decoder, prefix,
admission, reference, suffix, RTL, GPU, simulation and hardware dispatch are
zero. The contract discloses the `--out` command and restricts evidence to an
absent versioned build child, with exclusive command/validation/result JSON.
Historical failures and the legacy identity remain unchanged.

This localizes scalar output sensitivity, not a unique upstream cause,
incorrect RNE implementation, native model repair or downstream sufficiency.
Q24 residual state remains wider than FP16; native G128 asymmetric packed
INT4 semantics, FP16 scales/operator boundaries/KV and S16 RTZ remain fixed.
No policy adoption, successor, strict-FP16-state W4A16, new-token or full-model
admission follows.

## Coordinate-761 S18 RNE-cell anatomy

`diagnose_q24_s16_final_head_final_rmsnorm_selected_direct_hidden_mlp_stage17_s18_coordinate761_rne_cell_anatomy_v1 --check`
uses the independently reviewed full-stage17 S18 boundary capture and its
authenticated full-vector parent. It binds nine coordinate-761 accounts to
retained Q24 scratch integers/signed-zero tags, original-input stage17 FP16
operands, output state, FP16/binary64 references and unchanged numerical gates.
Only pure scalar accounting, cell geometry and an independent host binary16
oracle execute; no native state-add, decoder, producer, intervention, RMSNorm
or head executes.

The narrow ordinary-RNE-cell mechanism is **SUPPORTED**, pending independent
Host review. In all eight inside-before/fail-after controls, scratch integer
`-32162080` with zero tag `0` contributes `-1005065/524288`, and stage17 word
`bf1c` contributes `-455/256`. Their exact sum `-1936905/524288` is strictly
inside word `c364`'s cell `[-3785/1024, -3783/1024]`. The word has an even
significand and owns both midpoint ties, but the retained sum is **not a tie**:
its distance below the upper endpoint is `9/524288`.

Correct RNE moves the value outward by `503/524288`, from positive authority
margin `20995690215/562949953421312` to negative margin
`-519096447257/562949953421312`. The independently propagated binary64 reference
remains `-0x1.c8d0e46e19dcep+1`; its nearest-FP16 word is `c323`, with
representation floor `580415180519/1125899906842624`. The authority radius
remains that floor plus **`1/8`**, with equality accepted. The rounded stage12
view is not substituted for the wider Q24 scratch operand.

`mapped_all` is different: coordinate 761 passes both before and after rounding,
and its word `c361` has the odd-significand open cell
`(-3779/1024, -3777/1024)`. Consequently the boundary capture's failed-coordinate
detail does not contain this account; the diagnostic explicitly obtains it
from the authenticated parent's retained operands rather than fabricating a
missing failure. `mapped_all` still uniquely has **83/83 other retained failed
coordinates** outside the radius before rounding; the eight remaining controls
retain **82/83**. No historical failure or threshold is changed.

All nine independent scalar oracle checks agree, with no observed tie,
signed-zero, subnormal, overflow, scratch-view substitution, state/KV,
cell-geometry, oracle or source/capture anomaly. Authentication defects yield
UNKNOWN; arithmetic/integrity counterexamples or a failed crossing prediction
yield REJECTED. Full byte-exact inner/outer capture, source/test/review pins,
account/environment identity, scope lock, concurrency and one-command budgets
remain enforced.

This closes only the coordinate-761 mechanism branch after normal independent
review. It does not establish a downstream repair, strict-FP16-state W4A16,
new-token or full-model admission. Q24-wide residual state, native S16 RTZ,
official INT4 weights, FP16 scales/operator/KV boundaries and original global
references remain unchanged; no follow-on classifier or bridge is scheduled.

## Full-stage17 suffix S18 exact reference-boundary diagnosis

`diagnose_q24_s16_final_head_final_rmsnorm_selected_direct_hidden_mlp_stage17_full_vector_s18_boundary_diagnostic_v1 --check`
authenticates the reviewed full-stage17-vector suffix's complete inner and outer
captures, independent terminal review, source/test pins, retained operands and
original-input FP16/binary64 reference authorities. It performs only read-only
exact scalar accounting, with no decoder, state-add operator, RMSNorm, head,
prefix, admission, reference producer or closed-intervention execution.

The actual left S18 operand is the **wide Q24 scratch integer**, not its rounded
stage12 FP16 view. Each retained failed-coordinate account includes both views,
the stage17 FP16 operand, exact integer/signed-zero transition, RNE word,
independent original-input references, nearest-FP16 representation floor,
unchanged `<= 1/8` excess threshold and both rounded and unrounded margins.
The binary64 authority radius is the representation floor plus `1/8`; it is
not a new tolerance or a locally reanchored reference.

The boundary-only explanation for the entire suffix cone is **REJECTED**.
All **83 failed coordinates in each of nine controls** agree with exact Q24/RNE
and an independent scalar oracle; no operand/state/KV/lineage mismatch is found.
However, in eight controls only **82/83** failed-coordinate unrounded sums lie
outside the radius. At **coordinate 761**, the unrounded sum is inside while
the correctly rounded FP16 word fails. For example, the frozen-inherited exact
sum is `-1936905/524288`, with positive unrounded margin
`20995690215/562949953421312`; correct RNE produces word `c364`, whose unchanged
threshold margin is `-519096447257/562949953421312`.
The `mapped_all` control has **83/83** unrounded sums outside the radius and
individually supports that narrower explanation, but cannot rescue the joint
hypothesis. No failed coordinate's fixed-operand RNE word is passable.

This distinguishes a remaining **S18 rounding/reference-boundary question**
from a claim that every failure already exists before rounding. It does not
demonstrate an S18 implementation defect, select a repair, or authorize any
passable-word or final-margin intervention. All 896 coordinates per control
receive arithmetic, state-view and retained-oracle checks; only the original
83-per-control failure accounts are emitted in detail. Authentication,
source/test/capture, environment/account/budget or execution defects yield
UNKNOWN; authenticated arithmetic, rounding or integrity counterexamples yield
REJECTED instead.

This is bounded CPU non-admission evidence pending normal independent Host
review. Byte-exact command/stdout/stderr/whole-command sealing and existing
scope-lock, concurrency, account, budget and review gates remain in effect.
Historical failures, independently propagated global references, native S16
RTZ, official G128 INT4 semantics, FP16 scales/operator/KV boundaries and
wider-than-FP16 Q24 state remain unchanged. No strict-FP16-state W4A16, new-token
or full-model admission follows, and this branch stops after this diagnostic.

## Middle-pair full MLP-stage17 vector suffix intervention

`diagnose_q24_s16_final_head_final_rmsnorm_selected_direct_hidden_mlp_stage17_full_vector_suffix_intervention_v1 --check`
replaces only the complete working L23/P0 stage17 MLP output vector with its
retained original-input FP16 counterpart, for the fixed nine controls and pair
`(34319,13)`. The reviewed hotspot/direct-hidden parents and complete reviewed
single-coordinate suffix capture are authenticated without executing their
producers or interventions.

The preregistered direction includes **all 896 coordinates**, not merely the
selected hotspot union. Before any suffix computation, exact component sums
use authenticated actual/original-input stage17 operands, the retained branch
reference inverse-normalization anchor, and unchanged FP16 normalization and
two-row tied-head weights. Selected-coordinate contributions close exactly
against the reviewed accounts; the unselected complement remains explicit.
There is no reconstructed internal binary64 trajectory or local reanchoring.

All **18/18** control/reference rows exhibit exact full-vector component
prediction closure and strict positive actual pair-margin movement of
**15/128**. Nevertheless, **all nine S18 numerical gates fail**, each with
**83** original-input binary64 excess failures at the unchanged `<= 1/8`
threshold. Thus the joint bounded full-stage17-vector terminal sufficiency
hypothesis is **REJECTED**. Replacing the entire MLP vector does not remove
the obstruction with the retained non-MLP state and terminal boundaries
unchanged; this does not identify a unique remaining cause or adopt a repair.
These retained control/reference rows are dependent accounts, not independent
population observations.

The execution cone contains exactly nine S18 additions, nine state-lineage
oracle calls, nine final RMSNorm calls and nine two-row head calls, with zero
forbidden dispatches or writes. Independent rational/integer oracles verify
Q24 state and signed-zero transitions, FP16 conversion, the integer-root
RMSNorm boundary, and exact two-row dot rounding. Non-replaced prefix,
input/scratch state and FP16 KV bytes remain protected. The shared sealer
retains byte-exact stdout, stderr, whole-command and outer-launch captures
with source/test, environment, account, budget and independent-review gates.

UNKNOWN remains reserved for authentication, source/test/capture,
state/KV/lineage, zero-prediction or execution-integrity defects, rather than
authenticated numerical rejection. Original global FP16/binary64 references,
thresholds and historical failures remain intact. This is CPU non-admission
evidence pending normal independent Host review, not strict-FP16-state W4A16,
new-token or full-model admission. Q24 residual state remains wider than FP16;
native S16 RTZ, official asymmetric G128 INT4 weights, FP16 scales and
operator/KV boundaries are unchanged. This branch stops after this one
full-vector intervention.

## Middle-pair selected MLP-stage17 hotspot suffix intervention

`diagnose_q24_s16_final_head_final_rmsnorm_selected_direct_hidden_mlp_stage17_hotspot_suffix_intervention_v1 --check`
tests one retained `(34319,13)` mechanism question, not another coefficient
classification. Before any suffix execution, it authenticates the reviewed
component-hotspot and direct-hidden bytes and selects the MLP-stage17 coordinate
with greatest sum of exact absolute retained contributions across the nine
controls and both reference branches. Ties choose ascending coordinate. The
original-input FP16 internal stage17 counterpart remains distinct from the
independently propagated binary64 terminal reference; no internal binary64
trajectory is reconstructed.

The selected coordinate is **241**. Only that working stage17 value is replaced
in each control. All remaining prefix/MLP values, input and scratch Q24 state,
and FP16 KV bytes remain protected. The executable cone consists of nine S18
state additions, nine state-lineage oracle checks, nine final RMSNorm calls,
and nine two-row tied-head calls. No projection or prefix/reference producer
executes. Independent integer/FP16 oracles check the actual S18 state/view
transitions and exact two-row dot rounding, including unchanged-coordinate
closure. The shared sealer preserves complete inner and outer command bytes,
environment/source identities, stdout, stderr, statuses and distinct bindings.

All **18/18** control/reference rows have a strictly positive fixed-weight
hotspot movement equal to the retained prediction, and strictly positive
actual pair-margin movement of exactly **5/32**. Nevertheless, the unchanged
original-input binary64 S18 excess gate **fails in all nine controls**.
The joint preregistered sufficiency result is therefore **REJECTED**, not
SUPPORTED and not an authentication UNKNOWN. These dependent retained rows
are not independent population samples. Directional transfer succeeds here,
unlike the retained stage11 value-content intervention, but does not remove
the S18 numerical obstruction or establish a unique nonlinear/cancellation
cause.

SUPPORTED requires every numerical gate and every strict directional contrast
to pass. Authenticated zero/opposite movements, prediction mismatches or
numerical failures reject sufficiency; authentication, source/test/capture,
state/KV/lineage, zero-prediction and execution-integrity defects are UNKNOWN.
The existing S18 exact excess threshold remains `<= 1/8`; all historical
failures and original global reference authorities remain intact.

This is bounded CPU non-admission evidence pending independent Host Reviewer
closure. Q24 residual state remains wider than FP16; native S16 RTZ, official
asymmetric G128 INT4 weights and FP16 scales/operator/KV boundaries are
unchanged. There is no strict-FP16-state W4A16, new-token or full-model admission,
and this branch stops after this one observation without a new ranking or
closed producer/intervention replay.

## Shared stdout-only diagnostic byte capture

`ace3/model/candidates/diagnostic_capture_v1.py` supplies file-backed command
and outer-launch sealing. It retains the existing ordered command/argv/preflight,
stdout, stderr and framed whole-command members with separate byte counts and
SHA-256 bindings, actual exit status and timeout state. Files are exclusively
created, including terminal receipts; nonzero exits, stderr, launch errors and
timeouts retain their bytes rather than becoming success-shaped summaries.
Outer sealing derives and retains the canonical command from the complete
argv/environment when omitted. Stored-byte verification can likewise derive an
omitted expected command; an explicit invalid command or a missing/invalid
retained command still fails. This does not relax full identity or framing checks.
Timeout cleanup stops the launched process group before sealing its streams.
A stable, nonblocking scope lock complements the representative's existing
process-census gate. Additional helper/source-test pins do not replace the
diagnostic's own source/test, input, account, environment, review or budget gates.

The retained sign-profile quotient diagnostic's test-module `capture`/`launch`
path uses this helper without changing its scientific source or computation.
The new MLP-stage17 hotspot suffix test-module launcher also uses it, with its
own bounded suffix budget and integrity gates. The fresh ignored-build, branch, original process
census, authenticated-result and zero forbidden dispatch/write gates remain.
The helper is transport, not an authority grant or an independent review.
Compatibility tests use synthetic command results through that representative
CLI and the existing stored-byte capture validator, alongside real subprocess
byte/failure/timeout tests. They do not rerun the closed quotient or any accepted
producer and create no new scientific evidence. Historical captures and failures
remain unchanged. Q24-wide residual, native-S16-RTZ, INT4 weights, FP16 operator/KV
boundaries and all non-admission limitations are unaffected.

## Read-only selected direct-hidden canonical dose affine sign profile quotient classifier

`diagnose_q24_s16_final_head_final_rmsnorm_selected_direct_hidden_canonical_dose_affine_sign_profile_quotient_classifier_v1 --check`
consumes the reviewed affine root-margin bytes, authenticating source/test,
runtime, terminal independent-review and complete inner/outer capture pins
along with retained predecessor pointers. It does not execute ancestor
classifiers or model operators.

The quotient covers exactly **735 fields** and **2,940 class signs**. Its class
order is increasing rational dose: zero, 933/16777216, 321/4194304 and 3/32768.
Fields share a class only when their exact sign vectors, root kind and exact
location, zero-inclusive/strict stability, no-reversal status, complete nearest
class ties and orientation rule agree. This is sign-profile equivalence, not
numeric field equality. Explicit membership indices partition the retained
matrix without losing source identity. The required disjoint accounting is
**20 lower-endpoint departures**, **16 above-interval roots**, **485 identically
zero fields** and **214 constant nonzero fields**.

The executable quotient produces **23 distinct profiles** over these 735
fields, with no omitted or multiply assigned member; this bounded result
remains pending independent review.

There are **294 retained forward/reverse field pairs**, covering 588 fields.
The classifier preserves hidden and absolute-mass signs, negates oriented
weighted signs and swaps the left/right weight fields as appropriate, checking
the retained coefficient and class-value equations rather than trusting a
zero-residual label. The other **147 fields** belong to the retained unpaired
scope; no reverse producer is fabricated or requested. Full nearest-class ties
and root locations must agree across each orientation pair. The 715 stable
signs, 230 strictly nonzero stable signs and 735 no-reversal fields remain
separate census requirements.

Arithmetic, count, sign, stability, tie and orientation mismatches are
**REJECTED**. Missing, ambiguous or unauthenticated inputs and incompatible
bindings are **UNKNOWN**, never a supported fallback. **SUPPORTED** requires
complete authenticated evidence and zero forbidden dispatch/write/operator
counters. The task-native launcher compiles both new Python files and runs
targeted pytest and the new check once, retaining byte-exact stdout, stderr,
whole-command captures, command/environment identity, exit statuses and hashes
in a fresh ignored build attempt. Independent Host Reviewer certification is
still required.

This remains retained-only CPU non-admission accounting. Original-input global
references, exact thresholds and source/operand/state/KV/lineage gates are
unchanged. Native S16 RTZ/Q24-wide residual state is not strict-FP16-state
W4A16; native G128 asymmetric packed INT4 weights, nibble ordering, no qzero
plus-one adjustment and FP16 scales/operator/KV boundaries remain fixed.
Off-interval roots are algebraic locations, not evaluated model doses.
No prefix/admission/reference or accepted-producer replay, row319 work, bridge
adoption, closed-branch reopening, hardware, precision/scale expansion or
new-token/full-model admission follows from this quotient.

## Read-only selected direct-hidden canonical dose affine root margin classifier

`diagnose_q24_s16_final_head_final_rmsnorm_selected_direct_hidden_canonical_dose_affine_root_margin_classifier_v1 --check`
authenticates the reviewed affine slope-support evidence, source/test/runtime and
independent-review pins, complete inner and outer command captures, and retained
predecessor bindings. No ancestor classifier or model operator is executed.

For all **735 coefficient fields** in five scopes, the diagnostic derives the
exact rational root of `baseline + slope * dose` where the slope is nonzero.
Constant nonzero fields have no root; identically zero fields have every dose
as a root, not a fabricated unique root. The four canonical classes remain
`frozen_inherited` (zero), `mapped62` (933/16777216), `scratch` (321/4194304)
and `frozen_inherited_o` (3/32768), with their retained control aliases.
Each class exposes the signed value, sign, absolute value margin, signed/absolute
root distance where defined, and exact root-distance closure. All 2,940 class
values are checked against 5,880 retained control-field values and original
operand/lineage bindings. The 36 nonzero and 699 zero slope census is unchanged.

The matrix distinguishes roots below, at either endpoint, inside or above the
retained interval. It reports all tied nearest classes, the interval minimum
absolute margin, sign stability (including stable zero), strict nonzero sign
stability and absence of sign reversal separately. Aggregate summaries expose
nearest unique roots per class, nearest off-interval roots on either side and
nearest nonzero value margins. Off-interval roots are algebraic locations only,
not evaluated model doses, extrapolated mass behavior or proposed interventions.
These margins are not final-head admission margins or altered thresholds.

The measured retained matrix has **20 unique roots at zero**, **16 unique
roots above the interval**, **485 identically zero fields** and **214 constant
nonzero fields**. All 735 fields avoid sign reversal; 715 have an unchanged
sign including zero, and 230 have a strictly nonzero stable sign. The other
20 leave zero at the lower endpoint. The nearest off-interval root is exactly
26361/8388608, shared by four fields, with distance 25593/8388608 from the
retained upper endpoint. There is no unique root below the interval. These
are bounded algebraic measurements pending independent review, not an
extension of the affine mass model beyond its retained sign domain.

Arithmetic/count/sign/support mismatches are **REJECTED**; authentication,
capture, missing/ambiguous rational fields and incompatible bindings are
**UNKNOWN**. **SUPPORTED** requires authenticated supported prerequisites and
zero forbidden counters. The task-native launcher compiles the two new Python
files and runs targeted pytest and the new check once, preserving byte-exact
stdout/stderr, command/environment identity, whole captures, exits and hashes
under a fresh ignored build attempt. Independent Host Reviewer certification
is required; an Engineer result does not provide it.

This is retained-only CPU non-admission accounting, not repair, causality,
performance or model admission evidence. Original-input references, exact
thresholds and source/operand/state/KV/lineage gates remain unchanged. Native
S16 RTZ/Q24 residual state remains wider than FP16; packed INT4 weights and
FP16 scales/operator/KV boundaries remain fixed. No prefix/admission/reference
replay, closed producer replay, row319 availability work, bridge adoption,
strict-FP16-state W4A16, new-token/full-model admission, hardware, precision or
scale expansion is authorized.

## Read-only selected direct-hidden canonical dose affine slope support classifier

`diagnose_q24_s16_final_head_final_rmsnorm_selected_direct_hidden_canonical_dose_affine_slope_support_classifier_v1 --check`
authenticates the reviewed affine-normal-form bytes, complete command captures,
source/test/runtime/review pins and retained predecessor pointers without
executing ancestor classifiers or any model operator.

The sparse derivative contract is exactly **36 nonzero and 699 zero slopes**
across five scopes and 735 coefficient records. Only the two coordinate-62
ordered pairs may move. Each has ten moving component fields: hidden,
source delta, source-only term, weighted value and weighted movement for
`actual_residual_boundary` and `q24_to_fp16_conversion`. Their hidden/source
slopes are respectively minus one and plus one. Weighted slopes multiply
these by the retained common factor and negate under ordered-pair reversal.

The other eight moving fields per ordered pair are hidden/weighted absolute
and cancellation mass values/movements. Their derivatives close from component
slopes and retained baseline signs over the retained nonnegative dose interval;
hidden mass slopes are minus two, weighted mass slopes are minus twice the
absolute common factor, and both preserve orientation. Signed mass slopes,
all canonical/residual fields, factors, weights, row differences and every
coordinate-241/context derivative remain zero.

The output exposes complete zero/nonzero lists and support, sign, arithmetic,
mass and reversal matrices. Arithmetic/support/count/sign/orientation mismatch
is **REJECTED**; authentication, capture-integrity, missing/ambiguous retained
fields or incompatible bindings are **UNKNOWN**. **SUPPORTED** additionally
requires authenticated supported prerequisites and zero forbidden counters.
The stdout-only diagnostic requires file-backed stdout/stderr capture; the
outer launcher retains byte-exact command/environment, streams, whole capture
and exit status, then validates stored evidence without semantic recomputation.
Independent Host Reviewer approval remains required.

This is descriptive retained-only CPU non-admission accounting, not a repair,
causal explanation, extrapolation or performance claim. Original references,
thresholds and source/operand/state/KV/lineage gates remain fixed. Q24 residual
state remains wider than FP16; INT4 weights and FP16 operator/KV boundaries
are unchanged. No strict-FP16-state W4A16, new-token or full-model admission
is established, and no closed producer or branch is replayed.

## Read-only selected direct-hidden canonical dose affine normal form classifier

`diagnose_q24_s16_final_head_final_rmsnorm_selected_direct_hidden_canonical_dose_affine_normal_form_classifier_v1 --check`
consumes the independently reviewed canonical dose-class quotient and its
authenticated command/source/test/runtime/review and predecessor pointers.
Only retained-byte authentication, rational accounting and pointer resolution
are used; ancestor semantic entrypoints and model operators are not executed.

Each retained numeric coordinate/component/source/mass field has a baseline
at `frozen_inherited` and three independently derived candidate slopes from
the nonzero canonical classes. The exact form is `baseline + slope * dose`.
All three slopes must agree and reconstruct every member of all four classes.
The class-dose abscissa varies even for coordinate-241 context accounts:
their zero slopes are therefore identifiable, not a fit to repeated zeros.
Coordinate-62 source slopes are minus one for `actual_residual_boundary`,
plus one for `q24_to_fp16_conversion`, and zero for other components.

The output exposes coefficient uniqueness, every reconstruction residual,
independent product/source/mass closure residuals, and both coefficient and
numeric ordered-pair reversal matrices. Reversal swaps row weights, negates
signed weighted fields and preserves hidden dose and absolute/cancellation
masses. The retained quotient identities and pointers remain distinct.
Required counts remain 40 control-coordinate accounts, 280 component
accounts, 20 nonzero source movements, 260 zero-source accounts and 112
reverse-component checks.

Non-affinity, coefficient non-uniqueness, residual, count, mass, source-sign
or arithmetic/orientation mismatch is **REJECTED**. Authentication/capture
defects, missing or ambiguous retained fields and incompatible bindings are
**UNKNOWN**. **SUPPORTED** requires all exact checks and zero forbidden
dispatch/write/operator counters, with independent Host review still required.
The outer test harness exclusively creates complete byte-exact command
captures; its stored-byte validator performs no semantic recomputation.
This is retained-only CPU non-admission accounting, not a repair, causal or
performance claim. Original-input references, thresholds and
source/operand/state/KV/lineage gates are unchanged. Q24 residual state remains
wider than FP16, with native S16 RTZ, INT4 weights and FP16 operator/KV
boundaries unchanged; no strict-FP16-state W4A16, new-token, full-model,
precision/scale/hardware expansion or closed-branch reopening follows.

## Read-only selected direct-hidden canonical dose-class quotient classifier

`diagnose_q24_s16_final_head_final_rmsnorm_selected_direct_hidden_modal_canonical_dose_class_quotient_classifier_v1 --check`
authenticates the reviewed down-axis invariance evidence, complete command
capture, source/test/runtime and independent-review pins, and retained
predecessor/raw-row pointers. It does not execute ancestor semantic
entrypoints or model operators.

Within each of the five retained coordinate accounts, the eight controls
have four canonical dose classes: `frozen_inherited`,
`frozen_inherited_down`, and `inherited_native` at zero;
`frozen_inherited_o` and `frozen_inherited_o_down` at `3/32768`;
`scratch` and `scratch_down` at `321/4194304`; and singleton `mapped62`
at `933/16777216`. These are coordinate-62 absolute doses;
coordinate-241 context accounts remain zero-dose. The quotient does not
assert equality across different coordinates.

The classifier requires 90 within-class control comparisons covering every
retained numeric field, 210 cross-class component comparisons (including
zero-dose classes without dividing by zero), 280 component accounts,
20 nonzero source-only movements, 260 zero-source accounts and 112
reverse-component checks. Class dose and ordered-pair orientation determine
component terms, common factors, source/weighted movements and
signed/absolute/cancellation masses within the retained account. Reversal
preserves hidden doses, swaps row weights, negates weighted signed fields
and preserves absolute/cancellation masses. Identities and authenticated
control-specific pointers remain distinct, not merged by the quotient.

Numeric, membership, dose, count, mass, proportionality or orientation
mismatch is **REJECTED**. Authentication or capture defects,
missing/ambiguous fields and incompatible bindings are **UNKNOWN**.
**SUPPORTED** is bounded retained-only CPU non-admission accounting,
subject to normal independent Host Reviewer completion. It is not repair,
causal or performance evidence; it does not reopen closed branches or
the missing row319 producer. Original references, exact thresholds and
source/operand/state/KV/lineage gates remain fixed. Native S16 RTZ/Q24-wide
residual state is not strict-FP16-state W4A16; INT4 weights and FP16
operator/KV boundaries remain unchanged. No precision, scale, hardware,
GPU, RTL, FPGA, ACE2, new-token or full-model claim follows.

## Read-only selected direct-hidden modal down-axis invariance classifier

`diagnose_q24_s16_final_head_final_rmsnorm_selected_direct_hidden_modal_down_axis_invariance_classifier_v1 --check`
authenticates reviewed modal dose/rank evidence and its complete command,
source/test/runtime, independent-review and predecessor/raw-row bindings. It
compares `frozen_inherited_down` with `frozen_inherited` at zero dose,
`frozen_inherited_o_down` with `frozen_inherited_o` at `3/32768`,
`scratch_down` with `scratch` at `321/4194304`, and `inherited_native`
with `frozen_inherited`. These are coordinate-62 doses; coordinate-241 context
accounts retain zero dose.

The alias matrix covers all five coordinate accounts and seven components:
20 control-pair accounts and 140 component pairs. Numeric fields must match
exactly, while distinct control identities and retained pointers must each
authenticate to their original control. Product, delta, mass, dose-group and
reverse-orientation closures preserve 280 component accounts, 20 nonzero
source-only movements, 260 zero-source accounts and 112 reverse-component
checks. Arithmetic or alias mismatch is **REJECTED**; authentication defects,
missing/ambiguous fields or incompatible bindings are **UNKNOWN**.

This classifier is retained-only CPU non-admission accounting, not a new model
execution, repair, causal or performance claim. No ancestor semantic entrypoint,
closed branch or missing row319 producer is executed or reconstructed.
Original-input references, exact thresholds and source/operand/state/KV/lineage
gates are unchanged. Native S16 RTZ/Q24-wide residuals do not establish
strict-FP16-state W4A16; packed INT4 weights and FP16 operator/KV boundaries
remain unchanged. No precision, scale, hardware, GPU, RTL, FPGA, ACE2,
new-token or full-model claim follows. Independent Host Reviewer completion
is required before acceptance.

## Read-only selected direct-hidden modal source-only dose rank classifier

`diagnose_q24_s16_final_head_final_rmsnorm_selected_direct_hidden_modal_source_only_dose_rank_classifier_v1 --check`
consumes the reviewed modal product-term-collapse accounts, authenticating their
complete inner/outer command captures, source/test/runtime pins and immutable
Reviewer receipt. Predecessor/raw rows are bound through retained pointers using
read-only authentication helpers, without executing ancestor classifiers or
model operators.

The exact coordinate-62 absolute hidden-source doses, in descending order, are
`frozen_inherited_o` / `frozen_inherited_o_down`: `3/32768`;
`scratch` / `scratch_down`: `321/4194304`; and `mapped62`: `933/16777216`.
The respective larger-minus-smaller pairwise gaps are `63/4194304`,
`603/16777216` and `351/16777216`. Each differing control has negative
`actual_residual_boundary` movement and equal positive
`q24_to_fp16_conversion` movement. Both ordered pairs share these hidden doses;
weighted movement reverses orientation and remains exactly source delta times
the retained common factor. Canonical, down-only and native controls have zero
dose, as do all retained coordinate-241 context accounts.

The classifier requires all 20 nonzero movements and 260 zero-source accounts,
280 factor-proportionality checks and 112 reverse-component checks to close.
Arithmetic, dose, rank, gap, count or orientation failure is **REJECTED**;
authentication, missing/ambiguous fields or incompatible bindings are
**UNKNOWN**. This is descriptive retained CPU non-admission accounting, not
repair, causal or performance evidence. The missing row319 pre-round producer
and all closed branches remain untouched. Original-input references, thresholds,
lineage gates, INT4 weights and FP16 operator/KV boundaries remain unchanged;
Q24-wide residuals do not establish strict-FP16-state W4A16, new-token or
full-model admission. Normal independent Host Reviewer completion is required.

## Read-only selected direct-hidden modal source-only product-term collapse classifier

`diagnose_q24_s16_final_head_final_rmsnorm_selected_direct_hidden_modal_source_only_product_term_collapse_classifier_v1 --check`
joins the reviewed modal factor decomposition and modal source cancellation
accounts. It authenticates their retained stdout, complete command captures,
source/test bytes and immutable terminal Reviewer receipts. Raw direct-hidden
rows are accessed only through the authenticated factor account's retained JSON
pointers. The byte-authenticated predecessor module supplies read-only
authentication helpers; no ancestor classifier or producer is rerun.

The source-only product-difference prediction is **SUPPORTED** over the same
five coordinate accounts and eight modal controls, including both coordinate-62
binary64 ordered-pair mismatch partitions and the coordinate-241 context.
For each hidden source `h`, norm weight `w`, inverse-norm anchor `a` and tied-row
difference `d`, with canonical control `frozen_inherited`, the classifier checks
both exact identities without division:

```text
p - p0 = dh*w0*a0*d0 + h*dw*a0*d0 + h*w*da*d0 + h*w*a*dd
p - p0 = dh*F0 + h0*dF + dh*dF, where F = w*a*d
```

All 280 component accounts close. There are 20 nonzero weighted movements, all
source-only, and 260 zero-source accounts with no weighted movement. All 120
individual factor deltas and all 40 combined-factor deltas are zero. The
reported 1,400 zero factor-delta terms comprise 840 individual telescoping
terms and 560 terms from the alternative combined-factor identity; these are
two decompositions, not 1,400 distinct physical contributions. No factor-delta
term is nonzero.

The 5,937 exact checks include retained product identities, signed/absolute/
cancellation masses and their movements, absolute-factor weighted-mass closure,
zero signed source-delta sums, all 80 retained field mismatches, left/right
tied-row swaps and 112 reversed component accounts. Reversal preserves hidden
sources and absolute/cancellation masses while negating weighted signed
orientation. The nonzero movements remain the equal-and-opposite
actual-residual-boundary and Q24-to-FP16-conversion source deltas at coordinate
62; this does not change the parent's **REJECTED** source-equivalence result.

Nonzero individual factor changes, including compensating changes, or failed
arithmetic/source-only closure yield **REJECTED**. Authentication defects,
missing/ambiguous fields or incompatible retained bindings yield **UNKNOWN**,
never a success-shaped fallback. The diagnostic writes only stdout and reports
zero forbidden dispatch/write/operator/admission counters. Independent Host
Reviewer completion remains required.

This is descriptive retained CPU accounting, not a causal explanation,
performance result, intervention or model repair. Original-input global
references, thresholds, source/operand/state/KV/lineage gates and historical
failures remain unchanged. No prefix/admission/reference/native operator,
RMSNorm/head/row-dot producer is replayed; no row319 availability recheck or
missing-896-element reconstruction is performed. Closed branches remain
closed. Native S16 RTZ/Q24-wide residual state does not establish
strict-FP16-state W4A16, new-token or full-model admission. Packed INT4 weights
and FP16 scales/operator/KV boundaries are unchanged; there is no
precision/scale/hardware expansion or ACE2 mutation.

## Read-only selected direct-hidden modal factor decomposition classifier

`diagnose_q24_s16_final_head_final_rmsnorm_selected_direct_hidden_modal_factor_decomposition_classifier_v1 --check`
binds the reviewed modal source-cancellation coordinate accounts to the reviewed
direct-hidden residual payload's selected-coordinate rows and nested selected
head rows. It authenticates both immutable terminal Reviewer receipts, retained
stdout and captures, and the modal source/test and snapshots. No ancestor module,
producer, tensor decoder, RMSNorm, head or row-dot operator is executed.

The individual-factor equality prediction is **SUPPORTED** in this retained
domain: all 40 control-coordinate products close exactly to both retained
combined-factor fields, and all 120 individual factor fields equal their
`frozen_inherited` counterparts. There are no compensating factor changes.
The eight modal controls and all five parent coordinate accounts are retained,
including the two coordinate-62 binary64 ordered-pair partitions and the
coordinate-241 context accounts. The common inverse-norm anchor is exactly
`7350643585830161/18014398509481984`.

| Table | Ordered pair | Coordinate | Norm weight | Tied-row difference |
| --- | --- | --- | --- | --- |
| coordinate | 34319, 319 | 62 | `99/16` | `-135/8192` |
| coordinate | 319, 34319 | 62 | `99/16` | `135/8192` |
| coordinate | 34319, 319 | 241 | `25/2` | `1685/65536` |
| coordinate | 319, 34319 | 241 | `25/2` | `-1685/65536` |
| coordinate_component | 34319, 13 | 241 | `25/2` | `3101/65536` |

Each output row discloses its exact retained JSON pointers, individual factors,
left/right row-weight subtraction, product closure and factor deltas. The
ordered telescoping identity
`(w-w0)*a0*d0 + w*(a-a0)*d0 + w*a*(d-d0) = w*a*d-w0*a0*d0`
exposes compensating products without division or floating-point arithmetic.
Equal products with differing individual factors are **REJECTED**; missing or
ambiguous fields, authentication failure or failed closure produce explicit
**UNKNOWN**, not evidence of equality.

This resolves separate-factor equality only by binding the additional retained
raw rows; it does not change the earlier source-only artifact's UNKNOWN boundary
or its REJECTED source-equivalence result. It is descriptive non-admission CPU
accounting, not a causal explanation, intervention or model repair. Independent
Host review of this new classifier remains required. Original-input global
references, thresholds, source/operand/state/KV/lineage gates, accepted evidence
and historical failures are preserved. Native S16 RTZ/Q24-wide residual state
does not establish strict-FP16-state W4A16, new-token or full-model admission.
INT4 weights and FP16 scales/operator/KV boundaries are unchanged; there is no
row319 availability recheck, closed-branch reopening, precision/scale/hardware
expansion or ACE2 mutation.

## Read-only selected direct-hidden modal source cancellation topology classifier

`diagnose_q24_s16_final_head_final_rmsnorm_selected_direct_hidden_modal_source_cancellation_topology_classifier_v1 --check`
authenticates the reviewed modal component/source equivalence stdout, complete
command capture members, live source/test and retained snapshots, and immutable
terminal Reviewer receipt. It reads only those retained artifacts and its own
source/test; no ancestor module, producer or operator is imported or executed.
Scientific arithmetic uses canonical integer-rational strings, with no floating
report scalars. Capture/review timestamps are metadata, not numerical inputs.
Independent Host review of this new classifier remains required.

The input equivalence status stays **REJECTED**, with 80 mismatch rows and zero
unknown rows. The descriptive cancellation topology is **SUPPORTED** by 4,995
exact checks: all 80 rows are accounted for, and no forbidden dispatch, write,
operator or admission counters are nonzero. Forty control-coordinate accounts
cover all 280 retained component products. Coordinate 241, the
coordinate-component partition, candidate/comparator spreads, and signed sums
remain zero-mismatch.

At coordinate 62 in both binary64 coordinate partitions, the five differing
controls form three exact delta-signature groups relative to `frozen_inherited`:

| Controls | Actual residual boundary delta | Q24-to-FP16 conversion delta | Hidden absolute and cancellation mass delta |
| --- | --- | --- | --- |
| `frozen_inherited_o`, `frozen_inherited_o_down` | `-3/32768` | `3/32768` | `-3/16384` |
| `scratch`, `scratch_down` | `-321/4194304` | `321/4194304` | `-321/2097152` |
| `mapped62` | `-933/16777216` | `933/16777216` | `-933/8388608` |

`frozen_inherited_down` and `inherited_native` match the canonical control in
every retained coordinate field. Each differing control moves only the two
named sources and their absolute/cancellation masses. The hidden source deltas
sum to zero, as do their weighted deltas; the other five sources, combined
weighting factor, inverse-norm anchor and signed totals are unchanged. Hidden
absolute and cancellation mass movements equal twice the actual-boundary
delta because the retained boundary source is positive and conversion source
is negative. Weighted mass movements equal the hidden mass movement times
the absolute combined factor. Reversing the ordered pair preserves hidden
deltas and mass movements while negating signed weighted orientation.

This is exact redistribution accounting, not a causal explanation, repair,
performance result, intervention or admission. Separate weight and
row-difference equality remains **UNKNOWN**. Original-input global references,
thresholds and source/operand/state/KV/lineage gates are unchanged; no missing
row319 producer is reconstructed or rechecked and no closed branch is reopened.
Native S16 RTZ/Q24-wide residual semantics do not establish strict-FP16-state
W4A16, new-token or full-model admission. Packed INT4 weights, FP16 scales,
operator boundaries and KV remain unchanged, with no precision, scale,
hardware or ACE2 expansion.

## Read-only selected direct-hidden modal component/source equivalence classifier

`diagnose_q24_s16_final_head_final_rmsnorm_selected_direct_hidden_modal_component_source_equivalence_classifier_v1 --check`
authenticates the reviewed component-delta factorizer stdout, complete command
capture members, source/test bytes and terminal Reviewer receipt. Its pinned
read-only authentication helper is loaded from the same authenticated byte
snapshot; no ancestor producer or classifier check is called. The authenticated
outer stdout binds the embedded 3fd comparator and 449b/50f selected source rows.
Independent review of the new classifier remains required.

The retained domain is all eight modal controls relative to `frozen_inherited`
at coordinates 62 and 241 in the binary64 ordered pairs `(34319,319)` and
`(319,34319)`, and coordinate 241 in the coordinate-component pair `(34319,13)`.
Five coordinate accounts cover 40 control-coordinate accounts and 280 component
products. Exact rational fields include seven hidden sources, seven weighted
components, their sums and signed/absolute/cancellation masses, the combined
weight-times-reference-anchor-times-row-difference factor, and the separately
retained inverse-norm anchor. Individual weight and row-difference scalars are
not retained here: their separate equality remains **UNKNOWN**, not inferred
from equality of a product.

Every field reports signed differences from the canonical control, exact
max-minus-min spread, all tied extremum controls and mismatch rows. Missing
scalars explicitly produce UNKNOWN and mark extrema as partial; known
mismatches take precedence as REJECTED. SUPPORTED is limited to equality and
closure of available retained fields. Seven-component products/sums, selected
candidate and selected-plus-context sums, parent signed/absolute comparators
and exact reversed-pair orientation have separate closure accounts. The
component candidates remain a **difference**, not their sum or the other five
context components. Parent rank/tie/winner/contrast fields are compared as
retained fields, not recomputed or attributed to additive components.

The retained-field result rejects component/source equality in both
coordinate partitions while supporting it in the coordinate-component
partition. At coordinate 62, `actual_residual_boundary` and
`q24_to_fp16_conversion` each have exact hidden-source spread `3/32768`,
with equal-and-opposite per-control differences from the canonical control.
Five controls differ; `frozen_inherited_down` and `inherited_native` match
`frozen_inherited`. The corresponding weighted components and absolute/
cancellation masses also differ. All retained factor equalities, seven-term
signed sums, candidate and parent comparator closures and reversed-pair
orientation remain exact. Thus equality of retained signed comparators does
not imply equality of their retained component decomposition.

This is descriptive CPU-only evidence, not a causal, performance, repair,
intervention or admission result. Original-input references, exact thresholds
and source/operand/state/KV/lineage gates are unchanged. Native S16 RTZ, Q24-wide
residual state, packed INT4 semantics and FP16 operator/KV boundaries remain
unchanged; no strict-FP16-state W4A16, new-token or full-model admission follows.
There is no producer/operator/tensor/decoder/RMSNorm/head/row-dot replay,
row319 availability work, closed-branch reopening or precision/scale/hardware/
ACE2 expansion.

## Read-only selected direct-hidden source-only product-term collapse classifier

`diagnose_q24_s16_final_head_final_rmsnorm_selected_direct_hidden_mapped_all_exception_source_only_product_term_collapse_classifier_v1 --check`
authenticates the reviewed component-delta factorizer stdout, capture sidecars,
source/test bytes and terminal Reviewer receipt without importing its producer.
The domain remains coordinate `(34319,319)` and `(319,34319)`, and
coordinate-component `(34319,13)`, all binary64. Differences retain the signed
`mapped_all - frozen_inherited` orientation; the other seven modal controls'
component/source equality is not assumed.

The classifier requires five exactly zero coordinate weight deltas. All 35
retained product differences are checked against exact rational endpoint
products and their three-term identity. The 20 nonzero weighted movements must
equal `modal_weight_times_source_delta`; the 15 zero movements must have zero
source delta, rather than merely a zero weighting factor. Both weight-delta
product terms vanish in every account, giving 70 exact zero-term checks.
The partition counts are respectively 8/6, 8/6 and 4/3 source-only/zero-source.

Seven-component coordinate sums, parent candidate and signed comparator
closures, selected-plus-context closure, reversed-pair signed negation and
unchanged nonlinear parent fields remain explicit. Source-only is a descriptive
algebraic classification, not a causal, intervention, repair, performance or
admission claim. Original-input references and source/operand/state/KV/lineage
gates are unchanged. No producer/operator replay, closed-branch reopening or
row319 availability work is performed. Q24 residual state remains wider than
FP16; INT4 weights and FP16 operator/KV boundaries remain unchanged. This does
not establish strict-FP16-state W4A16, new-token or full-model admission.
Independent review of the new classifier is required.

## Read-only selected direct-hidden mapped-all exception component delta factorizer

`diagnose_q24_s16_final_head_final_rmsnorm_selected_direct_hidden_mapped_all_exception_component_delta_factorizer_v1 --check`
consumes the reviewed archetype stdout, capture sidecars, source/test bytes and
terminal Reviewer receipt. Nested 3fd/0146/6184/e795/449b/50f pins and retained
records remain authenticated without reopening ancestor files or importing
their producers. Independent review of this new factorizer is required.

The domain remains the three unstable binary64 partitions: coordinate
`(34319,319)` and `(319,34319)`, and coordinate-component `(34319,13)`.
The signed difference is `mapped_all - frozen_inherited`, using the reviewed
canonical modal comparator representative. Comparator equivalence of eight
controls does **not** imply component/source equivalence of those controls.

Five coordinate accounts expose all seven retained hidden-source components,
their retained weighting factors and 35 weighted component movements.
For each component, the exact product-difference identity is
`w_modal * delta_source + source_modal * delta_weight + delta_source * delta_weight`.
This is descriptive integer-rational algebra, not an intervention or a
reconstruction of missing binary64 internal stages. The seven signed sums close
to each coordinate; the two coordinate candidates close to the retained parent
candidate and exception-minus-modal signed comparator accounts.

For the coordinate-component partition, the selected terminal-remainder and
MLP-stage17 candidates at coordinate 241 remain distinct from the other five
same-coordinate components. Selected-plus-context sums close to the coordinate
account, while the selected **difference**, not their sum or the context, closes
to the parent comparator. Reversed coordinate partitions require exact negation
of weighted signed accounts and identical unweighted source movements.

Dominant classes include all tied largest nonzero absolute component deltas.
Opposing classes have polarity opposite to the net signed delta; zero-net
cancellation is explicit, with no invented aligned/opposing direction.
Absolute changes, competition ranks, winner/tie identities and dominance gaps
are non-linear retained-set properties, not additive component contributions.
No causal, performance, intervention, repair or admission conclusion follows.
Original-input global references, thresholds and source/operand/state/KV/lineage
gates are unchanged. Q24 residual state remains wider than FP16; packed INT4
weights, native S16 RTZ and FP16 operator/KV boundaries are unchanged.
The missing row-319 pre-round producer remains a limitation, not new work.

## Read-only selected direct-hidden mapped-all exception archetype classifier

`diagnose_q24_s16_final_head_final_rmsnorm_selected_direct_hidden_mapped_all_exception_archetype_classifier_v1 --check`
consumes the reviewed modal-control equivalence stdout, capture sidecars,
source/test bytes and terminal Reviewer receipt. The complete nested
0146/6184/e795/449b/50f evidence and pins are preserved without reopening
ancestor files or importing or executing their producers.

The domain is exactly the three retained unstable binary64 partitions:
coordinate `(34319,319)` and `(319,34319)`, and coordinate-component
`(34319,13)`. Each authenticated eight-control modal class collapses to
`frozen_inherited`, the first retained member, with `mapped_all` as a singleton
exception. This is equivalence of retained comparator/contrast profiles only,
not of full control trajectories or independent samples.

Every comparator field has an exact **mapped_all minus modal** delta and
**mapped_all divided by modal** ratio. A zero modal denominator is explicitly
`UNDEFINED_ZERO_MODAL_DENOMINATOR`, never replaced with zero or infinity.
The inherited nine-field contrast vector is bound to these new comparisons.
Sign, competition-rank, winner/runner-up, candidate tie/zero/maximum membership,
identity and dominance-gap transitions form partition categorical signatures.
Cross-partition comparisons separate signed orientation from exact equality of
absolute/rank/gap fields and group the magnitude-change archetypes. The focused
oracle independently derives these quantities from deeper retained selected
coordinate/component terms using integer common-denominator arithmetic.

In these retained partitions, both coordinate candidates contract in absolute
magnitude for `mapped_all`; the coordinate-component exception remains exactly
fixed while its modal competitor contracts. The two reversed coordinate pairs
have identical absolute/rank/gap comparisons and exactly negated signed values
and deltas. All three partitions exchange winner and runner-up, retain unique
winners without candidate ties, and increase the dominance gap. Their full
categorical signatures remain distinct; magnitude-change grouping has sizes
two and one. These observations concern only the retained candidates.

This is descriptive retained-rational accounting, not a causal, intervention,
performance, model-repair or admission claim. Original-input independently
propagated references, exact thresholds, historical failures and source/operand/
state/KV/lineage gates remain unchanged. No closed producer/operator replay or
row319 availability work is performed. Q24 residual state remains wider than
FP16; native S16 RTZ, INT4 weights and FP16 operator boundaries/KV are unchanged.
No strict-FP16-state W4A16, new-token or full-model admission follows.
Normal independent Host Reviewer closure is required.

## Read-only selected direct-hidden modal-control equivalence classifier

`diagnose_q24_s16_final_head_final_rmsnorm_selected_direct_hidden_modal_control_equivalence_classifier_v1 --check`
authenticates the reviewed modal-exception profiler stdout, capture, source/test
bytes and terminal Reviewer receipt. Its nested partition-localizer, stability,
hotspot-audit and direct-hidden bridge pins and complete evidence are retained
unchanged, without importing or replaying any producer.

The domain is the same three unstable binary64 partitions: coordinate
`(34319,319)` and `(319,34319)`, and coordinate-component `(34319,13)`.
Only the eight frequency-modal controls enter new pair comparisons:
28 unordered pairs per partition, 84 total. Every field delta is **left modal
control minus right modal control** in retained control order. Comparator
values, candidate gaps/ranks and mapped-all-minus-modal contrast vectors use
canonical exact rational strings; per-field spreads are exact maximum minus
minimum, with all extremum controls named.

`SUPPORTED` means all 28 pairs agree exactly on these retained numeric fields
and on signs, ranks, dominance gaps, tie and identity membership. Any mismatch
is `REJECTED`. The report separately exposes each agreement category and exact
equivalence-class membership: common winners or signs alone do not establish
numeric equivalence. This concerns only the retained comparator/contrast
profiles, not equality of complete control trajectories. The independent test
oracle uses integer common-denominator grids from deeper retained selected
coordinate/component terms, not the diagnostic's rational helpers.

The accounts remain dependent and descriptive, with no causal, intervention,
performance, repair or admission claim. Original-input independently propagated
global references, exact thresholds, historical failures and source/operand/
state/KV/lineage gates remain unchanged. No row319 availability work or producer,
prefix, admission, reference or operator replay is performed. Q24 residual state
remains wider than FP16; native S16 RTZ, INT4 weights and FP16 operator boundaries/
KV are unchanged. No strict-FP16-state W4A16, new-token or full-model admission
follows. Normal independent Host Reviewer closure is required.

## Read-only selected direct-hidden modal-exception contrast profiler

`diagnose_q24_s16_final_head_final_rmsnorm_selected_direct_hidden_hotspot_modal_exception_contrast_profiler_v1 --check`
authenticates the reviewed partition localizer stdout, capture, source/test
bytes and terminal Reviewer receipt, including the unchanged nested stability,
hotspot-audit and direct-hidden bridge pins. It retains the complete nested
evidence and capture history without importing or executing any producer.

Its computational domain is only the three variable pair/branch partitions:
the coordinate exceptions for `(34319,319,binary64)` and
`(319,34319,binary64)`, and the coordinate-component exception for
`(34319,13,binary64)`. Each has `mapped_all` as its sole minority control and
the other eight controls as its frequency-modal class. The coordinate
comparison is coordinate 62 against coordinate 241; the component comparison
is coordinate-241 terminal remainder against coordinate-241 `mlp_stage17`.

The profiler discloses 27 same-partition comparator rows, including both
candidate identities, signed and absolute values, competition ranks,
absolute/signed comparator signs, observed winners, runner-ups and dominance
gaps. Its 24 pairwise contrast rows use **mapped_all minus the named modal
control** for each signed/absolute value, comparator and dominance gap,
and expose rank deltas, rank-order signs, rank flips and full control
membership. The two-candidate absolute comparator is distinct from the
maximum-minus-runner-up dominance gap. All arithmetic uses canonical exact
rational strings, with an independent integer-grid oracle using the deeper
retained coordinate/component terms.

These are dependent descriptive accounts, not causes, performance diagnoses,
interventions or new independent observations. Original-input global references,
exact thresholds, historical failures and source/operand/state/KV/lineage
gates remain unchanged. No prefix, admission, reference, accepted producer,
tensor decoder, RMSNorm, head or row-dot is replayed. The missing row-319
pre-round producer remains a closed limitation. Native S16 RTZ and Q24-wide
residual semantics retain INT4 weights and FP16 operator boundaries/KV;
this does not establish strict-FP16-state W4A16, new-token or full-model
admission. Normal independent Host Reviewer closure is required.

## Final-RMSNorm mixed-factor ten-anchor prerequisite

The mixed-factor candidate now has a generation-time scalar prerequisite,
separate from the later classifier. Its retained operands are the nine fixed
actual 896-word FP16 stage18 vectors and the independently propagated
original-input FP16 stage18 vector. The reviewed vector-cutoff result binds
their archives, controls, reference authority, thresholds and historical
source/operand/state/KV/lineage evidence. Historical producer source identities
remain distinct from current executable-source pins; neither replaces the other.
The final norm weight is authenticated only as a downstream binding.

For each of exactly ten cases, the new capture records the exact squared sum,
mean square and radicand `mean_square + 1/1000000`, then one binary64 radicand
conversion, one binary64 square root and one reciprocal. It retains the
inverse-norm anchor as an exact rational and binary64 hex value, input-byte
bindings, exact closure residuals and separate conversion/root/reciprocal
approximation defects. This is new scalar evidence, not recovery of an earlier
hidden-delta report or an RMSNorm vector output.

All ten cases were captured. Twelve focused checks passed, including an
independent signed-integer FP16-grid mean-square oracle and exact binary64
rounding-cell checks for conversion, square root and reciprocal. The square-root
oracle compares squared rational cell boundaries rather than generating another
binary64 root. Source and test files compiled. The complete command capture
separately binds stdout, stderr and the framed whole capture.

The required normal independent Host review for this scalar prerequisite is
complete. The scalar capture itself ran no selected-head arithmetic or
scientific mixed-factor classification. The preregistered later substitutions
subtract only direct-hidden, scalar-scale or interaction from the retained
actual final-RMSNorm vector, keeping the separate actual-minus-reference
boundary remainder explicit in full closure. Their permitted scope remains rows
13, 319 and 34319 and the fixed 27 pair/control cases. Single-factor
reproduction must match every movement sign and endpoint crossing pattern;
closure alone is insufficient.

No model operator, RMSNorm vector computation, prefix/admission/reference
producer, decoder or closed-branch replay is part of this scalar capture.
Native-S16-RTZ/Q24, native INT4 weights, FP16 operator boundaries/KV, immutable
references, thresholds and historical failures remain unchanged. Q24 residual
state is wider than FP16. No strict-FP16-state W4A16, new-token or full-model
admission, causal dominance, precision expansion or hardware result follows.

## Final-RMSNorm mixed-factor cutoff classifier

`diagnose_q24_s16_final_head_final_rmsnorm_mixed_factor_cutoff_classifier_v1`
consumes the reviewed ten-anchor scalar capture and the reviewed vector-cutoff
result. It uses only retained actual/reference FP16 stage18 vectors, final norm
weights, reviewed scalar anchors and three authenticated tied-head rows. It
does not regenerate anchors, execute RMSNorm/vector/model operators, rerun
prefix/admission/reference producers or run a full-vocabulary head.

For each of the nine controls and ordered pairs `(34319,319)`, `(34319,13)` and
`(319,34319)`, the classifier evaluates three exact rational substitutions
against the retained actual final-RMSNorm vector:
`direct_hidden=(h_actual-h_reference)*weight*s_reference`,
`scalar_scale=h_reference*weight*(s_actual-s_reference)`, and
`interaction=(h_actual-h_reference)*weight*(s_actual-s_reference)`. A single
factor is supported only if its synthetic selected-row margin movement and
endpoint crossing pattern reproduce the reviewed vector-cutoff result in all
27 cases. The full direct+scale+interaction sum is checked separately with the
retained boundary margin; closure alone cannot support a single factor.

The durable result is **SUPPORTED** for `direct_hidden` only. Direct-hidden
matches 27/27 movement signs and 27/27 crossing patterns, selecting the nested
final-RMSNorm direct-hidden residual successor. Scalar-scale matches 0/27
movement signs and 9/27 crossing patterns; interaction matches 27/27 movement
signs but only 9/27 crossing patterns. All 27 full-closure cases retain zero
closure residuals. The run used 108 selected-row factor dots and 96,768 scalar
products over rows 13, 319 and 34319, with zero RMSNorm invocations, zero scalar
anchor regeneration, zero scalar RNE, zero model/operator replay and zero
artifact overwrites.

This classification is component localization only, not causal dominance or an
algorithm repair. Q24/native-S16-RTZ, INT4 weights, FP16 operator boundaries/KV,
original references, thresholds, lineage gates and historical failures remain
unchanged. No row319 provenance branch, middle/outer/dose branch, precision
expansion, strict-FP16-state W4A16, new-token or full-model admission follows.
Normal independent Host review is required for the new classifier evidence.

## Final-RMSNorm vector cutoff classifier

`diagnose_q24_s16_final_head_final_rmsnorm_vector_cutoff_classifier_v1`
authenticates the reviewed accumulation-boundary cutoff result, its retained
parent chain and original-input references. It substitutes only the independently
retained FP16 final-RMSNorm vector for the actual vector in the same exact head
on numeric rows 13, 319 and 34319. No RMSNorm, scalar RNE, producer, decoder,
prefix, admission or full-vocabulary head is executed. The original FP16 logits
remain fixed reference anchors, not substituted outputs.

For retained actual/reference logit margins `A,R`, exact vector head margins
`EA,ER`, vector account `V=EA-ER`, and boundary remainder
`H=(A-EA)-(R-ER)`, the preregistered prediction compares
`sign(ER-EA)` with `sign(R-A)` and the endpoint crossing pattern
`(sign(EA),sign(ER))` with `(sign(A),sign(R))`. Exact zero remains distinct
from either sign. Every case must agree for SUPPORTED; closure alone cannot
satisfy the prediction. The observed swap movement closes as
`ER-EA = -V = (R-A)+H`, keeping the head remainder separate.

The bounded result is SUPPORTED: all 27 ordered pair/control cases reproduce
both the retained movement direction and crossing pattern. All seven exact
closure residuals per case are zero. The computation uses 30 selected-row dots,
26,880 scalar products and no scalar RNE; an independent signed-integer FP16
grid oracle checks every dot. All 54 retained FP16/binary64 parent identities
are authenticated and checked, but no binary64 vector arithmetic is repeated.
Stored validation checks captured scalar identities and byte bindings without
recomputing vector dots.

SUPPORTED selects component localization within the final-RMSNorm vector
(direct-hidden, scalar-scale or interaction), not causal dominance or an
algorithm repair. REJECTED instead selects an upstream retained hidden-vector
or RMSNorm scalar-boundary classifier rather than head accumulation; UNKNOWN
allows integrity repair only. These ordered pairs are not independent samples.
Independent review remains required. Q24/native-S16-RTZ, INT4 weights, FP16
operator boundaries/KV, original references, thresholds, lineage gates and
historical failures remain unchanged. No row319 provenance, middle/outer/dose
branch, precision expansion, strict-FP16-state W4A16, new-token or full-model
admission follows from this result.

## Selected-row final-head accumulation-boundary cutoff classifier

`diagnose_q24_s16_final_head_accumulation_boundary_cutoff_classifier_v1`
uses the original bridge command capture's 54 retained accounts, not a
reconstruction of its unretained full stdout. The three fixed numeric rows
are 13, 319 and 34319. Only selected-row dot products against retained actual
and independent RMSNorm vectors are computed; no cutoff search is repeated.

For retained margins `A,R`, exact dot margins `EA,ER` and parent
`H=(A-EA)-(R-ER)`, the preregistered prediction is movement `-H` and endpoint
signs `(EA,ER,EA-ER)`. Both actual and independent-FP16 inputs receive a
diagnostic exact-dot/FP16-RNE shadow head. The original reference trajectory
and logits remain immutable anchors; binary64 receives no new FP16 boundary.
The observed relative movement closes exactly to `-H` plus the difference
of the two scalar-RNE margin remainders. A zero is distinct from either
direction, so rounding can reject the prediction despite exact closure.

All 27 FP16 pair/control direction and crossing-pattern tests must agree for
SUPPORTED (narrower accumulation/order/RNE-cell work). REJECTED selects
final-RMSNorm-vector work; UNKNOWN permits only integrity/availability repair.
These repeated ordered pairs are not independent samples. This is neither
a reference-independent repair nor a root-cause claim. Existing failures,
thresholds, Q24/native-S16-RTZ state and FP16 operator/KV boundaries remain
unchanged. No prefix, decoder, admission, reference-producer, RMSNorm or
full-vocabulary head replay, token publication, precision expansion or
strict-FP16-state/new-token/full-model admission is established.

The bounded measurement rejects the joint prediction: all 27 relative margin
movements are exactly zero, giving 0/27 directional agreements despite 27/27
endpoint-sign agreements. All 54 FP16/binary64 parent accounts close exactly.
The computation uses 33 selected-row dots (29,568 scalar products) and 30
FP16 scalar RNE conversions; no binary64 reference is rerounded. This result
selects only the preregistered final-RMSNorm-vector successor and remains
subject to independent review.

Preregistration, result and raw-capture command/metadata disclose the same
Python version and byte-bound interpreter plus exact NumPy/Torch versions.
Focused tests require these pins and reject missing or changed disclosures.
Stored-evidence validation binds all four disclosures to the validating
runtime without repeating selected-row arithmetic.

## Read-only final-head accumulation boundary residual margin bridge

`diagnose_q24_s16_final_head_accumulation_boundary_residual_margin_bridge_v1 --check`
authenticates reviewed final-head attempt001 and independently propagated
original-input final attempt003, the existing cutoff/coordinate-contribution
source pins, mixed-RMSNorm retained operands and runtime, and the final RMSNorm
hidden-delta/logit-margin inputs. It splits only the existing head-boundary
remainder, separately for every ordered diagnostic pair, control and FP16 or
binary64 reference branch.

For retained pair margins `A` and `R` and exact selected-row dot margins `EA`
and `ER`, the two components are `A-EA` and `-(R-ER)`. Their sum equals the
retained `head_boundary_remainder_change`; `EA-ER` equals the full
vector-coordinate sum. Thus `A-R = (EA-ER) + (A-EA) - (R-ER)` exactly.
Both retained margins, both exact margins, all four row dots, vector signed
and absolute sums, individual boundary effects, cancellation accounts and
zero-valued closure residuals are retained in normal stdout. Selected and
unselected final RMSNorm accounts remain unchanged and the upstream margin
report is included. Aggregate accounts stay separate by reference branch;
repeated ordered pairs are not independent samples or causal shares.

The only newly permitted computation is exact arithmetic for existing
selected FP16 tied-head rows against retained actual FP16 and independent
FP16/binary64 RMSNorm vectors. Row dots are cached by actual control or
reference branch and numeric row ID; both dot and scalar-product counts are
reported. No full-vocabulary matvec, RMSNorm, native/decoder/prefix/admission,
reference-producer or mixed-head counterfactual is replayed. This diagnostic
does not extend the unselected-coordinate category-selection gate.

These combined accumulation/order/conversion effects are not a rounding-only
or causal root-cause attribution. Exact thresholds, historical failures,
original global references, tokenizer/tied-head identity and source/operand/
state/KV/lineage gates remain intact. Old sparse-cut closure cannot certify,
substitute for or propagate the nine-control parents. Q24 state remains wider
than FP16; native S16 RTZ, INT4 weights and FP16 operator boundaries/KV remain
unchanged. No evidence writes, artifact overwrite, token selection/publication,
hardware/GPU/RTL/simulation/ACE2 work, precision/scale expansion, admission,
strict-FP16-state W4A16, new-token or full-model claim is authorized.

The stdout-only check compiles both changed Python files in memory and runs
focused independent retained-bit arithmetic, exact cancellation, mutation
refusal, single-JSON CLI and forbidden-dispatch/write tests. Normal independent
Host review is required.

Command:
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_final_head_accumulation_boundary_residual_margin_bridge_v1 --check`.

## Selected-head row319 control-specific rounding-boundary classifier

`diagnose_q24_s16_terminal_remainder_selected_head_row319_control_specific_rounding_boundary_classifier_v1 --check`
authenticates the independently reviewed retained-byte family contrast and its
reverse-quartile/dyadic parents, including sealed command streams, source/test
pins, independent review authority and preserved original-input global
references. It analyzes only reverse doses `65/2048`, `33/1024`, `67/2048` and
`17/512`, the fixed nine controls and rows 319/34319.

Unlike absolute-profile grouping, this classifier compares exact numeric
after-minus-baseline responses. The earliest nonuniform stage selects a
descriptive boundary only when the same field separates `mapped_all` alone at
all four doses: pre-RMSNorm operand FP16 increments, then RMSNorm integer
scalar/output increments, then individual selected-head rounding residuals.
Partial, shared or rotating separation at that stage is mixed/inconsistent.
Each classification has a distinct preregistered question, not permission to
execute it. Equal upstream increments do not imply equal absolute operands.
Neither this ordering nor retained floor-root cell slacks establish rounding-only
causality, a dominant stage, an actual-suffix threshold or a production repair.

The bounded retained-byte measurement selects pre-RMSNorm operand FP16 words:
exact operand increments separate `mapped_all` alone at all four doses, across
36 control/dose observations and 72 selected-row observations. Thus the
discriminator is already present in the operand response, not solely in the
absolute baseline profile. This does not exclude downstream rounding effects
or establish that those operand increments cause the selected-logit activation.
The preregistered operand rounding-cell question is selected for consideration
only; this classifier has completed independent review.

The reviewed bytes contain no individual selected-head exact/post-hidden-RNE
accumulators. This stage is explicitly unavailable; logits or margin differences
cannot supply those scalars. An earlier valid response discriminator remains
descriptive only. Reaching an unavailable stage, having no separating field, or
failing integrity selects `UNKNOWN/integrity` and no scientific successor until
the exact prerequisite is supplied and independently reviewed. No head dot
product, RNE execution, RMSNorm recomputation or suffix construction fills gaps.

The stdout-only CPU check compiles its source/tests in memory and executes
focused independent bit-arithmetic and integrity tests. Raw command capture
sealing is external to its zero-write boundary. Both `dose_search_closed` and
`global_threshold_model_closed`, historical FAIL/UNKNOWN/rejections, exact
thresholds and source/operand/state/KV/lineage gates remain unchanged.
Native S16 RTZ, INT4 weights, FP16 operators/KV and wider Q24 residual state
remain non-admission evidence, not strict-FP16-state W4A16, new-token or
full-model admission. New doses, bisection, coordinate selection,
prefix/admission/reference-producer/full-vocabulary replay, hardware/GPU/RTL,
ACE2, precision/scale expansion and production action remain forbidden.

## Selected-head row319 retained operand rounding-cell audit

`diagnose_q24_s16_terminal_remainder_selected_head_row319_retained_operand_rounding_cell_audit_v1 --check`
authenticates the reviewed control-specific boundary classifier and its three
reviewed retained-byte, reverse-quartile and dyadic parents. Only sealed
baseline/dose operands for `65/2048`, `33/1024`, `67/2048` and `17/512` across
the fixed nine controls are analyzed, in all-896-coordinate input order.

Exact per-coordinate FP16 word moves are paired with sign, significand parity,
numeric neighbor steps, rational cell endpoints and even-tie ownership.
Word-derived geometry is not an actual rounding-event binding: actual ties,
cell slacks and rounding residuals require independently authenticated retained
unrounded operands. A frozen terminal correction vector does not supply this
binding without forbidden operand reconstruction. Missing values are never
replaced by rounded word centers or inferred from logits or margin differences.

The preregistered outcomes are supported retained operand cell signature,
word-move-only/no cell binding, mixed/inconsistent controls, or
`UNKNOWN/integrity`. Support requires complete actual cell bindings and a
stable mapped-all-only signature at every fixed dose. Missing all unrounded
bindings permits only the word-move classification when the word separator
holds. Partial/malformed bindings or failed authentication select UNKNOWN.
Supported cells select a separately reviewed retained-cell/outcome association
question; absent bindings select an existing-sealed-provenance availability
question; mixed controls select a fixed-family consistency question. UNKNOWN
permits no scientific successor until its exact prerequisite is reviewed.
None authorizes execution of its successor or another dose search.

The bounded measurement is **word-move-only/no cell binding**: all four doses
retain a mapped-all-only exact operand-delta separator across 32,256 coordinate
pairs. Nine baseline vectors contain 16,128 FP16 bytes and 36 dose vectors
contain 64,512 FP16 bytes. Cell geometry is available, but none of the 72
baseline/dose record occurrences contains an unrounded operand binding.
Actual tie/cell bindings therefore remain unavailable, not false or inferred.
This result is independently reviewed and does not establish a rounding
mechanism.

The stdout-only CPU check compiles both Python files in memory and runs focused
independent binary16 decoding, neighbor/tie, integrity and retained-reference
closure tests. Raw stdout/stderr and whole-command capture sealing is external
to the diagnostic's zero-write boundary. Independent Host review is required.
Original references, exact thresholds, source/operand/state/KV/lineage gates,
historical FAIL/UNKNOWN and rejected outcomes remain intact. Both
`dose_search_closed` and `global_threshold_model_closed` stay true.
No suffix construction/rerounding, coordinate intervention, new dose/bisection,
prefix/admission/reference-producer/full-vocabulary replay, production repair,
hardware/GPU/RTL/ACE2 or precision/scale expansion is authorized. Native S16
RTZ, INT4 weights, FP16 operators/KV and wider Q24 residual state remain
non-admission evidence, not strict-FP16-state W4A16, new-token or full-model
admission.

## Selected-head row319 unrounded operand provenance availability

`diagnose_q24_s16_terminal_remainder_selected_head_row319_unrounded_operand_provenance_availability_v1 --check`
authenticates the independently reviewed retained rounding-cell audit and its
boundary, retained-byte, reverse-quartile and dyadic parents. Its question is
whether their pinned captures, build outputs and explicitly linked operand
provenance retain actual unrounded operands or exact cell bindings, not whether
rounded-word geometry can be computed again.

The bounded search enumerates exact paths, SHA-256, byte sizes, reference
locations, structured field locations and array member names/dtypes/shapes.
Source/test and review/history pins are authentication inputs, not operand
evidence. Whole-command captures and separate streams retain distinct hashes.
Only explicitly named, hash/size-bound operand-provenance links extend the
search; no directory crawl or model/weight/tokenizer loading is authorized.
Absence is confined to this enumerated scope, not the whole filesystem.

The row319 matrix covers nine baseline records and 36 retained reverse-dose
records at `65/2048`, `33/1024`, `67/2048` and `17/512`: 36 control/dose rows
and 72 baseline/dose occurrences. Complete availability requires consistent
all-896-coordinate exact bindings for all 45 distinct records. Unrounded
values must be canonical exact rationals bound to the retained operand words.
Exact cell bindings must carry word/sign identity and nonnegative rational
lower/upper slacks closing the cell with correct RNE tie ownership. Cell
membership is checked without recovering operands from slacks or rerounding.
Rounded words, Q24 state and independently propagated reference arrays are
not substitutes for actual unrounded operand provenance.

The existing `s16_unrounded_binary64` archive field is a separate,
4864-element S16 SiLU/product operand. Its pinned native producer declaration
and matching stage16 shape identify a different boundary from the
896-coordinate pre-final-RMSNorm baseline/dose operand. It is reported as
non-target retained unrounded data, not discarded silently, relabeled as a
final operand, or counted as malformed final-operand provenance.

The bounded measurement is **absent availability**: the authenticated artifact
census has 278 entries, including authentication-only inputs explicitly
distinguished from operand searches. None of the 45 fixed target records has
an unrounded operand or exact cell binding. All 36 retained S16 unrounded
array fields are explicitly identified as non-target, with no target
provenance issues. This measurement is pending normal independent review;
it neither establishes a rounding mechanism nor proves that unreferenced
artifacts cannot exist.

The four outcomes have materially different gates. **Complete availability**
permits only a separately preregistered retained-cell association question.
**Absent availability** closes this retained-cell mechanism route for the
searched artifacts and selects a genuinely distinct CPU question.
**Partial/malformed provenance** permits only correction of identified
already-existing provenance through new immutable bindings and independent
review, not a scientific successor. **UNKNOWN/integrity** blocks scientific
succession until the exact missing authentication prerequisite is reviewed.
Missing linked bytes or hash/size failures can never become absence.

Both dose-search and global-threshold-model closure remain in force for every
outcome. Original references, exact thresholds, source/operand/state/KV/lineage
gates and historical FAIL/UNKNOWN and rejected outcomes remain unchanged.
No operand reconstruction, suffix rerounding, prefix/admission/reference
producer replay, new dose, coordinate selection, precision/scale expansion,
GPU/RTL/hardware/ACE2 or production action is authorized. Native S16 RTZ,
INT4 weights, FP16 operators/KV and wider Q24 residual state remain bounded
CPU non-admission evidence, not strict-FP16-state W4A16, new-token or
full-model admission. Fresh in-memory compilation and focused tests run
inside the stdout-only check; raw command sealing remains outside its
zero-write boundary. Independent Host review is required.

## Selected-head row319 retained-byte family contrast

`diagnose_q24_s16_terminal_remainder_selected_head_row319_retained_byte_family_contrast_v1 --check`
reads the sealed, independently reviewed reverse-quartile capture and the
reviewed dyadic-straddle receipt. It authenticates whole-capture and separate
stdout/stderr bindings, source/test pins and inherited evidence before comparing
only retained reverse observations at `65/2048`, `33/1024`, `67/2048` and
`17/512`, for the fixed nine controls and rows 319/34319.

The preregistered discriminator groups exact equality separately for operand
FP16 words, changed-coordinate lists, RMSNorm FP16 outputs and RMSNorm integer
scalars. A field with a singleton `mapped_all` group at all four retained doses
selects one control-specific rounding-boundary question. Otherwise a nontrivial
partition selects one family-specific operand/rounding question. Identical
profiles or missing bytes/bindings select `UNKNOWN/integrity`, with no scientific
successor until the exact prerequisite is supplied and independently reviewed.
Selected-logit words, control labels, fixed references and historical gate
outcomes are not discriminator features. Baseline profiles are disclosed:
byte separation is not necessarily dose-induced, a rounding-only causal
attribution, an actual-suffix threshold proof, or an algorithm repair.

The report retains full operand/output words, differences against all eight
controls, scalar differences, both selected-row activities, and exact closure
against independently propagated original-input FP16/binary64 references.
Changed coordinates are checked against authenticated retained stage18 bytes;
no operand is constructed or rerounded. Original source/operand/state/KV/lineage
gates, exact thresholds, historical FAIL/UNKNOWN and dyadic rejection remain
unchanged. Both `dose_search_closed` and `global_threshold_model_closed` remain
true for every outcome. No new dose, bisection, coordinate selection, prefix,
admission, reference-producer or full-vocabulary replay is allowed.

The retained-byte result separates `mapped_all` alone from an identical group
of the other eight controls in operand words, RMSNorm output words and RMSNorm
integer scalars at all four doses. Changed-coordinate lists are identical
across all nine controls at each dose and are not a discriminator. The three
singleton fields also distinguish the retained baseline, so this result does
not establish which rounding boundary causes the early activation. It selects
the independently reviewed control-specific rounding-boundary question,
not further dose search. The comparison covers 36 retained observations,
32 mapped-all/control contrasts and 144 exact ordered-pair/reference closures.

This stdout-only CPU check compiles its source and focused tests in memory.
External raw capture sealing is separate from the diagnostic's zero-write
boundary. Native S16 RTZ, INT4 weights, FP16 operator/KV boundaries and wider Q24
state remain unchanged; no strict-FP16-state W4A16, new-token, full-model
admission or hardware claim follows. Independent Host review remains required.

## Selected-head row319 reverse quartile suffix threshold

`diagnose_q24_s16_terminal_remainder_selected_head_row319_reverse_quartile_suffix_threshold_v1 --check`
authenticates the independently reviewed dyadic-straddle rejection and its
original source/test, reference, operand, state/KV and lineage bindings.
It executes only three new reverse terminal-vector doses:
`1/32 < 65/2048 < 33/1024 < 67/2048 < 17/512`. Integer cross-products
certify this exact dyadic ordering. Retained scalar transfer predicts row 319
inactivity at all three new doses for every control; the authenticated prior
suffix already measures row 319 activity at `17/512` in all nine controls.
Neither endpoint nor the prior `9/256` dose is recomputed.

Each new operand is
`RNE_FP16(actual_FP16_stage18 + dose * frozen_terminal_remainder)`,
using all 896 coordinates in input order. Only final RMSNorm and selected
head rows 319/34319 execute. Baseline logits and independently propagated
original-input FP16/binary64 references remain fixed. The 27 suffix jobs
produce 36 ordered-pair/reference closure accounts per dose, not independent
samples. Row 34319 is measured separately, so cancellation cannot hide row
319 activation.

First measured row 319 activation at `65/2048` selects the near-boundary
quartile; at `33/1024` or `67/2048`, the second or third (middle) quartile.
No new-dose activity selects the upper quartile using the retained active
`17/512` endpoint. Different first quartiles select control-family separation.
This is the terminal experiment of the dose/threshold-localization branch:
after normal independent review, **every outcome closes dose search**,
including near-boundary, middle, upper, mixed, rejected and `UNKNOWN` results.
A uniform result may motivate a distinct control-specific rounding-mechanism
question using retained bytes, not further threshold localization. A mixed
result also closes the global-threshold model and may motivate at most one
preregistered family-contrast question using retained bytes. These possible
questions require new preregistration and independent review; they are not
authorized executions. `UNKNOWN` permits no scientific successor until its
exact missing integrity prerequisite is supplied and independently reviewed,
and never reopens dose search. No outcome permits another dose subdivision,
mini-bisection, coordinate choice, reference mutation or scalar-threshold
transfer claim.
Activity reversions are disclosed rather than assumed impossible. These are
sampled intervals, not proofs of monotonicity, continuous onset, or a causal
rounding-stage attribution. Integrity failures yield `UNKNOWN/integrity`;
valid early activation rejects the scalar prediction, not the measurement.

The stdout-only check compiles both new Python files in memory and runs
focused tests with independent rational/FP16-bit operand, integer RMSNorm,
selected-dot, scalar-threshold and original-reference oracles. Dispatch gates
allow exactly 27 new final RMSNorm calls and 27 two-row heads; filesystem
writes, prior diagnostics, closed doses, prefix/admission/reference producers
and full-vocabulary execution are forbidden.

The measured result **rejects the all-control scalar inactivity prediction**
and selects **mixed by control family**, closing dose search and the
global-threshold model after independent review, not selecting another
suffix-threshold search. It may motivate only the distinct preregistered
family-contrast question described above. `mapped_all` is already active
at `65/2048`: row 319 moves from word 18462 to 18463, and remains there at
both other new doses. The other eight controls retain row 319 word 18461
at all three new doses; their first retained active observation is `17/512`.
Thus `mapped_all` selects quartile 1 and the other eight controls select
quartile 4. No sampled activity reversions occur.

All three doses have 32/36 scalar-margin and individual-word agreements.
For order `(319,34319)`, the measured margin movement is `+1/64` for
`mapped_all` and zero for the other eight controls; reversing the pair
negates the movement. Both fixed-reference branches close exactly.
The 15 focused tests pass with both Python files compiled in memory and
exactly one native stdout JSON document. The dispatch census is 27 operands,
24192 operand scalar roundings, 27 final RMSNorm calls, 27 selected-head calls
and 54 row accumulations; every forbidden replay/write/overwrite counter is
zero. The closed dyadic rejection remains 36/72 and 40/72 agreements.
The bounded reverse-quartile result has completed independent Host review.
The diagnostic remains stdout-only. A new scientific execution requires
external complete byte-exact stdout/stderr capture sealed to a unique ignored
workspace build output before review, with original temporary path, exact
command/task/attempt, source/test pins, source/destination whole-capture sizes
and SHA-256 bindings, and a separate stdout binding. A stdout hash is not a
whole-capture hash; missing original bytes or bindings must remain explicit
gaps, never reconstructed from summaries.

Historical FAIL/UNKNOWN, closed rejection counts, exact thresholds and
source/operand/state/KV/lineage gates remain unchanged. Q24 residual state is
wider than FP16; native S16 RTZ, INT4 weights and FP16 operator/KV boundaries
remain unchanged. This is CPU-only reference-guided non-admission evidence,
not production repair, precision/scale expansion, strict-FP16-state W4A16,
new-token or full-model admission. Independent Host review is required.

## Selected-head row319 dyadic straddle suffix contrast

`diagnose_q24_s16_terminal_remainder_selected_head_row319_dyadic_straddle_suffix_contrast_v1 --check`
authenticates the reviewed crossing, bracket and asymmetric-localizer receipts.
For `mapped_all` reverse, the retained baseline accumulator, directed FP16
midpoint and signed final-hidden slope derive the exact inequalities
`17/512 < 49635103879110590464/1472273206149121574245 < 9/256`
for row 319, while row 34319's threshold
`13580686040196186112/775451185250281528043` is strictly below `1/32`.
Integer cross-products prove the strict inequalities; nearest-even midpoint
ownership remains explicit. These slopes predict scalar row words, not
interpolated hidden-rounding remainders or a working-dose threshold.

Only the two new doses construct working operands:
`RNE_FP16(actual_FP16_stage18 +/- dose * frozen_terminal_remainder)`.
The remainder is the unchanged original-input FP16 stage18 minus independent
binary64 terminal vector, including all 896 coordinates in input order.
Each of nine controls and two polarities runs only final RMSNorm and the
two selected head rows. Retained baseline logits avoid baseline suffix replay.
Each dose has 72 ordered-pair/reference contrasts; these are closure accounts,
not 72 independent samples. FP16 and independently propagated original-input
binary64 references stay separately fixed.

Support requires all scalar-margin and individual-row predictions at both
doses, including all four `mapped_all` reverse accounts: row 319 stays inactive
at `17/512` and becomes active at `9/256`, with row 34319 already active.
Control or early-row disagreement selects a control-specific suffix/rounding
successor; lower-dose movement, upper-dose inactivity and other selected-row
disagreement select distinct non-threshold/rounding successors. Authentication,
arithmetic, dispatch, census or algebraic closure failure is `UNKNOWN/integrity`,
not a scientific rejection. A valid model mismatch does not invalidate the
measured result.

The stdout-only check compiles both current Python files in memory and runs
focused pytest with independent FP16-bit/rational operand, selected-dot,
threshold and reference-margin oracles. All 36 suffix jobs are operand- and
row-gated; prefix/admission/reference producers, closed interventions,
full-vocabulary dispatch and filesystem writes remain forbidden.

The independently reviewed result **rejects dyadic-straddle threshold support**
and selects the **control-specific suffix/rounding successor**.
The exact retained-scalar inequalities remain valid, but do not
transfer to the terminal-vector suffix at these two doses:

| Dose | Scalar-margin agreements | Individual-row agreements | `mapped_all` reverse agreements |
| --- | --- | --- | --- |
| `17/512` | 36/72 | 36/72 | 0/4 |
| `9/256` | 40/72 | 40/72 | 4/4 |

Both doses measure `-1/128` forward for order `(319,34319)`, `+1/128`
reverse for the other eight controls, and `+1/64` reverse for `mapped_all`.
Reverse pair order negates these movements; both reference branches close
exactly. At the lower dose, `mapped_all` row 319 already moves from word
18462 to 18463, contrary to the scalar prediction. The other eight reverse
controls also move row 319 despite predicted inactivity at both doses.
Row 34319 matches its prediction. The preregistered control-divergence
precedence therefore applies even though lower-dose movement also occurs.
This comparison establishes a model-transfer failure, not which suffix
boundary causes it. All 14 focused tests pass, both Python files compile in
memory, and the executable emits one stdout JSON document. The 36 RMSNorm
and 36 selected-head calls include 72 selected-row accumulations; every
forbidden replay/write/overwrite counter is zero.

Historical FAIL/UNKNOWN, exact thresholds and both closed dose rejections
remain unchanged. Q24 residual state is wider than FP16; native S16 RTZ,
INT4 weights and FP16 operator/KV boundaries are unchanged. This is CPU-only
non-admission evidence, not production repair, precision/scale expansion,
strict-FP16-state W4A16, new-token or full-model admission. Normal independent
Host review is required.

## Selected-head asymmetric RNE threshold localizer

`diagnose_q24_s16_terminal_remainder_selected_head_asymmetric_rne_threshold_localizer_v1 --check`
authenticates the sealed independent crossing and bracket reviews, retained
individual scalars, original source/token/Q24/state/KV/lineage/head-row identities,
both fixed reference branches and all closed dose outcomes. It does not execute
either closed diagnostic. Exact scalar arithmetic partitions each row into
early-active, late-threshold or inactive at the two retained endpoints.

The new question concerns the **increment from `1/32` to `3/64`**, not the
entire movement from baseline. Subtracting each row's independently rounded
endpoint values gives two signed contributions whose sum must equal the
closed margin increment. Holding either retained rounded row at its lower
endpoint supplies a necessary/sufficient scalar account without a new
operand intervention or head computation.

The preregistered row319-late outcome requires all four `mapped_all` reverse
ordered/reference accounts to attribute the entire nonzero bracket increment
to newly active row 319 while row 34319 was active strictly before `1/32`
and remains unchanged across the bracket. Exclusive increment attribution to
already-active row 34319 selects the row34319/early-active-dominance successor.
Another valid row-state or both-row explanation selects the asymmetric-mixed
successor; authentication, arithmetic or closure failure is `UNKNOWN/integrity`
without a scientific successor. Here dominance means exclusive scalar
increment accounting, not runtime, causal or full-model dominance.

For the retained bracket words, order `(319,34319)` has an early contribution
of `1/128` from row 34319 and a further `1/128` from row 319, summing to
`1/64` at `3/64`. Thus exclusive localization of the **increment** must not be
misreported as row 319 explaining the entire baseline-to-`3/64` movement.
Reverse order negates these quantities; fixed FP16/binary64 reference copies
are closure accounts, not independent samples.

The executable result selects **row319-late-threshold localization**, pending
independent Host review. All four coupled increment accounts and all 72
ordered/reference increment accounts close exactly, with 72/72 agreements at
each bracket endpoint and 504/504 closed-dose activation/direction accounts.
Both current Python files compile in memory and all 14 focused pytest tests
pass. Every forbidden dispatch/replay/write/overwrite counter is zero.

The native stdout-only check compiles the current source and focused test in
memory, runs focused pytest with an independent FP16-bit nearest-even oracle,
and checks endpoint/reference closure, alternate successors, mutations and
forbidden dispatch/writes. Original FAIL/UNKNOWN, exact thresholds, both
closed dose rejections and independently propagated references stay intact.
No prefix/admission/reference producer, RMSNorm, full-vocabulary, selected-row
dot or closed diagnostic is replayed; working-dose remainders are not
interpolated and larger-dose rounded magnitudes are not inferred.
Q24 residual state remains wider than FP16; native S16 RTZ, INT4 weights and
FP16 operator/KV boundaries are unchanged. This is CPU-only non-admission
accounting, not repair, precision/scale expansion, strict-FP16-state W4A16,
new-token or full-model admission. Normal independent Host review is required.

## Selected-head coupled RNE dose threshold bracket

`diagnose_q24_s16_terminal_remainder_selected_head_coupled_rne_dose_threshold_bracket_v1 --check`
authenticates the independently reviewed crossing-attribution result, its
retained scalar receipt, closed dose receipts and original source/token/Q24/
state/KV/lineage/reference/head-row gates. It uses only retained individual
baseline, exact-target and working accumulator scalars, FP16 words and frozen
row slopes for rows 319 and 34319 across all nine controls and both polarities.

For baseline accumulator `b`, signed row dose slope `s` and the next directed
FP16 midpoint `m`, the exact threshold is `(m-b)/s`. Dose thresholds need not
be dyadic even though the retained accumulators are. The baseline owns a tie
if its significand is even; otherwise the neighboring word owns it. Reports
retain exact accumulator/dose slacks at `1/32` and `3/64`, baseline/crossed
dose intervals with endpoint ownership, and both rows' threshold infima.
The crossed interval means departure from the baseline cell, not continued
membership in a single neighboring word. Working scalars close only their
retained `3/64` endpoint; their hidden-RNE remainders are not interpolated.

The preregistered joint prediction requires both thresholds in every
`mapped_all` reverse coupled contrast to lie strictly after `1/32` and to
cross at/before `3/64`, with the other 68 contrasts single-row and no closed
dose contradiction. It selects **minimal joint-dose bracket localization**.
One already-active row or a valid threshold/closed-outcome discrepancy rejects
that prediction and selects the **asymmetric/single-row threshold successor**.
Authentication, arithmetic or retained contrast closure failures instead yield
`UNKNOWN/integrity`, with no scientific successor.

Both bracket endpoints compare individually rounded row margin deltas exactly.
For the larger closed doses (`1/16`, `1/8`, `1/4`, `1/2`, `1`), consistency
means only first-midpoint/nonzero and direction agreement, not an uncomputed
rounded-margin magnitude. FP16 and independently propagated original-input
binary64 references remain separately fixed. Ordered/reference duplicates
are closure accounts, not independent samples.

The executable result, pending independent Host review, rejects the joint
prediction and selects the **asymmetric/single-row threshold successor**.
For `mapped_all` reverse the exact thresholds are:

| Row | Directed midpoint | Dose threshold | Crossing at equality |
| --- | --- | --- | --- |
| 319 | `2109/256` | `49635103879110590464/1472273206149121574245` | No; baseline owns the tie |
| 34319 | `2165/256` | `13580686040196186112/775451185250281528043` | Yes; neighbor owns the tie |

Row 34319 has already crossed before `1/32`; only row 319 crosses between
`1/32` and `3/64`. Thus the four ordered/reference copies of the coupled
contrast do not require two newly crossed thresholds in that bracket.
For order `(319,34319)`, the closed `1/32` movement is already `+1/128`,
and the `3/64` movement is `+1/64`. The global closed `1/32` rejection
does not mean every control/polarity contrast was zero. The other 68
contrasts remain single-row. All 36 threshold equations, 108 retained
scalar cells and 72 exact-target/working contrasts close; exact margin
agreement is 72/72 at each bracket endpoint. First-crossing/nonzero and
direction consistency is 504/504 over seven closed doses, without claiming
larger-dose rounded-magnitude equivalence. Both closed dose rejections
remain unchanged.

The stdout-only check compiles both current Python files in memory and runs
focused independent FP16-bit, rational threshold, tie, endpoint, reference,
mutation and forbidden-dispatch/write tests. No prefix/admission/reference
producer, RMSNorm, full-vocabulary, selected-row dot, closed stage18/head-only,
scalar or crossing diagnostic is replayed. There are no artifact writes or
overwrites. Historical FAIL/UNKNOWN, original thresholds and closed outcomes
remain intact. Q24 residual state remains wider than FP16; native S16 RTZ,
INT4 weights and FP16 operator/KV boundaries are unchanged. This is bounded
CPU-only non-admission accounting, not production repair, precision/scale
expansion, strict-FP16-state W4A16, new-token or full-model evidence.
Normal independent Host review is required.

## Selected-head retained-scalar RNE crossing attribution

`diagnose_q24_s16_terminal_remainder_selected_head_rne_crossing_attribution_v1 --check`
uses the reviewed scalar classifier's retained individual accumulator rationals
and FP16 words, not scalar recovery or selected-row dot recomputation. It
authenticates the scalar source/test/review/receipt, the original retained
source/token/Q24/state/KV/lineage/reference/head-row identities, closed dose
outcomes and current diagnostic source/test identities.

For each of 36 control/polarity/row records, the diagnostic accounts exactly
for both adjacent nearest-even midpoints, baseline/target/working distances,
tie parity, signed exact-dose and working movement, remaining threshold slack,
individual word changes and hidden-RNE accumulator remainder. It reproduces
72 ordered/control/reference margin contrasts by subtracting individually
rounded rows; FP16 and independently propagated original-input binary64
global references remain separately fixed. Exact-dose precedence is preserved.

The falsifiable split selects **single-row RNE threshold localization** only
if every contrast has one crossing row and a non-crossing paired row.
Any two-row crossing or opposite row compensation selects **two-row coupled
threshold localization**. Receipt authentication, midpoint or contrast closure
failure is `UNKNOWN/integrity`, with no scientific successor. Ordered/reference
duplicates are closure accounts, not independent samples or causal shares.

The independently reviewed executable result closes **108/108**
individual scalar midpoint accounts, **72/72** row slack accounts, and
**72/72** ordered/reference contrasts in each of the exact-target and working
branches. **68 contrasts have one crossing row; four have both rows crossing**,
with zero opposite-compensation contrasts. The universal single-row prediction
is therefore rejected and the selected successor is **two-row coupled
threshold localization**. The coupled case is `mapped_all` reverse, repeated
for both row orders and both fixed reference branches: row 319 moves upward
one FP16 word and row 34319 downward one word, contributing together to the
`1/64` margin increase in order `(319,34319)`. This does not promote that
bounded contrast into a causal-dominance or model-repair claim.

The stdout-only native check compiles both current Python files in memory
and runs focused independent nearest-even, midpoint/slack, row/reference
closure, mutation and forbidden-dispatch/write tests. It does not replay
prefix/admission/reference producers, RMSNorm, full-vocabulary or selected-row
head computation, closed stage18/head-only interventions, or the closed scalar
diagnostic. Historical FAIL/UNKNOWN evidence, exact thresholds and closed
outcomes remain unchanged. Q24 state remains wider than FP16; INT4 weights
and FP16 operator/KV boundaries remain unchanged. This is bounded CPU-only
non-admission accounting, not repair, strict-FP16-state W4A16, new-token,
full-model, causal-dominance or production evidence. Independent Host review
is required.

## Selected-head individual scalar RNE threshold classifier

`diagnose_q24_s16_terminal_remainder_selected_head_scalar_rne_threshold_classifier_v1 --check`
recovers individual baseline, exact-target and post-hidden-RNE working
accumulator scalars for retained FP16 tied-head rows 319 and 34319. The
authenticated retained final-RMSNorm hidden operands, rather than closed
margin differences alone, supply these otherwise unretained individual
scalars. This is the bounded selected-row computation, not replay of the
closed head-only diagnostic or any stage18/RMSNorm/prefix/reference producer.

The preregistered split applies native FP16 round-to-nearest-even to each
individual scalar before subtracting rows. If exact-target rounding
reproduces all 72 closed ordered/control/reference contrasts, the successor
is **exact-dose accumulator threshold localization**. If only working-dot
rounding reproduces all 72, it is **hidden-RNE-induced accumulator threshold
crossing**. Neither match, failed authentication or failed scalar/retained
receipt closure is `UNKNOWN/integrity`, without a scientific successor.
Rounding a dot-margin difference is not a substitute for rowwise rounding.

The executable result is `supported`, pending independent Host review:
exact-target rounding matches **72/72**, and working-dot rounding also matches
**72/72**. The preregistered precedence therefore selects **exact-dose
accumulator threshold localization**, not the working-only successor.
The two branches produce identical individual FP16 words in all 36
control/polarity/row records despite their distinct exact accumulator inputs.
For `(319,34319)`, forward rounded movement is `-1/128` throughout;
reverse movement is `1/128` except `mapped_all`, where it is `1/64`.
This establishes pattern reproduction without requiring hidden-RNE
perturbations for this split; it does not establish that hidden rounding has
no numerical effect.

The stdout-only contract includes every scalar input, rounded FP16 word,
exact rounded value and rounding remainder, the exact hidden targets and
working words, both agreement counts, fixed original-reference margins,
reviewed source/test/review pins, closed dose receipts through three-sixty-fourths,
current source/test identities and dispatch/write audits. Focused tests use an
independent integer-bit FP16 decoder and nearest-even oracle, with mutation
guards for inputs, identities, receipts, selection, closed replay and writes.
Only 92 selected-row exact accumulations (including two residual dots),
16,128 hidden conversions and 90 accumulator conversions are budgeted;
RMSNorm, full-vocabulary head dispatch and filesystem mutation are forbidden.
The native check compiled both current Python files in memory and passed
twelve focused tests with no errors, failures or skips. It emitted exactly
one stdout JSON document and measured the stated scalar budgets, with zero
forbidden calls, head/RMSNorm dispatches, prefix/admission/reference-producer
or closed-stage18/head-only replay, writes and artifact overwrites.

This split selects narrower scalar threshold evidence, not a dominant
runtime stage or a reference-independent repair. Original thresholds,
references, historical `FAIL`/`UNKNOWN`, closed outcomes and false admission
flags remain intact. Native S16 RTZ/Q24 residual state remains wider than
FP16; INT4 weights and FP16 operator/KV boundaries are unchanged. No
strict-FP16-state W4A16, new-token or full-model admission follows.

## Outer-pair final-RMSNorm head-only three-sixty-fourths classifier

`diagnose_q24_s16_terminal_remainder_final_rmsnorm_headonly_three_sixtyfourths_vector_dose_classifier_v1 --check`
freezes the independently propagated original-input FP16 final-RMSNorm hidden
minus the original-input binary64 final-RMSNorm hidden. It subtracts or adds
exactly `3/64` of this vector to each retained actual final-RMSNorm hidden,
in all-896-coordinate input order, before one native RNE FP16 conversion.
The intervention is at the hidden/head boundary, not at stage18: no RMSNorm,
closed stage18 intervention, prefix/admission or reference producer is replayed.
Only the two retained FP16 tied-head rows, 319 and 34319, are evaluated.

The preregistered prediction compares all 72 ordered-pair/control/reference
contrasts with the authenticated closed three-sixty-fourths rounded-margin
pattern. Reproduction selects **selected-head accumulation/RNE threshold
localization**; non-reproduction with exact dose separation selects
**final-RMSNorm input/scale/cancellation localization**. Integrity or
authentication defects classify `UNKNOWN`, without a scientific successor.
The scalar analytical `1/32` and `1/16` comparisons establish exact target-dot
separation only; they do not construct or execute neighboring hidden doses.

The executable result is `supported`, pending independent Host review:
72/72 pattern agreements and 72/72 exact target-dose separations. For
`(319,34319)`, rounded forward movement is `-1/128` in every control;
reverse is `1/128` in eight controls and `1/64` in `mapped_all`.
The opposite ordered pair negates each movement, and both fixed original
reference branches share it. Every working hidden changes 801 FP16 words.
The exact unconverted target-dot movement has magnitude
`421448323387388081679/37778931862957161709568`; the actual post-hidden-RNE,
pre-head-RNE movements remain separately retained:

| Controls | Forward exact dot movement | Reverse exact dot movement |
|---|---|---|
| Eight controls other than `mapped_all` | `-5826457/536870912` | `729608809/68719476736` |
| `mapped_all` | `-363285/33554432` | `734221833/68719476736` |

The stdout-only check compiles both current Python files in memory, runs twelve
focused tests with an independently implemented rational/FP16 selected-head
oracle, and emits exactly one JSON document. It retains exact target and
post-conversion row dots/margins, selected rounded logits, conversion
remainders, original-reference margin closure, immutable closed-dose receipts
through three-sixty-fourths, and source/test/review/head-row/input identities.
The dispatch budget is nine retained-baseline head closures plus eighteen
new head-only interventions: 27 two-row head calls, zero RMSNorm calls and
zero forbidden calls. Mutation tests refuse changed identities, altered
receipts, coordinate/dose changes, operand/row splices, closed replay and writes.

Pattern reproduction selects a narrower experiment; it does not identify a
causal or dominant boundary, distinguish accumulation from conversion, or
establish a reference-independent native repair. Closed three-sixty-fourths
and thirty-second rejections, earlier intervention outcomes, historical S18
`FAIL` and common-selection `UNKNOWN`, exact thresholds, source/token/Q24/state/
KV/lineage gates and original global references remain unchanged. Native S16
RTZ, G128 asymmetric packed INT4 weights, native GEMM nibble ordering, no qzero
plus-one, FP16 scales/operator/KV boundaries and wider-than-FP16 Q24 state
remain fixed. No strict-FP16-state W4A16, new-token/full-model admission,
precision/scale expansion or hardware/GPU/RTL/ACE2 capability is claimed.

## Retained outer-pair suffix-boundary asymmetry/cancellation localizer

`diagnose_q24_s16_terminal_remainder_suffix_boundary_asymmetry_cancellation_localizer_v1`
consumes only the pinned sixteenth, thirty-second and three-sixty-fourths dose
captures, their original launcher commands, source/tests and immutable independent
reviews. It does not import or invoke their producers. The retained capture schema
contains changed-word counts and 36 binary64-reference margin rows per dose, but
not operand words/conversion remainders, FP16-reference rows, final-RMSNorm
vector/scale/cancellation terms, or selected-head accumulation/RNE remainders.
Inherited producer environments and complete native streams were not archived by
those summary launchers. The opaque protected-input identity is not a substitute
for the absent individual original-reference/source/operand/state/KV/lineage bindings.

The localizer preserves available exact binary64 margin and ordered-pair reversal
closures and emits a separate account for every dose/control/polarity/pair/reference.
Absent components remain `UNKNOWN`; neither changed-word counts nor rounded margin
movements establish a causal boundary. It must not reconstruct FP16-reference rows
from reference cancellation or infer exact components from summary statistics.
The fixed lossy captures therefore cannot establish the preregistered operand-boundary
prediction or its final-RMSNorm/head alternative. A missing binding terminates this
branch as `UNKNOWN`, not as evidence supporting or rejecting either mechanism.
This is a retained-schema limitation and a synthetically exercised reporting
surface, not a completed scientific localization. No `SUPPORTED` or `REJECTED`
mechanism result is established.

The CPU-only check requires fresh source/test/interpreter/command/environment and
validation-capture bindings, denies producer/service/hardware dispatch and evidence
writes, and emits one JSON document for normal independent Host review. Command
sidecars retain their exact byte hashes while binding the parsed argv and complete
environment independently of environment-assignment ordering; duplicate assignments
or changed values are integrity failures. Historical
dose rejections, S18 failures and common-selection UNKNOWN remain unchanged.
No additional dose, row319 provenance task, mixed-RMSNorm replay, admission,
precision expansion or successor is authorized. Q24-wide residual state, native
S16 RTZ, INT4 weights and FP16 operator/KV boundaries remain fixed.

## Outer-pair three-sixty-fourths terminal vector dose bracket

`diagnose_q24_s16_terminal_remainder_three_sixtyfourths_vector_dose_bracket_v1 --check`
applies exactly `3/64` of the frozen original-FP16-stage18 minus independently
propagated original-binary64-terminal vector to working actual FP16 stage18.
Forward subtracts and reverse adds. All 896 coordinates, including zeros, are
traversed in input order; exact rational multiplication precedes one native RNE
FP16 conversion. No ranking, selected-union reconstruction, coordinate
reselection, dose search or averaging of rounded neighboring operands is used.
A changed-word census distinct from both closed neighboring doses proves that
neither the sixteenth nor thirty-second rounded operand was substituted.

The closed full/reverse/half/quarter/eighth/sixteenth/thirty-second receipts and
reviewed source/test/review pins bind the original source/token/operand/Q24/state/
KV/lineage/reference/weight identities and closed outcomes. The thirty-second
receipt retains the complete ordered 36-row binary64 comparator census; fixed
reference subtraction cancels for both original reference branches. Its
rejected 40/72 result and 32 reverse-zero contrasts remain fixed. No closed
intervention, prefix, admission or reference producer is replayed.

The preregistered prediction requires strict nonzero same-direction movement in
all 72 retained contrasts, including recovery of the 32 reverse-zero contrasts.
Absolute movement must be no greater than same-polarity sixteenth movement,
strictly greater than same-polarity thirty-second movement wherever nonzero, and
below same-polarity eighth/quarter/half/full movement. Supported selects the
reverse-obstruction threshold bracket `(1/32, 3/64]` and a tighter
threshold-localization successor. Rejected selects suffix-boundary asymmetry/
cancellation localization rather than smaller fixed-dose continuation.
Authentication/integrity defects classify `UNKNOWN`. This does not establish
continuous-dose monotonicity or identify a causal suffix boundary.

The stdout-only check compiles both current Python files in memory and runs
fourteen focused tests, reusing reviewed independent integer/Fraction suffix
oracle and integrity tests with new exact-dose, comparator and classification
cases. Only nine retained-baseline closures and eighteen new-dose final RMSNorm/
two-row head suffixes are permitted. Mutation tests refuse source/operand/state/
KV/lineage drift, forbidden replay and writes. Exactly one stdout JSON document
is emitted. Normal independent Host review is required.

For the retained nine controls, the executable result is `rejected`, pending
independent Host review. All 72 contrasts move in the predicted nonzero
direction, and all 32 previously zero reverse contrasts recover. However, only
36/72 satisfy the complete dose prediction: every forward movement equals its
nonzero thirty-second comparator instead of strictly exceeding it.
For `(319,34319)`, forward movement remains `-1/128` throughout. Reverse movement
is `1/128` in eight controls and `1/64` in `mapped_all`, equal to each retained
sixteenth comparator. Opposite ordered pairs negate these movements, and both
original reference branches share them. Every new operand changes 795 FP16
words, distinct from the closed thirty-second 742 and sixteenth 827.
Reverse recovery alone does not support the joint bracket prediction; the
preregistered successor is suffix-boundary asymmetry/cancellation localization,
not smaller fixed-dose continuation. No particular suffix mechanism is identified.

Historical S18 `FAIL`, common-selection `UNKNOWN`, rejected middle-pair and
thirty-second outcomes and earlier supported interventions remain unchanged.
Native S16 RTZ, G128 asymmetric packed INT4 weights, native GEMM nibble ordering,
no qzero plus-one, FP16 scales/operator/KV boundaries and wider-than-FP16 Q24
state remain fixed. No hardware/GPU/RTL/ACE2, precision/scale expansion,
strict-FP16-state W4A16, new-token/full-model admission or reference-independent
native repair is claimed.

## Outer-pair thirty-second terminal vector dose contrast

`diagnose_q24_s16_terminal_remainder_thirtysecond_vector_dose_contrast_v1 --check`
applies exactly one-thirty-second of the frozen original-FP16-stage18 minus
independently propagated original-binary64-terminal vector, subtracting forward
and adding reverse. All 896 coordinates, including zeros, are traversed in input
order. Exact rational division by 32 precedes one native RNE FP16 conversion of
the working actual stage18 operand; rounded larger-dose operands are never
averaged. Only nine retained-baseline closures and eighteen thirty-second-dose
final RMSNorm/two-row head suffixes are permitted.

Reviewed source/test/review pins and closed full-forward, full-reverse, half,
quarter, eighth and sixteenth receipts bind original source/token/operand/Q24/
state/KV/lineage/reference/weight identities, controls, thresholds and obstructions.
The sixteenth receipt retains all 36 binary64 control/polarity/ordered-pair
contrasts as named fields and values. Exact census, predictions, older dose
comparators and original-reference margin closure are required. Fixed-reference
subtraction cancels for both original branches, so these movements apply to both
without reference recomputation. No closed intervention, coordinate ranking,
selected-coordinate union or missing gate census is replayed or reconstructed.

The preregistered prediction requires all 72 contrasts to move in the corresponding
strict nonzero direction, with
`0 < abs(thirty-second) < abs(same-polarity sixteenth)` and below same-polarity
eighth/quarter/half/full movement when comparable. Supported selects
conversion-resilient sub-sixteenth local terminal-boundary dose sensitivity and
a smaller fixed-dose successor. Zero, opposite, equal-bound or larger movement
rejects and selects below-sixteenth FP16 conversion-threshold/asymmetry/
cancellation follow-up, not a causal attribution. Authentication/integrity defects
or incompatible comparators classify `UNKNOWN`, never omitted passing rows.

The stdout-only check compiles both current Python files in memory and runs
fourteen focused tests against the reviewed independent integer/Fraction suffix
oracle, including exact targets, RNE ties/subnormals/zeros, non-equivalence to
averaging rounded sixteenth operands, complete comparator binding, strict bounds,
source/operand/state/KV/lineage mutation refusal and forbidden replay/write paths.
It emits exactly one stdout JSON document. Normal independent Host review is
required.

For the retained nine controls, the executable result is `rejected`: 40/72
contrasts satisfy both direction and strict dose bounds, while 32 reverse
contrasts have exactly zero movement. Independent review is pending.
For `(319,34319)`, forward movement is `-1/128` throughout, below the same-polarity
sixteenth magnitude `1/64`. Reverse movement is zero in eight controls despite
their retained sixteenth movement `1/128`; only `mapped_all` moves `1/128`,
below its sixteenth movement `1/64`. Opposite ordered pairs have opposite signs
or the same exact zero; both original reference branches share the movement.
Every thirty-second-dose operand changes 742 FP16 words. Thus the joint
both-polarity prediction is falsified despite nonzero working-operand changes.
This selects the preregistered below-sixteenth conversion-threshold/asymmetry/
cancellation follow-up; it does not identify which suffix boundary or mechanism
causes the zero final-margin movement.

Historical S18 `FAIL`, common-selection `UNKNOWN`, the rejected middle-pair
outcome and closed supported interventions remain unchanged. Native S16 RTZ,
G128 asymmetric packed INT4 weights with native GEMM nibble ordering and no
qzero plus-one, FP16 scales/operator/KV boundaries and wider-than-FP16 Q24 state
remain fixed. No prefix/admission/reference-producer replay, hardware/GPU/RTL/
ACE2, precision/scale expansion, continuous-dose monotonicity, strict-FP16-state
W4A16, new-token/full-model admission or reference-independent repair is claimed.

## Outer-pair sixteenth terminal vector dose contrast

`diagnose_q24_s16_terminal_remainder_sixteenth_vector_dose_contrast_v1 --check`
applies exactly one-sixteenth of the frozen original-FP16-stage18 minus
independently propagated original-binary64-terminal vector, subtracting forward
and adding reverse. All 896 coordinates, including zeros, are traversed in input
order. Exact rational sixteenthing precedes one native RNE FP16 conversion of the
working actual stage18 operand; rounded eighth/quarter/half/full operands are
never averaged. Only nine retained-baseline closures and eighteen sixteenth-dose
final RMSNorm/two-row head suffixes are permitted.

Reviewed source/test/review pins and closed full-forward, full-reverse, half,
quarter and eighth receipts bind original source/token/operand/Q24/state/KV/
lineage/reference/weight identities, controls, thresholds and obstructions.
The compact eighth receipt retains four distinct `(polarity, movement, quarter,
half, full)` signatures for `(319,34319)`. Each control is bound through its
authenticated same-polarity quarter/half/full tuple. Keys must be unique and all
signatures must be used; missing or ambiguous matches are integrity failures.
Opposite-pair movements follow exact margin antisymmetry; fixed-reference
subtraction cancels for either original branch. This identity lookup is not a
dose search, new computation of closed operands, coordinate ranking or
reconstruction of a selected-coordinate union or missing gate census.

The preregistered prediction requires all 72 control/polarity/ordered-pair/
reference contrasts to move in the corresponding strict nonzero direction, with
`0 < abs(sixteenth) < abs(same-polarity eighth)` and below same-polarity
quarter/half/full movement when comparable. Supported selects conversion-resilient
sub-eighth local terminal-boundary dose sensitivity and a smaller fixed-dose
successor. Zero, opposite, equal-bound or larger movement rejects and selects
below-eighth FP16 conversion-threshold/asymmetry/cancellation follow-up, not a
causal attribution. Authentication/integrity defects or incompatible comparators
classify `UNKNOWN`, never omitted passing rows.

The stdout-only native check compiles both current Python files in memory and
runs fourteen focused tests against the reviewed independent integer/Fraction
suffix oracle, including exact targets, RNE ties/subnormals/zeros, non-equivalence
to averaging rounded eighth operands, unique comparator binding, strict bounds,
source/operand/state/KV/lineage mutation refusal and forbidden replay/write paths.
It emits exactly one stdout JSON document. Normal independent Host review is
required.

For the retained nine controls, the executable result is `supported` in all
72 directional and strict dose comparisons; independent review is pending.
For `(319,34319)`, forward movement is `-1/64` throughout, strictly below the
closed eighth magnitude `1/32`. Reverse movement is `1/128` in eight controls
and `1/64` in `mapped_all`, versus same-control eighth movements `1/32` and
`5/128`, respectively. All movements are also strictly below same-polarity
quarter/half/full magnitudes. Opposite ordered pairs have opposite signs; both
original reference branches share the movement. Every sixteenth-dose operand
changes 827 FP16 words. This fixed-dose response does not establish
proportionality, continuous monotonicity or a conversion-threshold mechanism.

Historical S18 `FAIL`, common-selection `UNKNOWN`, the rejected middle-pair
outcome and closed supported interventions remain unchanged. Native S16 RTZ,
G128 asymmetric packed INT4 weights with native GEMM nibble ordering and no
qzero plus-one, FP16 scales/operator/KV boundaries and wider-than-FP16 Q24 state
remain fixed. No prefix/admission/reference-producer replay, hardware/GPU/RTL/
ACE2, precision/scale expansion, continuous-dose monotonicity, strict-FP16-state
W4A16, new-token/full-model admission or reference-independent repair is claimed.

## Outer-pair eighth terminal vector dose contrast

`diagnose_q24_s16_terminal_remainder_eighth_vector_dose_contrast_v1 --check`
applies exactly one-eighth of the frozen original-FP16-stage18 minus
independently propagated original-binary64-terminal vector, subtracting forward
and adding reverse. All 896 coordinates, including zeros, are traversed in input
order. Exact rational eighthing precedes one native RNE FP16 conversion of the
working actual stage18 operand; rounded quarter/half/full operands are never
averaged. Only nine retained-baseline closures and eighteen eighth-dose final
RMSNorm/two-row head suffixes are permitted.

Reviewed source/test/review pins and closed full-forward, full-reverse, half and
quarter execution receipts bind original source/token/operand/Q24/state/KV/
lineage/reference/weight identities, controls, thresholds and obstructions.
The quarter receipt retains all 36 binary64 control/polarity/ordered-pair
contrasts; each is bound directly, including both `(319,34319)` and
`(34319,319)`. Only the half receipt requires the disclosed opposite-pair
antisymmetry derivation. Fixed-reference subtraction cancels in movement for
either original branch. These algebraic identities do not reconstruct missing
coordinate gate censuses or a selected-coordinate union. No closed intervention
is rerun.

The preregistered prediction requires all 72 control/polarity/ordered-pair/
reference contrasts to move in the corresponding strict nonzero direction, with
`0 < abs(eighth) < abs(same-polarity quarter)` and below same-polarity half/full
movement when comparable. Supported selects conversion-resilient sub-quarter
local terminal-boundary dose sensitivity and a smaller fixed-dose successor.
Zero, opposite, equal-bound or larger movement rejects and selects below-quarter
FP16 conversion-threshold/asymmetry/cancellation follow-up, not a causal
attribution. Authentication/integrity defects or missing/incompatible comparators
classify `UNKNOWN`, never omitted passing rows.

The native check compiles both current files in memory and runs fourteen focused
tests against the reviewed independent integer/Fraction suffix oracle. Coverage
includes exact targets, RNE ties/subnormals/zeros, non-equivalence to averaging
rounded quarter operands, comparator derivations, strict bounds, source/operand/
state/KV/lineage mutation refusal and forbidden ranking/union/replay/write/dose
search. It emits exactly one stdout JSON document. Normal independent Host
review remains required.

For the retained nine controls, the independently reviewed executable result is
`supported` in all 72 directional and strict dose comparisons.
For `(319,34319)`, forward movement is `-1/32` throughout, versus the closed
quarter movement `-1/16`. Reverse movement is `1/32` or `5/128`, versus
same-control quarter movement `7/128` or `1/16`, respectively. All movements
are also strictly below the same-polarity half/full magnitudes. Opposite
ordered pairs have opposite signs; both original reference branches share
the movement. Every eighth-dose operand changes 858 FP16 words. The check
compiles both files, passes all fourteen focused tests without skips, emits
one stdout JSON document, and records 27 final RMSNorm calls, 27 two-row head
calls and zero forbidden calls. This is a fixed-dose local observation, not
continuous monotonicity, proportionality or admission.

Historical S18 `FAIL`, common-selection `UNKNOWN`, the rejected middle-pair
outcome and closed supported interventions remain unchanged. Native S16 RTZ,
G128 asymmetric packed INT4 weights with native GEMM nibble ordering and no
qzero plus-one, FP16 scales/operator/KV boundaries and wider-than-FP16 Q24 state
remain fixed. No prefix/admission/reference-producer replay, hardware/GPU/RTL/
ACE2, precision/scale expansion, continuous-dose monotonicity, strict-FP16-state
W4A16, new-token/full-model admission or reference-independent repair is claimed.

## Outer-pair quarter terminal vector dose contrast

`diagnose_q24_s16_terminal_remainder_quarter_vector_dose_contrast_v1 --check`
applies exactly one-quarter of the frozen original-FP16-stage18 minus
independently propagated original-binary64-terminal vector, subtracting forward
and adding reverse. All 896 coordinates, including zeros, are traversed in input
order. Exact rational quartering precedes one native RNE FP16 conversion of the
working actual stage18 operand; rounded half/full operands are never averaged.
Only nine retained-baseline closures and eighteen quarter-dose final RMSNorm/
two-row head suffixes are permitted.

Reviewed source/test/review pins and the closed full-forward, full-reverse and
half-dose execution receipts bind the original source/token/operand/Q24/state/
KV/lineage/reference/weight identities, controls, thresholds and obstructions.
The half receipt retains binary64 `(319,34319)` contrasts for both polarities.
The opposite pair's movement is derived by exact margin antisymmetry, and
fixed-reference subtraction cancels in movement for either original branch.
These disclosed algebraic derivations do not reconstruct missing coordinate
gate censuses or a selected-coordinate union. No closed intervention is rerun.

The preregistered prediction requires every one of the 72 retained control/
polarity/ordered-pair/reference contrasts to move in its strict nonzero predicted
direction, with `0 < abs(quarter) < abs(same-polarity half)` and
`abs(quarter) < abs(same-polarity full)`. Supported selects sub-half
conversion-resilient local monotone terminal-boundary dose sensitivity and a
smaller-dose bound. Zero, opposite, equal-bound or larger movement rejects and
selects below-half conversion-threshold/asymmetry/cancellation follow-up, not a
causal attribution. Authentication/integrity defects or missing/incompatible
comparators classify `UNKNOWN`, never omitted passing rows.

The native check compiles both current files in memory and runs fourteen
focused tests against the reviewed independent integer/Fraction suffix oracle.
Exact targets, RNE ties/subnormals/zeros, non-equivalence to averaging rounded
half operands, comparator derivations, strict bounds, authentication mutations
and forbidden ranking/union/replay/write/dose-search paths are covered.
It emits exactly one stdout JSON document; normal independent Host review is
required.

Historical S18 `FAIL`, common-selection `UNKNOWN`, the rejected middle-pair
outcome and closed supported interventions remain unchanged. Native S16 RTZ,
G128 asymmetric INT4 weights with native GEMM nibble ordering and no qzero
plus-one, FP16 scales/operator/KV boundaries and wider-than-FP16 Q24 state
remain fixed. No prefix/admission/reference-producer replay, hardware/GPU/RTL/
ACE2, precision/scale expansion, continuous-dose monotonicity, strict-FP16-state
W4A16, new-token/full-model admission or reference-independent repair is claimed.

## Outer-pair half terminal vector dose contrast

`diagnose_q24_s16_terminal_remainder_half_vector_dose_contrast_v1 --check`
preregisters both forward subtraction and reverse addition of exactly one-half
of the frozen original-FP16-stage18 minus independently propagated original
binary64-terminal vector. All 896 coordinates, including zeros, are traversed
in input order. Exact rational halving precedes a single native RNE FP16
conversion of each working actual FP16 operand; rounded full-dose operands are
not averaged. Only final RMSNorm and the two outer tied-head rows execute:
nine retained-baseline closures and eighteen half-dose suffixes.

The forward and reverse full-dose measurements come from authenticated closed
execution receipts whose source/test pins match independently reviewed parents.
The reverse parent is closed by its second-round independent review. The
original-input vector, norm weights, selected head rows, control census,
retained margin obstruction, source/token/Q24/state/KV/lineage/reference
identity and full-vector linear bridge must bind before comparison. Neither
full dose is recomputed. Coordinate ranking, selected-union reconstruction and
closed coordinate-363/non-363/middle-pair interventions are forbidden.

The falsifiable prediction requires all 72 control/polarity/ordered-pair/reference
contrasts to move in the corresponding retained terminal prediction's strict
nonzero direction, with `0 < abs(half movement) < abs(same-polarity full movement)`.
A supported result selects local monotone terminal-boundary dose sensitivity,
not proportionality. Zero, opposite, equal-full-dose or larger movement rejects
and selects conversion-threshold/asymmetry/cancellation follow-up, without
claiming which mechanism caused it. Authentication or integrity defects,
including unavailable or incompatible comparators, are `UNKNOWN`, not skipped
passing rows. The fixed-reference margin-change movement equals actual-margin
movement for both original reference branches.

The native check compiles the two current files in memory and runs fourteen
focused tests against the reviewed independent integer/Fraction suffix oracle.
Tests cover both half operands, ties-to-even/subnormals/zeros, exact strict dose
bounds, source/test/review/receipt pins, operand/state/KV/reference/lineage drift,
and forbidden ranking/replay/write and suffix order/scale/budget paths. It emits
exactly one stdout JSON document; independent Host review remains required.

For the retained nine controls, the executable result is `supported` in all
72 directional and strict dose comparisons, independently reviewed. The
`(319,34319)` margin moves by `-15/128` forward and `15/128` reverse in every
control; the opposite ordered pair has opposite signs. Both original reference
branches see the same movement. The retained forward full-dose magnitude is
`31/128` for `mapped_all` and `1/4` otherwise; the reverse full-dose magnitude
is `15/64` throughout. Every half operand changes 883 FP16 words. The fresh
check compiles both files, passes all fourteen focused tests with no skips,
emits one stdout JSON document and records 27 final RMSNorm calls, 27 two-row
head calls and zero forbidden calls. This is a bounded half-versus-full-dose
observation, not proof of monotonicity over a continuous dose interval.

Historical S18 `FAIL`, common-selection `UNKNOWN`, exact thresholds and all
closed intervention outcomes remain unchanged. Missing detailed coordinate gate
censuses are not reconstructed. Native S16 RTZ, G128 asymmetric packed INT4
weights with native GEMM nibble ordering and no qzero plus-one, FP16 scales and
operator/KV boundaries, and wider-than-FP16 Q24 residual state remain fixed.
No prefix/admission/reference-producer replay, hardware/GPU dispatch, precision
or scale expansion, strict-FP16-state W4A16, new-token/full-model admission or
reference-independent native repair is established.

## Outer-pair reverse terminal vector suffix contrast

`diagnose_q24_s16_terminal_remainder_reverse_vector_suffix_contrast_v1 --check`
adds the frozen original-FP16-stage18 minus independently propagated original
binary64-terminal vector to the working actual FP16 stage18 input. All 896
coordinates are processed in input order, including zeros, with native RNE FP16
conversion and exact target/conversion-remainder disclosure. Only final RMSNorm
and the two outer tied-head rows execute: nine retained-baseline closures and
nine reverse suffixes. No closed scalar, forward-vector, non-363 or middle-pair
intervention, prefix, admission or reference producer is replayed.

The retained forward execution receipt contains the exact per-control,
per-outer-pair terminal predictions, separately from the full-vector linear
bridge. Its authenticated source/test identities match the independently
reviewed forward implementation. Negating those recorded predictions gives
the reverse prediction without loading or regenerating a selected-coordinate
union. The full-vector linear account binds the original terminal references,
norm weights and tied-head rows; it does not replace the retained prediction.
The missing detailed coordinate census is not reconstructed or represented as
retained evidence. The retained signed component, fixed actual/reference margin
change and unchanged common-selection rule/`UNKNOWN` gate bind each contrast.

All 36 strict nonzero directional agreements select local sign-symmetric
aggregate terminal-boundary sensitivity. An authenticated zero/opposite response
selects conversion/asymmetry/cancellation follow-up; authentication or integrity
defects are `UNKNOWN`. The native check compiles both current Python files in
memory, runs 14 focused tests with the reviewed independent integer/Fraction
suffix oracle, and emits exactly one stdout JSON document. Tests cover reverse
operand conversion, receipt/source/test/review pins, state/KV/reference/lineage
drift, forbidden ranking/union/replay/writes and suffix order/scale/budget guards.
Normal independent Host review remains required.

For the retained nine-control input set, the reverse executable result is
`supported` in all 36 ordered-pair/control/reference contrasts, independently
reviewed. The `(319,34319)` actual margin increases by `15/64` in
every control; `(34319,319)` decreases by `15/64`. Both fixed reference branches
see that same actual-margin movement. Each working operand changes 890 FP16
words. This supports sign symmetry only, not equal forward/reverse magnitudes
or threshold closure. The fresh check reports 14 passing tests, one stdout
JSON document, 18 final RMSNorm calls, 18 two-row head calls and zero forbidden
calls.

Historical S18 `FAIL`, common-selection `UNKNOWN`, closed supported outcomes,
the rejected middle-pair intervention and exact thresholds remain unchanged.
Native S16 RTZ, asymmetric G128 INT4 weights with native GEMM nibble ordering
and no qzero plus-one, FP16 scales/operator/KV boundaries and wider-than-FP16
Q24 residual state remain fixed. This reference-guided non-admission diagnostic
is not a reference-independent repair, strict-FP16-state W4A16, new-token or
full-model capability.

## Outer-pair non-363 terminal complement suffix contrast

`diagnose_q24_s16_terminal_remainder_non363_complement_suffix_contrast_v1 --check`
implements the isolated CPU-only, non-admission complement counterfactual for
`(319,34319)` and `(34319,319)`. It freezes the original-input terminal
differences `original_FP16_stage18[i] - original_binary64_terminal[i]` and
subtracts them from the working actual FP16 stage18 operand at exactly the
895 coordinates other than 363. Native RNE FP16 conversion applies separately
to each exact target. Word 363 is copied bit-for-bit, including its zero sign.
The input-ordered mask includes zeros and the retained selected-coordinate
union; there is no ranking, alternate-coordinate selection or fitting.
Neither the closed coordinate-363 intervention nor the all-896 intervention
is replayed, and their supported 36/36 results remain fixed.

The preregistered directional prediction is opposite the retained binary64
terminal complement contribution: the unchanged unselected terminal-component
total minus its coordinate-363 term. Exact signed/absolute closure authenticates
that retained account. The output separately discloses the linear bridge for
all 895 working coordinates; this is not substituted for the retained
common-selection obstruction's complement. Both fixed original-input reference
branches use the same actual margin movement for each ordered pair and control.
All 36 strict nonzero directional agreements support distributed
terminal-boundary complement sufficiency for this bounded question; any
zero/cancelled or opposite response rejects it and selects hotspot-dominated
or cancellation follow-up. Authentication or integrity defects are `UNKNOWN`.

Only final RMSNorm and the two retained tied-head rows execute, with nine
baseline closures and nine complement suffixes. The stdout-only native check
compiles both current Python files in memory and runs 16 focused tests using
the reviewed independent integer/Fraction suffix oracle. It emits one JSON
document containing all 895 targets and conversion remainders per control,
protected word-363 identities, unchanged retained obstructions, reviewed parent
source/test/review pins and current source/test pins. Negative cases cover
selection, source/operand/Q24 state/KV/reference/lineage drift, saturation,
forbidden replay/write paths, and suffix operand/order/scale/budget violations.
Normal independent Host review remains required.

Original source/token/reference identities, controls, exact thresholds,
historical S18 `FAIL` and common-selection `UNKNOWN` remain unchanged.
Native-S16 RTZ, INT4 G128 asymmetric weights without qzero plus-one, and FP16
scales/operator/KV boundaries remain fixed; Q24 residual state is wider than
FP16. This reference-guided operand diagnostic is not a reference-independent
native repair, strict-FP16-state W4A16, admission, new-token or full-model result.

## Outer-pair full-vector terminal-remainder suffix contrast

`diagnose_q24_s16_terminal_remainder_vector_suffix_contrast_v1 --check`
implements a distinct isolated CPU-only, non-admission counterfactual for
`(319,34319)` and `(34319,319)`. It freezes all 896 exact differences
`original_FP16_stage18[i] - original_binary64_terminal[i]` once and subtracts
them from each control's working actual FP16 stage18 input, converting each
exact target back to native RNE FP16. Every coordinate is included in input
order, including zeros and the previously selected-coordinate union; there is
no hotspot ranking, alternate-coordinate choice or coordinate-363 scalar replay.
The original references and the completed scalar result remain unchanged.

The preregistered prediction requires every retained outer-pair/control/reference
margin delta to have the strict nonzero sign opposite the retained binary64
terminal-remainder bridge contribution. The output binds each contrast to its
unchanged common-selection obstruction and separately exposes the full-vector
linear bridge delta; the selected-coordinate complement is not mistaken for
the intervention's operand mask. Authenticated, oracle-verified directional
agreement is `supported`; any zero/cancelled or opposite response is `rejected`;
authentication or execution-integrity defects are `UNKNOWN`. These outcomes
distinguish aggregate terminal-boundary directional sufficiency from
coordinate-specific/cancellation follow-up, without executing either successor.

For the fixed nine-control input set, the executable contrast is `supported`
in all 36 ordered-pair/control/reference rows. The `(319,34319)` actual margin
changes by `-1/4` in eight controls and `-31/128` in `mapped_all`; the reverse
pair has the opposite deltas. Both unchanged reference branches see the same
actual-margin movement, not independent additional interventions. All 896
coordinate targets and conversion remainders are retained; 890 FP16 words
change in each control. Only the final RMSNorm and two tied-head rows execute:
nine retained-baseline closures and nine full-vector suffixes. An independent
exact integer/Fraction oracle checks the resulting normalized words, selected
logits and norm scalars.

The single stdout JSON document binds reviewed parent source/test/review pins,
current diagnostic source/test pins, retained source/token/state/KV/lineage and
independently propagated original-input FP16/binary64 reference identities.
Negative cases enforce drift, forbidden replay/write, operand and suffix-budget
refusal. Historical S18 `FAIL`, common-selection `UNKNOWN`, exact thresholds and
false/zero non-admission flags are preserved; directional response does not
replace the historical component census or establish threshold closure.
Native-S16 RTZ, INT4 G128 asymmetric weights, FP16 operator/scales/KV boundaries
and wider-than-FP16 Q24 residual state remain fixed. This reference-guided
working-operand contrast is not a reference-independent native algorithm repair,
strict-FP16-state W4A16, admission, new-token or full-model capability.

## Outer-pair terminal-remainder coordinate-363 suffix intervention

`diagnose_q24_s16_terminal_remainder_coordinate363_suffix_intervention_v1 --check`
preregisters one isolated CPU-only counterfactual for `(319,34319)` and
`(34319,319)`. Coordinate 363 must be the unique maximum-absolute retained
binary64 terminal-remainder hotspot in every outer-pair/control row before
execution. The intervention subtracts
`original_FP16_stage18[363] - original_binary64_terminal[363]` from a working
copy of actual final stage18 coordinate 363, with native RNE conversion back to
FP16 and an explicit exact rounding remainder. It does not modify a reference.
The derived terminal remainder supplies only that fixed discrepancy; it is not
edited, replaced, reclamped or used as movable ground truth.

Only final RMSNorm and the two tied-head rows are recomputed, including a
retained-baseline closure control. The nine controls, original source/token,
Q24 state, FP16 operator/KV boundaries, INT4 weights, S16 RTZ, lineage, exact
thresholds and independently propagated original-input references remain fixed.
The prediction is a strict margin delta opposite the retained binary64
coordinate contribution in both pairs, all controls and both reference branches.
Authenticated, oracle-verified execution with that contrast is `supported`;
an absent/opposite contrast is `rejected`; integrity failure is `UNKNOWN`.
For this fixed retained-input/control set, all 36 pair/control/reference rows
move nonzero in the predicted direction: actual `(319,34319)` margin deltas
are `-1/32` or `-1/64`, and reverse-pair deltas are `1/32` or `1/64`.
This supports only the preregistered directional contrast.

This is a directional intervention, not a new component-selection census.
The historical common selection remains `UNKNOWN`, historical S18 `FAIL`
facts remain intact, and the final head has no new admission threshold.
Q24 residual state is wider than FP16. Neither classification establishes
admission, strict-FP16-state W4A16, new-token or full-model capability.
This is a single coordinate-363 classification, not a coordinate search or
authorization for a retry or middle-pair continuation.

## Read-only final RMSNorm terminal-remainder coordinate hotspot audit

`diagnose_q24_s16_final_head_final_rmsnorm_unselected_direct_hidden_terminal_remainder_coordinate_hotspot_audit_v1 --check`
uses the authenticated in-memory bridge/census/dominance inputs for the two
binary64-divergent pairs `(319, 34319)` and `(34319, 319)`. This is a declarative
ordering of retained coordinates, not a new nested bridge or component choice.
The original selected-coordinate union, including coordinate 62, is excluded
unchanged for every control and separate reference branch.

At coordinate `i`, the retained factor is the norm weight times the retained
reference inverse-norm anchor times the tied-head row difference. Multiplying
that factor by `original_FP16_stage18[i] - original_branch_terminal[i]`
gives the terminal-remainder contribution. The same factor multiplies the
retained actual-minus-original-FP16 stage17 difference for the MLP comparison.
No binary64 internal MLP or residual stages are reconstructed.

All complement coordinates remain in ascending identity order, with a separate
descending absolute-terminal-contribution permutation, ascending coordinate ID
as the tie-breaker, the top eight nonzero rows, and all maximum-magnitude ties.
Each row includes its signed/absolute contributions and exact same-coordinate
gap `abs(terminal contribution) - abs(MLP contribution)`. These coordinate gaps
are not the prior component net-magnitude gaps. Signed sums, sums of absolute
contributions and cancellation mass close exactly to both retained component
accounts. FP16 terminal deltas and contributions must be zero at every
coordinate; their empty hotspot list is a zero boundary, not a missing census.

The retained input yields 36 pair/control/branch rows, including 18 exact FP16
zero rows. For `frozen_inherited`, each divergent pair retains 876 binary64
complement coordinates and 877 FP16 complement coordinates. The largest
binary64 terminal coordinate is 363 in both ordered pairs. Its signed
contribution for `(319, 34319)` is
`15700263965591301665106670350469251/664613997892457936451903530140172288`;
the reverse pair has the negative of that value. The same-coordinate
terminal-minus-MLP magnitude gap is positive in both directions, exactly
`2154868421040743529178271904139395/664613997892457936451903530140172288`.
These are retained coordinate accounts, not independent samples or causal shares.

The diagnostic retains the complete prior dominance audit, obstruction census
and bridge selection, including the unaudited middle pair `(34319, 13)`.
The two divergent pairs remain `UNKNOWN`, the middle pair retains `mlp_stage17`,
and the common `UNKNOWN` still stops nested expansion. A coordinate order
does not bypass the original unique-largest, direction, strict-half-or-reversal
or complete-census gates. Historical FAIL/PASS facts, exact thresholds,
source/operand/state/KV/lineage gates and independently propagated original-input
FP16/binary64 global references are preserved.

This stdout-only CPU-software interface writes no evidence and runs no
predecessor checks/tests or native/decoder/prefix/admission/reference/RMSNorm/head/
row-dot/local operators. Hotspots are not causes, performance bottlenecks,
executed counterfactuals or admission. Q24 residual state remains wider than
FP16; native S16 RTZ, asymmetric G128 packed INT4 weights and FP16 operator
boundaries/KV are unchanged. No hardware, GPU, RTL, ACE2, precision/scale
expansion, strict-FP16-state W4A16, new-token or full-model capability is
established. Independent Host review remains required.

## Read-only final RMSNorm middle-pair MLP-stage17 coordinate hotspot audit

`diagnose_q24_s16_final_head_final_rmsnorm_unselected_direct_hidden_mlp_stage17_coordinate_hotspot_audit_v1 --check`
consumes the authenticated in-memory bridge/census/dominance interface for
`(34319, 13)`. It requires the retained `mlp_stage17` unique-largest net signed
component in all nine controls and both separate FP16/binary64 branches. The
unchanged selected-coordinate union, including 62, is excluded separately for
each row. All remaining coordinates are retained in ascending identity order.

Each MLP contribution is the norm weight times the retained reference
inverse-norm anchor times the tied-head row difference times
`actual_FP16_stage17[i] - original_FP16_stage17[i]`. The terminal comparison
uses the same factor times
`original_FP16_stage18[i] - original_branch_terminal[i]`. This is exact rational
accounting of retained operands, not replay of RMSNorm, head, row-dot or local
operators, and no binary64 internal stage is reconstructed.

The interface reports a descending absolute-MLP-contribution permutation with
ascending coordinate IDs breaking ties, the top eight nonzero coordinates,
all maximum-magnitude ties, and all repeated-magnitude groups including zeros.
Signed sums, absolute sums and cancellation mass must close exactly to the
retained MLP and terminal component totals, with explicit zero closure residuals.
Binary64 rows compare terminal contributions at the MLP hotspots using
`abs(MLP contribution at i) - abs(terminal contribution at i)`. This is not a
component net-magnitude gap or a causal share. Every FP16 terminal delta and
contribution must remain exactly zero; its terminal hotspot/comparison lists
are empty, while MLP ordering and the full coordinate census remain present.

The complete prior dominance audit, obstruction census and bridge selection
remain in the report, not just the middle pair. The middle pair retains
`mlp_stage17`; the two divergent pairs retain `UNKNOWN`, and common `UNKNOWN`
still stops nested expansion. Coordinate localization does not change the
unique-largest, nonzero direction, strict-half-or-reversal or complete-census
gates. Exact thresholds, historical FAIL/PASS facts, source/operand/state/KV/
lineage gates and independently propagated original-input global references
remain intact.

The retained middle-pair input yields 18 control/branch rows, including nine
FP16 terminal-zero rows. For `frozen_inherited`, the FP16 complement contains
878 coordinates and the binary64 complement contains 877. Coordinate 741 is
the largest absolute MLP contribution in both branches. Its positive signed
contribution is
`972712872287995455840195/19342813113834066795298816` on FP16 and
`1955800403415784668441195/38685626227668133590597632` on binary64.
At that binary64 coordinate the terminal contribution is negative,
`-268635362575671109748932664128339785/10633823966279326983230456482242756608`,
and the MLP-minus-terminal absolute-coordinate gap is positive,
`268970958715490608083588028617818295/10633823966279326983230456482242756608`.
These exact retained accounts do not identify a causal source or an operator
bottleneck.

This stdout-only CPU-software diagnostic performs no predecessor checks/tests
or native/decoder/prefix/admission/reference/RMSNorm/head/row-dot/local replay
and writes no evidence. Q24 residual state remains wider than FP16; native
S16 RTZ, asymmetric G128 packed INT4 weights and FP16 operator boundaries/KV
are unchanged. The audit establishes no causality, performance bottleneck,
executed counterfactual, admission, strict-FP16-state W4A16, new-token or
full-model capability, and authorizes no hardware/GPU/RTL/ACE2 or precision/
scale expansion. Normal independent Host review remains required.

## Read-only final RMSNorm middle-pair MLP-stage17 down-input bridge

`diagnose_q24_s16_final_head_final_rmsnorm_unselected_direct_hidden_mlp_stage17_down_input_bridge_v1 --check`
bridges each retained `(34319, 13)` MLP-stage17 maximum, including every maximum
tie, through authenticated L23 MLP-down stage16 inputs. This bounded selection
uses the middle-pair MLP coordinate audit, not the earlier final-head-channel
MLP-down selection. The complete prior audit, selected-coordinate exclusions,
all pair decisions and the common `UNKNOWN` stop gate remain unchanged.

For each control and separate FP16/binary64 branch, the bridge reports all
4864 exact `(actual_FP16_stage16[j] - original_FP16_stage16[j]) * W_down[j,i]`
terms and their signed/absolute contributions after multiplication by the
retained coordinate's final-RMSNorm/tied-head scalar factor. Native GEMM nibble
ordering, asymmetric packed INT4 with no qzero plus-one, FP16 scales and all
38 contiguous G128 groups follow the existing down-input helper. Descending
absolute input magnitude and ascending coordinate identity determine the eight
selected inputs, with all magnitude ties, including zero groups, disclosed.
Selected/unselected and group signed/absolute totals retain all input terms.

The retained stage17 delta minus the exact input sum is an explicit projection
boundary remainder. Multiplying both by the same retained scalar factor closes
exactly to the retained MLP-stage17 coordinate contribution. Absolute input mass
plus absolute boundary mass differs from the absolute retained contribution
by explicitly reported cancellation; the boundary is not attributed solely to
rounding. Both final branches use original-input FP16 internal stages, without
reconstructing binary64 internal stages or changing either global reference.

This interface is stdout-only CPU-software accounting, not an operator replay,
causal attribution or permission to continue through the common stop gate.
It preserves historical failures, exact thresholds and source/operand/state/
KV/lineage gates. Q24 residual state remains wider than FP16, with native S16
RTZ, INT4 weights and FP16 operator/KV boundaries unchanged. It establishes no
admission, strict-FP16-state W4A16, new-token or full-model capability. Independent
Host review is required.

## Read-only final RMSNorm middle-pair MLP-stage17 down-input SiLU/product bridge

`diagnose_q24_s16_final_head_final_rmsnorm_unselected_direct_hidden_mlp_stage17_down_input_silu_product_bridge_v1 --check`
consumes the authenticated in-memory middle-pair down-input bridge, not the
earlier final-head-channel selections. For every retained maximum coordinate
of `(34319, 13)`, including maximum ties and coordinate-741-derived selections,
it decomposes exactly the eight retained selected stage16 inputs per
control/reference-branch account. Original L23 stage14 gate, stage15 up and
stage16 operands are bound to the authenticated actual and original-input
FP16 archives. No binary64 internal stage is reconstructed.

The retained inputs yield 18 control/branch projection accounts, 144 selected
stage16 accounts and 576 exact closure terms.

For retained operands `g=stage14`, `u=stage15`, `y=stage16`, define the raw
baseline `g*u` (not SiLU) and separate actual/reference boundaries `b=y-g*u`.
The exact local identity is
`delta_y = delta_g*u_ref + g_ref*delta_u + delta_g*delta_u + delta_b`.
The interaction is explicit and unallocated. The boundary jointly contains
the nonlinear SiLU effect and implementation/product/conversion differences;
separate SiLU and product-rounding effects are not identifiable without replay.
Only exact rational accounting is performed, not sigmoid, exponential, SiLU
or product-operator evaluation or a new operator precision.

All four terms are weighted first by the authenticated native G128 down
weight and then by the retained final-RMSNorm/tied-head coordinate factor.
Their sum closes to each selected down-input contribution. The unchanged
unselected down-input total and separate down-projection boundary close the
retained stage17 coordinate contribution exactly. Signed/absolute totals and
cancellation mass distinguish four-term expansion from net input accounts.
Each control and FP16/binary64 final reference branch remains separate, even
when the same internal FP16 operands are selected repeatedly.

The full predecessor bridge, coordinate selections, pair decisions, exact
thresholds, historical FAIL/PASS facts, source/operand/state/KV/lineage gates
and independently propagated original-input global references are preserved.
The common `UNKNOWN` still stops expansion. This stdout-only CPU diagnostic
compiles its source and focused tests in memory, runs a nonzero independent
retained-bit oracle suite and emits one JSON document. It writes no evidence
and replays no native/decoder/prefix/admission/reference/RMSNorm/head/row-dot/
local operators or predecessor checks/tests. Q24 state remains wider than
FP16, with native S16 RTZ, INT4 weights and FP16 operator/KV boundaries
unchanged. These accounts establish neither causality nor admission,
strict-FP16-state W4A16, new-token or full-model capability. Normal independent
Host review remains required.

## Read-only final RMSNorm middle-pair MLP-stage17 stage11 attention source/value bridge

`diagnose_q24_s16_final_head_final_rmsnorm_unselected_direct_hidden_mlp_stage17_stage13_rmsnorm_stage12_attention_source_value_bridge_v1 --check`
expands only the 2304 retained `attention_stage11_delta` terms of the stage12
residual source bridge for pair `(34319,13)`. The exact parent report and its
selections are authenticated without running a predecessor check. Ten retained
actual/original L23 archives, empty-P0 KV lineage, source position 0/token 9707,
canonical native G128 INT4 `o_proj` tensors, FP16 operand bits and all downstream
multipliers remain bound to the retained original-input authority.
Decoded attention operand tuples use exact `Fraction` equality rather than
JSON serialization, preserving rational actual/original binding checks.

For each retained attention output coordinate, the shared local account contains
all 128 GQA value components, each summing seven query-head products. Its exact
terms are `delta(P)*V_ref*W`, `P_ref*delta(V)*W` and
`delta(P)*delta(V)*W`. The AV boundary is the retained stage10 delta projected
through the same weights minus those exact components; the separate
`o_projection` boundary is the retained stage11 delta minus the retained stage10
weighted delta. These are boundary accounts, not solely rounding or causes.
Singleton P0 probabilities make probability, interaction and AV-remainder
terms zero in this retained scope.

Every parent term references its local control/output-coordinate account and
discloses four exact multipliers: unity; norm weight times original scalar
anchor; that product times gate/up weight; and that product times down weight,
row factor and opposite original-reference operand. Each referenced component
and boundary uses those multipliers, closing the parent's stage12, direct
stage13, projection and coordinate-linear contributions exactly. Shared local
tables avoid duplicating identical arithmetic across retained selections;
logical account counts still include every occurrence.

The eight largest absolute net value components (ascending-coordinate tie
breaks) and the remaining 120 components have separate signed, absolute and
cancellation totals at every weighting boundary. Absolute mass is measured
after each component's GQA aggregation. Projection, row and global totals
distinguish five aggregate terms from all component-plus-boundary accounts.
The 18 control/final-reference rows retain their coordinate-741 examples,
independent global references, historical PASS/FAIL facts, exact thresholds,
other residual components and common `UNKNOWN` stop gate.

The CPU-only stdout check compiles its source and focused tests in memory and
requires independent half-bit/native-nibble, exact-closure and mutation checks
before emitting one JSON document. It performs no native/decoder/prefix/
admission/reference/RMSNorm/attention/head/row-dot/local operator replay and
writes no evidence. Internal attention uses original-input FP16 for both final
reference branches; binary64 attention is neither substituted nor reconstructed.
Q24 residual state remains wider than FP16, with unchanged INT4 weights, native
GEMM ordering, no qzero plus-one, FP16 scales/operator boundaries/KV and S16 RTZ.
No causality, admission, strict-FP16-state W4A16, new-token or full-model capability
is established. Independent Host-owned Reviewer validation remains required.

## Stage11 running-claim execution authorization

The proposed current-runtime release implementation
`ace3.model.stage11_current_runtime_release` is non-inheriting: frozen release
016 stays UNKNOWN, all six historical closures stay unresolved, and current
source/runtime identity does not repair earlier operational admission. Its
zero-science builder authenticates Manager JSON and retained review/seal
lineage without importing a candidate. Only isolated synthetic issuer tests
run. The audit covers Python hooks, command receipts and a member census,
not OS-wide confinement or sandbox equivalence.

Its dormant `emit_after_claim` API requires a distinct first-attempt normal
native running claim, a post-claim Manager request, unchanged source/runtime
and exact proposal/command/environment lineage. It requires the native Host
Reviewer DONE reason to be exactly the six-line acceptance defined by
`review_acceptance`: admission `ADMISSIBLE_CURRENT_RUNTIME_NON_INHERITING`,
audit `ACCEPTED_PYTHON_SCOPED_NOT_OS_WIDE`, exact contract and seal hashes,
`ACCEPTED_EXACT_POSTCLAIM_ONE_SHOT`, and zero-science acceptance. An Engineer
checkpoint cannot substitute for that Reviewer reason. The canonical
per-mission issuance directory is exclusive; partial publication consumes
the attempt, and the envelope is written last. All four emitted documents
bind integer science budget one. Issuance itself never executes science;
normal candidate launch checks remain separate and mandatory. Host review
reads the produced root and source without running the builder, emitter,
tests, candidate, observer, or an execution-only Skill.

Current support limit: synthetic issuer tests do not establish compatibility
with the original identity capture schema. The issuer requires a capture
receipt with a `results` member inside the original accepted-file census;
that census contains no such JSON object. A data-only, authenticated mapping
between the original capture and the execution-consumer schema is still
required. This implementation must not be treated as an admissible release
or a usable scientific issuance path merely because its synthetic tests pass.

The zero-science forward-handoff consumer is separate from scientific launch
authorization. It consumes the pinned Manager preflight wrapper and retained
report strictly as authenticated JSON bytes; retained candidate/preflight source
is never imported or executed. Its native Manager owner-claim receipt must
predate consumer/test edits and exclusive root creation and is bound into the
seal. Frozen UNKNOWN reports and all four release blockers remain inadmissible;
current identity evidence cannot close FAILED/UNKNOWN history. A rejected sealed
handoff remains immutable and superseded, not repaired in place. Independent
Host review reads the existing root only, executes no Engineer code or Skill,
and cannot authorize release, science, or a stage transition.

The dormant stage11 full-vector attention-output suffix candidate separates
current execution authority from completed independent evidence review.
`validate_execution_authorization` is read-only and is consumed by the captured
`execution_authorization` launch path; it does not create authority or run science.

The capture envelope must pin both `declaration` and `issuance`. The latter is
a Manager `manager_stage11_execution_issuance` binding the exact declaration,
mission, running-claim snapshot, accepted capture receipt and proposal, with
integer `scientific_run_budget=1`. Declaration time must follow the snapshot's
observed/start times. The latest matching native backlog record must still be
running with that exact start time; a retry of the same mission is a new claim.
Source/snapshot, contract, command, complete environment, account, role/model,
budget, access and concurrency bindings remain mandatory.

Launch selection compares the complete native row's stable identity before and
after authorization selection. Only the top-level textual Host diagnostic fields
`notes` and `last_error` may change or be absent; structured values are rejected.
Every other field, including unknown fields, presence and JSON numeric types,
remains bound. In particular, attempt, authorization, scope/path, constraints,
cost/cycle/budget accounting, access, concurrency, source, routing, outcome and
review changes are not bookkeeping exemptions. When the authenticated Manager
snapshot includes `native_record`, that record's stable identity also binds the
live row; older snapshots retain the exact mission/start and runtime-row gates.
Claim failures expose JSON field differences with explicit presence flags and
remain catchable as both `ValueError` and `RuntimeError`, allowing captured
preflight callers to retain UNKNOWN evidence rather than lose their JSON on an
exception-type mismatch. Neither diagnostics nor command derivation authorizes a
retry or changes any historical failure.

The envelope's `review` identifies the already-terminal accepted-preflight
mission, not the unfinished execution mission. It must bind the accepted terminal
receipt, original accepted payload and proposal. `accepted_identity.payload_source`
names those original reviewed payload bytes; the retained capture's stdout must
match them exactly, so a later capture's filename or receipt cannot substitute
for the historical review link. The terminal receipt's `manager_authority`
also retains its distinct latest independent origin review and issuance binding.
Manager/Engineer roles cannot substitute for either terminal Reviewer.

No terminal DONE review of the current running scientific mission is required
before its check. Old envelopes lacking the current issuance or authentic
original-payload link fail closed. Changed candidate/test bytes do not inherit
old source authority: all source and identity pins must still match, and this
interface repair neither updates declarations nor repeats accepted preflight.
The isolated dual-root source binder resolves frozen scientific source/test
identities against the immutable snapshots named by the reviewed interface's
source-delta manifest. It authenticates evolved workspace source/test bytes
separately against that manifest's operational snapshots; a shared path does not
make the two versions interchangeable. The complete original contract, source
roles and member pins must still equal the Manager execution template.
The shared retained-member verifier remains strict, including rejection of
frozen pins against evolved live bytes. Interface origin, release/member census,
independent review and exact byte pins remain mandatory. Corrected consumer
bytes do not inherit authorization from the older sealed consumer; a later
Manager decision must bind the independently reviewed correction before use.
These gates confer no scientific result, strict-FP16-state W4A16, new-token or
full-model admission; Q24-wide residual/native-S16-RTZ, INT4, FP16 operator/KV,
original-input global references and the frozen `(34319,13)` suffix scope remain
unchanged.

## Middle-pair stage11 value-content suffix intervention

`diagnose_q24_s16_final_head_final_rmsnorm_unselected_direct_hidden_mlp_stage17_stage13_rmsnorm_stage12_attention_value_content_suffix_intervention_v1 --check`
defines one CPU-only non-admission intervention after the independently reviewed
singleton score/value bridge. The question is whether replacing one exact
retained stage07 value component with its original-input FP16 counterpart moves
the retained `(34319,13)` MLP-stage17 maxima and final pair margin in the
bridge-predicted direction.

Selection precedes suffix execution: for each of the 128 value coordinates,
sum its signed bridge-weighted contributions within each retained
control/reference row, then maximize the sum of absolute row totals, breaking
ties by ascending coordinate. The same single selected coordinate is replaced
in all nine controls. This is not the earlier all-128-component value
sufficiency experiment. Each predicted row delta is the negative selected
component's retained contribution; no suffix result enters selection.

Only working stage07 is changed. Source position 0/token 9707, stage03,
stage08 scores, unit stage09 probabilities, input Q24 I/Z/H, and actual FP16
K/V remain protected. The necessary cone is S10-S18, final RMSNorm, and the two
tied-head rows only. Native G128 asymmetric INT4, GEMM nibble order, no
qzero-plus-one adjustment, FP16 scales/operator boundaries, and S16 RTZ are
unchanged. Independent original-input FP16/binary64 global references are not
recomputed or reanchored. Existing stage gates and exact thresholds still apply;
final margins have no new admission threshold.

`supported` requires every affected numerical gate to pass and strict nonzero
agreement with the bridge-predicted sign for both the fixed-weight retained
MLP maxima delta and actual pair-margin delta in all 18 rows. Authenticated
execution with an absent/opposite contrast or a numerical gate failure is
`rejected`. Authentication, operand/state/KV/lineage, zero prediction or
execution-integrity defects yield `UNKNOWN`. Exact measured-minus-predicted
remainders expose the difference between linear attribution and nonlinear
suffix response; retained attribution is not presumed causal.

The measured selection is value coordinate 46. In all nine controls and both
reference branches, the predicted delta and measured fixed-weight MLP maxima
delta are negative, but the actual pair-margin delta is exactly `+1/128`.
Thus none of the 18 rows satisfies the joint directional contrast. S10-S17
numerical gates pass and S18 fails in every control. This independently reviewed
bounded result is `rejected`; it neither refutes the retained linear closure
nor establishes a cause for the opposite terminal-margin response.

The check compiles both files in memory, runs independent bit/nibble, selected
operand, row-dot, directional, closure, mutation and forbidden-dispatch tests,
and emits one stdout JSON document without writing retained evidence. The
reviewed parent has no preserved stdout artifact: its pure report is freshly
bound to pinned source/test/review and authenticated inputs, with a disclosed
report digest rather than a fabricated saved-output pin. Historical failures,
the common `UNKNOWN` selection, and false/zero non-admission flags remain intact.
Q24 residual state is wider than FP16. No outcome establishes strict-FP16-state
W4A16, new-token or full-model admission. This decomposition branch stops after
this one classified intervention.

## Read-only final RMSNorm middle-pair stage11 score/value contrast bridge

`diagnose_q24_s16_final_head_final_rmsnorm_unselected_direct_hidden_mlp_stage17_stage13_rmsnorm_stage12_attention_score_value_contrast_bridge_v1 --check`
extends the retained middle-pair `(34319,13)` o_proj input bridge with
stage08 score/stage09 probability versus value-content accounts. Immediate
parent source/test pins, all actual/original L23 archive fields, canonical
native G128 asymmetric INT4 tensors, source position 0/token 9707, empty-P0
FP16 KV and stage08/stage09 lineage are authenticated before accounting.

Each control ranks its 14 query-head score deltas by descending absolute value,
then ascending head index. The actual and original-input FP16 singleton
probabilities must both equal one. For every retained local output coordinate,
all 128 GQA value components disclose the four exact weighted products
`P_ref*V_ref`, `P_actual*V_ref`, `P_ref*V_actual`, `P_actual*V_actual`, including
their native o_proj weights summed over the component's seven query heads.
Both product-difference orders disclose zero probability and interaction
effects and the same exact value-content difference. This is retained-operand
arithmetic, not softmax execution, Q/K attribution or a causal allocation.

The unchanged selected eight and remaining 120 components partition each
product and effect exactly. Shared local accounts and indexed logical accounts
apply the original norm/scalar/gate/up/down/row/opposite-reference multipliers
through all four downstream fields. Value differences plus the retained AV
boundary and separate actual/negative-original o_proj boundaries close the
existing stage11 contributions. All inherited V-projection, stage00 complement,
o_proj input and source/value entries remain unchanged. Repeated logical
accounts are not independent observations.

The stdout-only CPU check compiles both files in memory, runs independent
half-bit/native-nibble product, closure, mutation and guard tests, and emits one
JSON document. It permits no predecessor checks, operator/prefix/admission
replay, evidence writes or binary64 attention reconstruction. Original-input
global references, exact thresholds, historical failures, the common `UNKNOWN`
gate and false/zero non-admission flags remain intact. Q24 residual state is
wider than FP16; INT4 weights, S16 RTZ and FP16 operator/KV boundaries are
unchanged. This does not establish a dominant cause, strict-FP16-state W4A16,
new-token or full-model admission. Independent Host review remains required.

## Read-only final RMSNorm middle-pair stage11 o_proj input bridge

`diagnose_q24_s16_final_head_final_rmsnorm_unselected_direct_hidden_mlp_stage17_stage13_rmsnorm_stage12_attention_o_projection_input_bridge_v1 --check`
extends the reviewed middle-pair `(34319,13)` V-projection complement with
stage10 input-channel accounts for every retained local stage11 attention
account. Parent source/test pins, actual and original L23 archives, canonical
native asymmetric G128 INT4 `o_proj` weights with FP16 scales and no qzero
plus-one adjustment, stage10/stage11 lineage, source position 0/token 9707 and
empty-P0 FP16 KV remain authenticated. Internal attention uses the retained
original-input FP16 reference, never reconstructed binary64 attention.

Each shared control/output-coordinate table accounts for all 896 exact
`delta(stage10) * weight` terms. The eight largest absolute terms use ascending
input-coordinate tie breaks. Seven contiguous G128 groups in input order
partition the remaining 888 coordinates, disclosing exact signed, absolute and
cancellation mass (`absolute - abs(signed)`). These are channel masses before
GQA value-component aggregation, not the existing source/value component masses.
The actual stage11 output minus its exact stage10 weighted sum and the negative
original-input projection boundary are disclosed separately. They are boundary
accounts, not solely rounding; their sum matches the retained combined `o_proj`
boundary, while the exact input delta matches the retained value account plus
its AV boundary.

Logical accounts index the unchanged parent selections and apply the same norm,
original scalar anchor, gate/up, down, row and opposite-reference multipliers.
Selected plus complement plus both projection boundaries closes each retained
stage11 contribution through all four downstream fields. Shared group signed
mass scales by the field multiplier, absolute mass by its magnitude, and
cancellation is recomputed after aggregation. Existing source/value, selected
V-projection, stage00 complement and other inherited entries are unchanged;
repeated logical selections are not independent samples.

The stdout-only CPU check compiles the diagnostic and focused tests in memory,
requires independent half-bit/native-nibble oracles and mutation/guard checks,
and emits one JSON document. It permits no predecessor checks, operator or
prefix/admission replay, evidence writes or binary64 attention reconstruction.
Independent original-input global references, exact thresholds, historical
failures, the common `UNKNOWN` gate and typed false/zero non-admission flags are
preserved. Q24 residual state is wider than FP16; this does not establish a
dominant cause, strict-FP16-state W4A16, a new token or full-model admission.
Normal independent Host review remains required.

## Read-only final RMSNorm middle-pair stage11 V projection unselected stage00 complement

`diagnose_q24_s16_final_head_final_rmsnorm_unselected_direct_hidden_mlp_stage17_stage13_rmsnorm_stage12_attention_value_projection_unselected_stage00_complement_v1 --check`
extends the reviewed middle-pair `(34319,13)` V projection-input bridge without
changing any selected entry, ranked full-input group, downstream field or parent
gate. Each shared control/value-coordinate account partitions the 888 inputs
outside its eight selected stage00 coordinates into seven contiguous G128 groups
in input order. Each group and the whole complement disclose exact rational
signed, absolute and cancellation mass (`absolute - abs(signed)`); cancellation
is recomputed after aggregation, not summed across groups.

The shared tables and unchanged per-account `input_coordinate_multipliers` define
every downstream group: signed mass scales by the multiplier, absolute mass by
its magnitude, and cancellation is recomputed. The diagnostic checks every
retained selected value account's selected-plus-unselected mass and separate
actual/negative-original projection boundaries through the original norm,
gate/up, down, row and opposite-reference factors. Logical repeated selections
are not independent samples; shared tables avoid duplicating their operands.

Parent source/test pins, actual and original L23 archives, native asymmetric
INT4 `v_proj` tensors, FP16 scales/operands, stage00/stage03/stage07/output-cache
lineage, empty-P0 KV and source token 9707 remain authenticated. Independent
original-input FP16/binary64 global references, exact thresholds, historical
failure gates and the common `UNKNOWN` stop gate remain unchanged. Internal
attention uses only the retained original-input FP16 reference. No predecessor
checks, operator/prefix/admission replay, evidence writes or binary64 internal
attention reconstruction are permitted. Q24 residual state is wider than FP16;
this CPU-only account does not establish strict-FP16-state W4A16, a dominant
cause, a new token or full-model admission. The stdout-only check compiles its
source and focused tests in memory and emits one JSON document; normal
independent Host review remains required.
Inherited non-admission claims remain boolean false, while dispatch, replay and
write counters remain integer zero; their values and types are preserved.

## Read-only final RMSNorm middle-pair stage11 V projection-input bridge

`diagnose_q24_s16_final_head_final_rmsnorm_unselected_direct_hidden_mlp_stage17_stage13_rmsnorm_stage12_attention_value_projection_input_bridge_v1 --check`
extends the source/value bridge's eight selected value components per retained
attention account for pair `(34319,13)`, without reselection or predecessor
checks. It binds that reviewed source version, the original actual/reference
L23 archives, canonical `v_proj` tensors, stage00/stage03/stage07/output-cache
lineage, source position 0/token 9707 and empty-P0 prior KV.

Each shared control/value-coordinate account computes exact rational native
AWQ `input * weight` sums from the retained FP16 stage00 operands, including
the shared FP16 bias. Native GEMM nibble ordering, asymmetric G128 INT4 weights,
FP16 scales and no qzero plus-one adjustment are unchanged. All 896 input
delta terms contribute to signed/absolute/cancellation totals. The eight
largest absolute coordinates and their 888-coordinate complement are separate;
ties use ascending coordinates. Seven contiguous G128 groups rank by absolute
signed sum, then group index.

The retained stage03 delta equals the exact stage00 delta sum plus the actual
projection boundary minus the original-input FP16 projection boundary. These
two boundaries are disclosed separately, not identified solely as rounding.
The original unit-P0 probability times the seven-query-head `o_proj` weight
sum maps each account to its selected parent value component. The same norm
weight, original scalar anchor, gate/up weight, down weight, row factor and
opposite original-reference operand propagate it through all four parent
weighting fields. Projection, control/final-reference-row and global summaries
retain signed, absolute and cancellation mass; shared arithmetic does not turn
repeated logical accounts into independent samples.

The retained scope has 18 control/final-reference rows, 18,432 selected value
accounts, 16,515,072 logical stage00 coordinate terms and 129,024 logical G128
groups, backed by 415 distinct control/value-coordinate accounts. The
`frozen_inherited` FP16 and binary64 final-reference rows both retain the
coordinate-741 hotspot and 1,024 selected value accounts. All selected terms
close exactly at each of the four downstream fields.

The CPU-only stdout check compiles its diagnostic and focused tests in memory,
requires independent half-bit/native-nibble oracles and tamper checks, and emits
one JSON document. Other parent components, all original global references,
exact thresholds, historical failures and common `UNKNOWN` stop gate remain
unchanged. There is no original-prefix/admission/operator replay, evidence
write or binary64 internal-attention reconstruction. Q24 residual state is
wider than FP16; INT4 weights, S16 RTZ and FP16 operator/KV boundaries remain
unchanged. This diagnostic establishes no causal allocation, admission,
strict-FP16-state W4A16, new-token or full-model capability. Independent
Host-owned Reviewer validation remains required.

## Read-only final RMSNorm middle-pair MLP-stage17 stage12 residual source bridge

`diagnose_q24_s16_final_head_final_rmsnorm_unselected_direct_hidden_mlp_stage17_stage13_rmsnorm_stage12_residual_source_bridge_v1 --check`
expands only the retained ranked `direct_stage12_input` terms of the stage13
RMSNorm source bridge for pair `(34319,13)`. Parent selections, scalar anchors,
native gate/up and down weights, row factors and opposite reference operands
are unchanged. The parent report is authenticated by exact reconstruction of
its read-only accounts, not by running its check or replaying model operators.
All ten actual/original L23 archives, operand bits, Q24 state, signed-zero/KV
lineage and canonical tensor bindings remain authenticated.

For actual retained input hidden `h_a`, attention output `att_a`, input Q24
state `q_a`, scratch Q24 state `s_a`, stage12 FP16 boundary `x_a` and
original-input FP16 operands `h_r`, `att_r`, `x_r`, the exact identity is
`delta_x = (h_a-h_r) + (att_a-att_r) + (q_a-h_a) +
(s_a-q_a-att_a) + (x_a-s_a) - (x_r-h_r-att_r)`.
These six terms disclose input-hidden and attention deltas, actual input-Q24
carry, actual attention-state update, actual stage12 FP16 conversion and the
negative original FP16 attention residual boundary separately. Actual and
original residual-boundary totals are also reported separately; they are not
identified causes or rounding-only attributions.

Each component is multiplied by the unchanged post-attention norm weight,
original scalar anchor and gate/up weight, then by the retained down weight,
row factor and opposite reference operand. The six terms close exactly to
each parent direct-stage12 term at stage13, projection and row-weighted linear
operand levels. Global scalar-scale, interaction, stage13 output boundaries,
unranked inputs and later projection/product boundaries are not reassigned.
The census is 2304 logical ranked accounts and 13824 residual components over
18 control/reference-branch rows, including retained coordinate-741 rows.
Account, projection, row and global signed/absolute/cancellation totals are
distinct from cancellation between already-summed parent contributions.

The CPU-only stdout check compiles source/tests in memory and requires focused
independent half-bit/native-nibble oracle checks before emitting one JSON
document. There is no native/decoder/prefix/admission/reference/RMSNorm/
attention/head/row-dot/local replay or evidence write. Both final-reference
branches still use independently propagated original-input FP16 internal
operands; no binary64 internal stages are reconstructed. Exact thresholds,
historical PASS/FAIL facts and the common `UNKNOWN` stop gate remain unchanged.
Q24 residual state is wider than FP16; native G128 asymmetric packed INT4
weights, GEMM nibble ordering without qzero plus-one, FP16 scales, operator
boundaries and KV remain unchanged. This does not establish causality,
admission, strict-FP16-state W4A16, new-token or full-model capability.
Independent Host-owned Reviewer validation remains required.

## Read-only final RMSNorm middle-pair MLP-stage17 stage13 RMSNorm source bridge

`diagnose_q24_s16_final_head_final_rmsnorm_unselected_direct_hidden_mlp_stage17_stage13_rmsnorm_source_bridge_v1 --check`
expands only the reviewed gate/up projection-input bridge's retained ranked
stage13 inputs, without reselection. It authenticates all ten actual/original
L23 archives, including stage12/stage13 operands and Q24/KV state, the canonical
`model.layers.23.post_attention_layernorm.weight`, and the existing six native
gate/up tensors. Both final-reference branches retain the same independently
propagated original-input FP16 internal trajectory.

For retained stage12 FP16 values `x_a`, `x_r`, retained stage13 outputs `y_a`,
`y_r`, norm weight `w` and disclosed scalar anchors `s_a`, `s_r`, the exact
account is `delta_y = w*delta_x*s_r + w*x_r*delta_s +
w*delta_x*delta_s + b_a-b_r`, where `b = y-w*x*s`. The four components are
direct stage12 input, global scalar-scale, interaction and output/boundary
remainder. The binary64 accounting anchor is
`s=1/math.sqrt(float(exact_mean_square+1/1000000))`, not a replayed RMSNorm.
Exact energies, radicand conversion, root/inverse hexadecimal values,
inverse-square identity defects and the native epsilon difference are
disclosed. Actual raw scratch Q24 and its stage12 FP16 conversion difference
remain separate. The output remainders are combined implementation, epsilon,
normalization, multiplication, conversion and scalar-anchor effects, not
rounding-only or individually identified causes.

Multiplying each component by the unchanged native gate/up weight closes to
its parent ranked stage13 contribution. Additional row-weighted accounts use
the retained down weight, final row factor and opposite reference gate/up
operand. Ranked components plus unchanged unranked and projection-boundary
remainders close to the parent's corresponding linear operand term. The
gate/up interaction, joint SiLU/product boundary and down-projection boundary
are not reassigned. Each account and component aggregate retains signed,
absolute and cancellation totals; expanded-component cancellation differs
from cancellation between already-summed parent terms.

The retained census is 18 control/branch rows, 288 gate/up accounts and 2304
ranked inputs, giving 9216 source components. Repeated selections are logical
accounts, not independent samples. The stdout-only CPU check compiles source
and focused tests in memory and requires a nonzero independent half-bit,
native-nibble and scalar-anchor oracle suite before emitting one JSON document.
No native/decoder/prefix/admission/reference/RMSNorm/head/row-dot/local or
nonlinear/product operator is replayed, no binary64 internal L23 stages are
reconstructed, and no evidence is written. Exact thresholds, original global
references, source/operand/state/KV/lineage gates, historical FAIL/PASS facts
and the common `UNKNOWN` stop gate remain unchanged. Q24 residual state is
wider than FP16; INT4 weights, native S16 RTZ and FP16 operator/KV boundaries
are unchanged. These identities establish neither causality nor admission,
strict-FP16-state W4A16, new-token or full-model capability. Independent Host
review remains required.

## Read-only final RMSNorm middle-pair MLP-stage17 gate/up projection-input bridge

`diagnose_q24_s16_final_head_final_rmsnorm_unselected_direct_hidden_mlp_stage17_gate_up_projection_input_bridge_v1 --check`
follows only the retained stage16 selections of the reviewed middle-pair
`(34319, 13)` down-input SiLU/product bridge, not the earlier final-head
channel selections. It binds actual and independently propagated
original-input L23 FP16 stage13, stage14 and stage15 archives, plus the six
canonical gate/up tensors. Native G128 asymmetric packed INT4 GEMM nibble
ordering, no qzero plus-one adjustment, FP16 scales and no bias are preserved.

For each selected gate/up output coordinate, all 896 exact
`(stage13_actual - stage13_original_input_FP16) * W` terms are retained in
input order. The eight largest absolute terms use ascending coordinate IDs
to break ties. All seven contiguous G128 groups, ranked and unranked totals
retain signed contributions, absolute mass and cancellation. Each retained
stage14 gate or stage15 up delta closes through its own separate
projection-boundary remainder; that remainder is not solely rounding.
Input-plus-boundary cancellation is distinct from input-only cancellation.

The retained census has 18 control/branch rows and 144 selected stage16
coordinates, giving 288 gate/up projection accounts, 258048 exact input terms,
2304 ranked input terms and 2016 contiguous G128 group accounts.

Each account binds back to the unchanged gate/up delta in the retained
four-term stage16 bridge. The interaction, joint SiLU/product boundary,
weighted stage17 closure and separate down-projection remainder stay intact.
Control and FP16/binary64 final-reference branches remain separate, including
coordinate-741 representatives and retained maximum ties. Both branches use
the original-input FP16 internal trajectory; no binary64 internal stages are
reconstructed. Repeated selections count as logical accounts, not independent
samples or causal shares.

This stdout-only CPU diagnostic compiles both source and focused tests in
memory and emits one JSON document after a nonzero independent half-bit and
native-nibble oracle suite. It rejects modified predecessor selections,
controls, branches, hotspots, stage16 operands, stage13/14/15 operands and
canonical tensors. No native/decoder/prefix/admission/reference/RMSNorm/head/
row-dot/local or nonlinear/product operators are replayed and no evidence is
written. Exact thresholds, source/operand/state/KV/lineage gates, historical
FAIL/PASS facts and the common `UNKNOWN` stop gate remain unchanged. Q24 state
is wider than FP16; INT4 weights, native S16 RTZ and FP16 operator/KV boundaries
are unchanged. These accounts establish neither causality nor admission,
strict-FP16-state W4A16, new-token or full-model capability. Independent Host
review remains required.

## Read-only final RMSNorm unselected direct-hidden dominance divergence audit

`diagnose_q24_s16_final_head_final_rmsnorm_unselected_direct_hidden_dominance_divergence_audit_v1 --check`
consumes the existing authenticated in-memory bridge/census report interface.
It preserves the complete obstruction census and binds its rows and stored gates
back to the bridge report before adding declarative dominance accounts. It does
not invoke predecessor checks/tests or replay native, decoder, prefix, admission,
reference, RMSNorm, head, row-dot or local operators, and writes no evidence.

Each shared pair/control/branch row retains all seven component signed
corrections, its unique-largest winner (or all largest components if tied),
larger/equal component identities, and exact rational magnitude gaps against
`mlp_stage17` and `fp16_to_branch_terminal_remainder`. A gap is
`abs(component signed correction) - abs(anchor signed correction)`: positive
means larger, negative smaller, and zero equal. It is not a difference of signed
corrections or of absolute coordinate-mass accounts. Original gate rows, strict
inequalities, half-or-reversal alternatives and zero boundaries remain intact.
Partitions retain both branch and control identities, including empty buckets.

The retained census contains three shared pairs, 54 control/branch rows and 378
component rows, with no missing, unexpected, duplicate or out-of-order rows,
no largest-magnitude ties and no equal-magnitude component rows. There are no
nonshared pairs in this input; the full predecessor census remains in the report.

| Pair | FP16 unique-largest, all nine controls | Binary64 unique-largest, all nine controls | MLP unique-largest failures |
|---|---|---|---|
| `(319, 34319)` | `mlp_stage17` | `fp16_to_branch_terminal_remainder` | Nine, binary64 only |
| `(34319, 13)` | `mlp_stage17` | `mlp_stage17` | Zero |
| `(34319, 319)` | `mlp_stage17` | `fp16_to_branch_terminal_remainder` | Nine, binary64 only |

In each of the two divergent pairs, every control has exactly one MLP
unique-largest failure, on binary64; the terminal remainder is its only larger
component. The controls are `frozen_inherited`, `frozen_inherited_o`,
`frozen_inherited_down`, `frozen_inherited_o_down`, `scratch`, `scratch_down`,
`mapped62`, `mapped_all` and `inherited_native`. For `frozen_inherited` on
binary64, the MLP signed correction is
`-1237262848689449616693132231/4951760157141521099596496896`
for `(319, 34319)` and its positive counterpart for `(34319, 319)`.
Both MLP-minus-terminal magnitude gaps are exactly
`-626166459290350775986901351966420866631/10889035741470030827987437816582766592`.

Every pair has nine exact FP16 terminal-remainder zero rows. Their neutralized
margin change equals the retained margin change; zero correction neither
satisfies nonzero directional stability nor produces a strict reversal. These
are separate FP16/binary64 reference boundaries, not missing rows or ties.
The first and third pairs remain `UNKNOWN`, the middle pair selects
`mlp_stage17`, and the common component remains `UNKNOWN` with nested expansion
stopped. The MLP half-gate successes do not remove the mandatory unique-largest
obstruction.

This is row-level ordering of net correction magnitudes, not upstream causality,
a dominant operator, performance attribution, a new residual split, executed
counterfactual, ranking/token result or admission. Historical FAIL/PASS facts,
exact thresholds, source/operand/state/KV/lineage gates and independently
propagated original-input global references remain unchanged. FP16 and binary64
references stay separate; binary64 internal stages are not reconstructed.
Q24 residual state is wider than FP16; native S16 RTZ, native asymmetric G128
packed INT4 weights and FP16 operator boundaries/KV are unchanged. No hardware,
GPU, RTL, ACE2, precision/scale expansion, strict-FP16-state W4A16, new-token or
full-model capability is established. Normal independent Host review is required.

## Read-only final RMSNorm unselected direct-hidden gate obstruction census

`diagnose_q24_s16_final_head_final_rmsnorm_unselected_direct_hidden_gate_obstruction_census_v1 --check`
consumes the existing bridge's in-memory report interface under its retained-input
authentication and read-only guards, without invoking the earlier check or its
tests. The census itself uses only reported rational accounts and decisions;
bridge regeneration is forbidden during census evaluation. No native, decoder,
prefix, admission, reference, RMSNorm, head, row-dot or local operator is replayed.

For each numeric pair, component, control and separate FP16/binary64 branch it
reports exact net and absolute magnitudes, larger competitors, equal-magnitude
ties, strict-half slack and direction product, retained and neutralized margin
changes, and the sign-reversal product. Gate failures are distinguished from
blocking conditions: **more-than-half OR reversal** suffices, but unique-largest,
stable nonzero direction and the complete ordered nine-control/two-branch census
remain mandatory. Zero correction, aggregate, original change and neutralized
change are counted separately; neither a zero product nor exact half equality
satisfies a strict inequality. Missing, duplicate and out-of-order rows are
explicit. Nonshared pairs remain ineligible rather than being treated as shared
evidence. Every shared pair must select the same component to continue.

The original component signed/absolute accounts and within-coordinate,
across-coordinate, within-component and between-net-component cancellation
totals accompany the census. Counts describe overlapping diagnostic rows, not
independent samples or causal shares. A zero failure count is not an obstruction,
and `UNKNOWN` does not imply that every gate fails. The report's stored gate
decisions, parent selection and selected/unselected/head closure are checked
against the exact inputs rather than trusted as labels.

For the retained three shared pairs, every nine-control/two-branch census is
complete and there are no largest-magnitude ties. Pair `(34319, 13)` selects
`mlp_stage17`: it is uniquely largest, directionally stable and explains more
than half in all 18 rows, even though none reverses the retained margin change.
Pairs `(319, 34319)` and `(34319, 319)` remain `UNKNOWN`: `mlp_stage17` fails
unique-largest in nine of 18 rows despite passing the half gate in all rows;
the terminal-reference remainder fails nonzero directional stability because
its nine FP16 corrections are zero. Every other component fails unique-largest
in all 18 rows of those two pairs. Thus the common component remains `UNKNOWN`
without attributing the stop to missing controls, ties or a requirement that
both alternative effect gates pass.

This is a stdout-only declarative explanation, not a new residual split,
counterfactual execution, causal attribution, token/ranking result or admission.
Historical FAIL/PASS facts, exact thresholds, source/operand/state/KV/lineage
gates and independently propagated original-input global references remain
unchanged. The FP16 and binary64 references stay separate; internal binary64
stages are not reconstructed. Q24 residual state is wider than FP16; native S16
RTZ, INT4 weights and FP16 operator boundaries/KV remain unchanged. No evidence
writes, hardware/GPU/RTL/ACE2 work, precision/scale expansion, strict-FP16-state
W4A16, new-token or full-model claim is authorized. Independent Host review
remains required.

## Read-only final RMSNorm unselected direct-hidden residual margin bridge

`diagnose_q24_s16_final_head_final_rmsnorm_unselected_direct_hidden_residual_margin_bridge_v1 --check`
authenticates the retained final-head attempt001/original-input final attempt003
chain and the hidden-delta, logit-margin, selected direct-hidden, scalar-scale,
interaction, boundary and unselected-coordinate bridge inputs. Only when the
parent shared-pair gate selects `direct_hidden` does it split that category over
the unchanged complement of each selected-coordinate union. Coordinate 62 and
all other selected accounts, the unselected global-scale/interaction/boundary
categories, and the separate head-boundary remainder remain intact.

The seven components use the selected direct-hidden bridge's retained L23
accounting: `input_hidden`, `attention_stage11`, `mlp_stage17`,
`actual_residual_boundary`, `negative_reference_residual_boundary`,
`q24_to_fp16_conversion`, and `fp16_to_branch_terminal_remainder`.
Each coordinate is weighted by the same tied-head row difference, norm weight
and reference inverse-norm anchor. Component net signed corrections close
exactly to the unselected direct-hidden aggregate. Absolute accounts measure
the sum of absolute coordinate contributions, not absolute net corrections;
within-coordinate, across-coordinate and between-net-component cancellation
are reported separately.

Original-input FP16 and independently propagated binary64 terminal references
remain separate. Retained internal residual operands are FP16 only. The
`fp16_to_branch_terminal_remainder` is zero for FP16 and explicitly preserves
the independent binary64 terminal difference; binary64 internal stages are
`NOT_RETAINED_NO_RECONSTRUCTION`. This is neither reference substitution nor
an internal binary64 input/attention/MLP reconstruction.

The nested gate uses absolute **net signed** component magnitudes, never
absolute accounts. A shared numeric pair selects a component only when it is
uniquely largest and has the same nonzero direction in every one of the nine
controls and both reference branches, including mapped-all and inherited
variants. In every row it must either explain strictly more than half of the
signed unselected **direct-hidden aggregate** in that aggregate's direction,
or its algebraic removal must strictly reverse the full retained margin
**change**. Here `retained_margin_change = retained_actual_margin -
retained_reference_margin` and `neutralized_retained_margin_change =
retained_margin_change - component_net_signed_correction`; all other terms
stay fixed. Zero is not reversal. This accounting-only change-sign reversal
is not a token choice, actual pair-order/ranking flip or executed counterfactual.
Ties, missing rows, unstable direction or any failed row produce `UNKNOWN`.
Nested expansion stops unless every shared pair selects the same component;
nonshared pairs remain `UNKNOWN`.

The normal stdout JSON retains actual/reference/change margins, the
direct-hidden signed/absolute aggregate, every component's signed/absolute
account, the full unselected remainder, selected signed sum, head-boundary
remainder, every neutralized change, row-level largest/half/reversal/stability
booleans and pair/common decisions. No sidecar or second extraction is needed
to decide the gate. The check compiles both changed Python files in memory and
runs focused retained-bit oracle, cancellation, gate-boundary, mutation-refusal,
single-JSON CLI and forbidden-dispatch/write tests.

This diagnostic is stdout-only, read-only CPU accounting. It preserves
historical failures, exact thresholds, original global references and
source/operand/state/KV/lineage, tokenizer and tied-head gates. It forbids native,
decoder, prefix, admission, reference-producer, RMSNorm, head, row-dot or local
operator replay, earlier-check dispatch, evidence writes and accepted-artifact
overwrites. No hardware/GPU/RTL/simulation/ACE2 work or precision/scale expansion
is authorized. Q24 state remains wider than FP16; native S16 RTZ, INT4 weights
and FP16 operator boundaries/KV are unchanged. No causality, dominant-stage,
runtime-bottleneck, admission, strict-FP16-state W4A16, new-token or full-model
claim follows. Normal independent Host review is required.

Command:
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_final_head_final_rmsnorm_unselected_direct_hidden_residual_margin_bridge_v1 --check`.

## Read-only final RMSNorm unselected-coordinate residual margin bridge

`diagnose_q24_s16_final_head_final_rmsnorm_unselected_coordinate_residual_margin_bridge_v1 --check`
resolves only the signed and absolute unselected-coordinate remainders of the
retained final RMSNorm logit-margin bridge. Each existing pair/branch uses the
complement of its unchanged selected union; coordinate 62 remains selected.
The full boundary/interaction/scalar-scale/direct-hidden/logit-margin/hidden-delta
chain remains present. Reviewed final-head attempt001, independently propagated
original-input final attempt003, retained L23 archives, norm weights, tied-head
row differences and tokenizer identities use the existing authentication gates.
All historical failures, exact thresholds and source/operand/state/KV/lineage
checks are preserved.

For each unselected coordinate, `dW = W[left]-W[right]` weights the exact terms
`w*(a-r)*s_ref`, `w*r*(s_actual-s_ref)`,
`w*(a-r)*(s_actual-s_ref)` and
`(y_actual-w*a*s_actual)-(y_ref-w*r*s_ref)`.
Their aggregate signed sum equals the retained unselected signed remainder.
The absolute remainder is the sum of absolute **coordinate totals**, not the
sum of absolute component terms. Per-category signed/absolute totals, within-
coordinate cancellation, across-coordinate cancellation and cancellation between
net category sums are separately disclosed. Selected accounts and the separate
head-boundary remainder are not reassigned. FP16 and binary64 retain distinct
original inputs and outputs with the same disclosed binary64 scalar anchors.
Boundary effects are not rounding-only; Q24 input conversion stays in the
retained accounts and binary64 internal L23 stages are not reconstructed.

The predeclared selection rule uses net signed corrections, never absolute mass
or hotspot rank. For a numeric pair shared by all nine controls, a category must
be uniquely largest in net signed magnitude in both reference branches and
directionally stable across every control, including mapped-all and inherited
variants. In every row it must explain strictly more than half of the signed
unselected remainder in its direction, or reverse the retained-margin-change
sign upon algebraic removal with every other term fixed. Zero is not a reversal.
This is an accounting counterfactual, not operator replay or token selection.
Ties, missing controls or unmet conditions give `UNKNOWN` and stop nested bridge
expansion. A common category is reported only when every shared pair selects it;
unshared pairs stay explicitly unclassified. This localizes accounting, not an
upstream root cause, dominant stage or runtime bottleneck.

The retained-data result selects `direct_hidden` for each of the three existing
numeric pairs (`319 -> 34319`, `34319 -> 13`, and `34319 -> 319`), each shared by
all nine controls and both reference branches. The complement accounts cover
47,349 coordinates across 54 pair/branch/control rows; these are repeated
coordinate accounts, not additional model positions or tokens. For
`frozen_inherited`, the signed aggregates below are rounded for display; the
diagnostic retains exact rational values and cancellation identities.

| Pair | Reference | Direct hidden | Global scale | Interaction | Boundary delta | Unselected remainder |
|---|---|---:|---:|---:|---:|---:|
| 34319 -> 319 | FP16 | +0.3326507057 | +0.01907087108 | +0.002013435446 | +0.0006081837803 | +0.3543431960 |
| 34319 -> 319 | binary64 | +0.05357032241 | +0.002883154195 | +0.00003835825486 | +0.0001362100705 | +0.05662804493 |
| 34319 -> 13 | FP16 | +0.3935715586 | +0.02237304376 | +0.002382171187 | +0.0007992542475 | +0.4191260278 |
| 34319 -> 13 | binary64 | +0.07728636621 | +0.004771265589 | +0.00005533978514 | -0.0003130274017 | +0.08179994418 |

This identifies the next accounting category under the stated selection rule;
it does not identify an upstream producer mechanism or establish admission.

The stdout-only check compiles both new Python files in memory and runs 20
focused tests, including independent retained-bit and Decimal scalar oracles,
exact signed/absolute closure, selection-rule edge cases, mutation refusals and
forbidden-dispatch/write probes. It emits exactly one JSON document. No earlier
checks, native/decoder/prefix/admission/reference-producer/RMSNorm/head/row-dot/
local-operator replay, evidence writes, accepted-artifact overwrites, hardware,
GPU, RTL, simulation, ACE2 changes or precision/scale expansion are permitted.
Scope remains current P0 and nine controls; the P0-only probability conclusion
is unchanged, and old sparse-cut evidence cannot certify these new parents.
Q24 state is wider than FP16, with unchanged native S16 RTZ, INT4 weights, FP16
operator boundaries and KV. No admission, strict-FP16-state W4A16, new-token or
full-model capability follows. Normal independent Host review is required.

Command:
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_final_head_final_rmsnorm_unselected_coordinate_residual_margin_bridge_v1 --check`.

## Read-only final RMSNorm boundary residual margin bridge

`diagnose_q24_s16_final_head_final_rmsnorm_boundary_residual_margin_bridge_v1 --check`
splits only the selected weighted `boundary_remainder_delta` term. It uses the
read-only authentication chain for reviewed final-head attempt001, independent
original-input final attempt003, hidden-delta/logit-margin/direct-hidden/
scalar-scale/interaction bridge inputs, L23 residual archives, final norm weights,
tied-head rows and tokenizer identities. Failures, exact thresholds,
source/operand/state/KV/lineage gates, existing pairs and selected-coordinate union
(including 62) remain unchanged.

For each selected coordinate and each independent reference branch, it accounts
exactly for `b_actual = y_actual - w*h_actual*s_actual` and
`b_ref = y_ref - w*h_ref*s_ref`, using retained final RMSNorm outputs and unchanged
binary64 scalar anchors. The two weighted components are `dW*b_actual` and
`-dW*b_ref`, where `dW = W[left]-W[right]`. Their sum is exactly the retained
weighted boundary term. Each side retains its unsigned-by-subtraction remainder
and anchored product; the reference contribution explicitly carries a minus
sign. Signed sums, absolute sums, within-coordinate cancellation and
across-coordinate cancellation remain distinct, including zero net boundaries
with nonzero cancelling sides. No division by a boundary delta is used.

FP16 and independently propagated binary64 inputs and outputs remain separate.
Scalar radicand conversion, inverse-square anchor defects, native/contract
epsilon difference and actual raw-Q24-to-FP16 input conversion remain disclosed.
These are combined implementation/normalization/multiply/conversion/anchor
remainders, **not separately identified rounding errors**. Input conversion is
not reassigned to the final RMSNorm boundary. Binary64 internal L23 stages are
not reconstructed. The complete interaction report and its nested bridge
reports remain present; direct-hidden, scalar-scale, interaction, unselected
signed/absolute margin and head-boundary remainders are unchanged.

The stdout-only check compiles both new Python files in memory and runs 20
focused tests, including independent retained-bit and Decimal scalar oracles,
exact cancellation identities, mutation refusals and forbidden-dispatch/write
probes. It emits one JSON document. No earlier checks, native/decoder/prefix/
admission/reference-producer/RMSNorm/head/row-dot/local-operator replay, evidence
writes, accepted-artifact overwrites, hardware/GPU/RTL/simulation, ACE2 work or
precision/scale expansion is permitted. This is accounting closure, not causal
attribution or a performance diagnosis. Q24 state is wider than FP16; INT4
weights and FP16 operator boundaries/KV are unchanged. No admission, policy
adoption, successor publication, strict-FP16-state W4A16, new-token or full-model
capability follows. Normal independent Host review is required.

Command:
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_final_head_final_rmsnorm_boundary_residual_margin_bridge_v1 --check`.

## Read-only final RMSNorm interaction residual margin bridge

`diagnose_q24_s16_final_head_final_rmsnorm_interaction_residual_margin_bridge_v1 --check`
splits only the selected weighted `interaction` term of the final RMSNorm
logit-margin bridge. The existing read-only authentication chain preserves
reviewed final-head attempt001, original-input final attempt003 authority,
hidden-delta/logit-margin/direct-hidden/scalar-scale bridge inputs and outputs,
L23 residual archives, norm weights, tied-head row tensors and tokenizer
identities. Existing cutoff/exchanged pairs, selected coordinates including 62,
historical failures, exact thresholds and source/operand/state/KV/lineage gates
remain unchanged.

For each selected output coordinate, the exact grid is
`G[k,j] = (W[left]-W[right])*norm_weight*c_k*delta_s_j`.
Its seven rows are the retained direct-hidden coordinate components; its nine
columns are the retained **global** scalar-scale delta components. The two
scalar-anchor-defect columns remain separate from the seven residual-energy
columns. The identity
`sum(G) = (W[left]-W[right])*norm_weight*delta_hidden*delta_scale`
closes only the weighted interaction term. No division by hidden or scale delta
is needed: zero net deltas can still contain nonzero cancelling cross-products.
Per-coordinate row, column and grid masses and per-cell totals across selected
coordinates disclose signed sums, absolute sums and cancellation. Within- and
across-coordinate cancellation remain distinct.

The complete scalar-scale report, including its nested direct-hidden,
logit-margin and hidden-delta reports, is retained. Scalar-anchor conversion and
inverse-square identity defects, unselected residual-energy totals, direct-hidden,
global-scale, RMSNorm boundary, unselected-coordinate signed/absolute margin
remainders and head-boundary remainders are not reassigned. FP16 and independently
propagated binary64 references stay separate. Binary64 internal L23 stages remain
unavailable and are not reconstructed; the explicit FP16-terminal-minus-binary64
terminal component is retained, including its interaction cross-products.

This is **accounting closure only**, not causal attribution, independent
branch-error effects, rounding-only attribution or a runtime bottleneck claim.
The stdout-only command compiles its two Python files in memory and runs 22
focused tests with independent retained-bit, Decimal scalar and exact-algebra
oracles, mutation refusals and forbidden-dispatch/write probes. It emits exactly
one JSON document. Earlier checks, native/decoder/prefix/admission,
reference-producer/RMSNorm/head/row-dot/local-operator replay, evidence writes,
accepted-artifact overwrites, hardware/GPU/RTL/simulation, ACE2 work and
precision/scale expansion remain forbidden. Q24 state is wider than FP16;
INT4 weights and FP16 operator boundaries/KV remain unchanged. No admission,
policy adoption, successor publication, strict-FP16-state W4A16, new-token or
full-model capability follows. Normal independent Host review is required.

Command:
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_final_head_final_rmsnorm_interaction_residual_margin_bridge_v1 --check`.

## Read-only final RMSNorm scalar-scale residual margin bridge

`diagnose_q24_s16_final_head_final_rmsnorm_scalar_scale_residual_margin_bridge_v1 --check`
splits only the selected weighted `global_scale` term of the retained
logit-margin bridge. Its read-only authentication chain preserves reviewed
final-head attempt001, original-input final attempt003 authority, retained
hidden-delta/logit-margin/direct-hidden bridge inputs and outputs, L23 residual
archives, norm weights, tied-head rows and tokenizer identities. The existing
cutoff/exchanged pairs, selected coordinates including 62, historical failures,
exact thresholds and source/operand/state/KV/lineage gates remain unchanged.

For actual stage18 hidden `a` and independent branch terminal `r`, use the same
seven exact hidden-difference components `c_k` as the direct-hidden bridge:
input, attention stage11, MLP stage17, actual residual boundary, negative FP16
reference residual boundary, actual Q24-to-FP16 conversion, and FP16 terminal
minus independent branch terminal. The exact residual-energy allocation is
`E_k = mean((a+r)*c_k) = mean(2*r*c_k) + mean((a-r)*c_k)`.
Its sum is `mean(a*a)-mean(r*r)`. Linear and quadratic allocations, signed and
absolute component totals, selected-coordinate energy and unselected
signed/absolute energy remainders are disclosed. Quadratic allocations include
cross-component interactions; they are not independent squared branch errors.

With the unchanged binary64 scalar anchors `s_a`, `s_r`, exact radicands
`q = mean(h*h)+1/1000000` and anchor defects `d = s*s*q-1`, the scalar identity is
`s_a-s_r = (-delta_mean_square + q_r*d_a - q_a*d_r)/(q_a*q_r*(s_a+s_r))`.
Each residual-energy component receives the common negative reciprocal
denominator. The actual and negative reference anchor-defect components remain
separate. This identity never divides by the energy delta, including when it is
zero. Radicand conversion deltas and the original inverse-square anchor defects
remain present; no RMSNorm or reference output is regenerated.

Each selected **output** coordinate multiplies all nine global scalar
components by `(W[left]-W[right])*norm_weight*r`. The global energy account
uses the retained 896-coordinate hidden width, not just the selected output
coordinate's energy. Signed/absolute totals and within/across-coordinate
cancellation close exactly the selected weighted global-scale term. This
symmetric secant allocation is a disclosed accounting convention, not a unique
causal allocation, rounding-only attribution or runtime bottleneck claim.

FP16 and binary64 references remain independent. Binary64 internal L23 stages
are not retained or reconstructed: input/attention/MLP components use retained
original-input FP16 stages, with the explicit independent binary64 terminal
remainder. The complete direct-hidden and logit-margin reports remain present;
direct-hidden, interaction, RMSNorm boundary, unselected-coordinate and
head-boundary accounts are not reassigned or substituted.

The stdout-only command compiles its two Python files in memory and runs
20 focused tests, including independent retained-bit, Decimal scalar and exact
algebra oracles, mutation refusals and forbidden-dispatch/write probes. Only one
JSON document is emitted. No prior checks, native/decoder/prefix/admission,
reference-producer/RMSNorm/head/row-dot/local-operator replay, evidence writes,
accepted-artifact overwrites, hardware/GPU/RTL/simulation, ACE2 changes or
precision/scale expansion are permitted. Q24 state remains wider than FP16,
with unchanged INT4 weights and FP16 operator boundaries/KV. No admission,
policy adoption, successor publication, strict-FP16-state W4A16, new-token or
full-model capability follows. Normal independent Host review is required.

Command:
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_final_head_final_rmsnorm_scalar_scale_residual_margin_bridge_v1 --check`.

## Read-only selected direct-hidden hotspot partition localizer

`diagnose_q24_s16_final_head_final_rmsnorm_selected_direct_hidden_hotspot_partition_localizer_v1 --check`
consumes only the authenticated, independently reviewed stability stdout,
capture, source/test bytes and terminal review. Nested hotspot-audit and
direct-hidden bridge pins and complete evidence remain unchanged. No ancestor
producer, tensor decoder, RMSNorm, head/row-dot or native decoder is executed.

The localizer partitions the same 54 dependent retained accounts by branch,
control, ordered pair, control/branch, pair/branch and control/pair. Each
partition discloses stable/variable status, every winner class and its complete
control/pair/branch membership, exact maximum-minus-runner-up dominance gaps,
and class/partition gap extrema. An exception means a winner different from a
partition's unique frequency-modal winner, not a causal baseline. Frequency
ties have no distinguished baseline or labeled exceptions; all tied modes and
winner-class members remain explicit. Overlapping counts are not independent
observations.

The retained global coordinate counts are 241:34 and 62:20. Only the two
binary64 outer-pair partitions `(34319,319)` and `(319,34319)` vary across
controls: each has eight coordinate-241 wins and one coordinate-62 win, the
latter under `mapped_all`. Its exact dominance gap in both orientations is
`1827610068439962304726431454702255/1329227995784915872903807060280344576`.
The global coordinate-component counts remain `(62,input_hidden)`:36,
`(241,mlp_stage17)`:17 and `(241,fp16_to_branch_terminal_remainder)`:1.
Only `(34319,13,binary64)` varies across controls: eight MLP-stage17 wins at
coordinate 241 and one terminal-remainder win at that coordinate, again under
`mapped_all`. The exception gap is
`3832757189268268603981093761768175/1329227995784915872903807060280344576`.
All other pair/branch partitions have one winner identity across all nine
controls.

These are retained-domain descriptive accounting facts, not causal allocation,
performance attribution, an intervention or a successor result. Exact thresholds,
independently propagated original-input global references, all
source/operand/state/KV/lineage gates and historical failures remain unchanged.
Q24 residuals remain wider than FP16; native S16 RTZ, official G128 asymmetric
packed INT4 semantics and FP16 operator/scales/KV boundaries are unchanged.
The row-319 missing 896-element pre-round producer remains a closed limitation.
No strict-FP16-state W4A16, new-token or full-model admission is established.
Independent Host Reviewer closure is required.

## Read-only selected direct-hidden hotspot stability classifier

`diagnose_q24_s16_final_head_final_rmsnorm_selected_direct_hidden_hotspot_stability_classifier_v1 --check`
consumes the authenticated, independently reviewed selected-direct-hidden hotspot
audit stdout, capture, source/test pins and review receipt. It retains the complete
audit and its nested direct-hidden bridge evidence without reopening tensor inputs
or importing/replaying either producer. Its domain is exactly 54 dependent
control/ordered-pair/branch accounts, 1,035 selected-coordinate accounts and 7,245
component terms, not the unselected coordinates or new model execution.

The classifier distinguishes stable nonempty winner sets from stable unique
hotspots for component absolute-coordinate mass, net direct-hidden coordinates,
coordinate-component cells and each component's coordinates. The dominance gap
is the exact rational maximum minus the second row magnitude, including tied
maxima. A separate nullable distinct-level gap prevents a tied maximum from
appearing uniquely dominant. Every row's signed value, absolute magnitude,
competition rank and gap to maximum is disclosed. Zero-only tables retain all
maximizers and zero rows but have no nonzero winner or stable nonzero hotspot.

Global, branch, control, ordered-pair, control/branch, pair/branch and control/pair
partitions expose winner membership, unique-win counts, ties, zero rows and exact
gap extrema. These are descriptive counts of overlapping retained accounts, not
independent observations, causal attribution, performance diagnosis or an
intervention. All original-input references, thresholds, failure and
source/operand/state/KV/lineage gates remain in the unchanged nested evidence.

In this retained domain, `mlp_stage17` is the unique top component in all 54
accounts. The unique net-coordinate winner varies: coordinate 241 wins 34
accounts and coordinate 62 wins 20. The unique coordinate-component winner is
`(62, input_hidden)` in 36 accounts, `(241, mlp_stage17)` in 17, and
`(241, fp16_to_branch_terminal_remainder)` in one. Per-component coordinate
hotspots are globally stable and unique only for `input_hidden` (62) and
`mlp_stage17` (241). The other component coordinate winners vary; the terminal
remainder additionally has 27 zero-only FP16 accounts (504 zero coordinate
rows). There are no tied nonzero maxima in these retained tables. The 7,245
coordinate-component terms include 630 zeros: 504 terminal-remainder terms and
126 negative-reference residual-boundary terms. Zero rows remain in the output,
not excluded observations or inferred unique hotspots.

Q24-wide residual state, native S16 RTZ, official INT4 semantics and FP16 operator
boundaries/KV remain unchanged. There is no strict-FP16-state W4A16, new-token or
full-model admission, successor publication or reopening of closed branches.
Independent Host Reviewer closure is required.

## Read-only selected direct-hidden component hotspot audit

`diagnose_q24_s16_final_head_final_rmsnorm_selected_direct_hidden_component_hotspot_audit_v1 --check`
consumes only the byte-pinned, independently reviewed direct-hidden bridge JSON
and its authenticated capture. It does not import the bridge or replay its
producer, tensor decoding, RMSNorm, head, selected-row dot or reference stages.
The producer source/test bytes are checked against the retained capture.
Historical source/input pins, original-input references, failure gates and
lineage remain present verbatim in `retained_50f`; tensor inputs are not reopened.

The audit covers the retained 1,035 selected-coordinate accounts (7,245 weighted
component terms), separately for nine controls, 27 ordered pairs and 54 branch
accounts. Each table keeps every coordinate and all seven components, including
zeros. Exact absolute-magnitude rankings use competition ranks for ties,
ascending coordinate IDs and the declared component order for presentation.
Component magnitude is the sum of coordinate absolutes, not the absolute net
signed total. Zero-only tables have no nonzero hotspot. Coordinate 62 appears
once per selected union with an exact separate complement, never as an additional
contribution.

Signed, absolute, positive and negative masses retain exact rational arithmetic.
Cancellation is `absolute_mass - abs(signed_mass)`, twice the one-sided cancelled
mass. Within-coordinate and across-coordinate cancellation close exactly to the
retained selected direct-hidden totals. All global-scale, interaction, RMSNorm
boundary, unselected-coordinate and head-boundary accounting stays unchanged.
The FP16 terminal remainder remains zero; the binary64 remainder uses the
independently propagated original terminal, not reconstructed internal stages.

These rankings describe retained accounting, not causes, performance bottlenecks,
an intervention or a successor. Q24 state remains wider than FP16; native S16 RTZ,
official INT4 weights and FP16 operator boundaries/KV remain unchanged. No
strict-FP16-state W4A16, new-token or full-model admission follows. Closed branch
failures and the row-319 missing pre-round producer limitation remain unchanged.
Independent Host Reviewer closure is required.

## Read-only final RMSNorm direct-hidden residual margin bridge

`diagnose_q24_s16_final_head_final_rmsnorm_direct_hidden_residual_margin_bridge_v1 --check`
extends only the selected `direct_hidden` term of the logit-margin bridge.
The existing read-only chain authenticates reviewed final-head attempt001,
original-input final attempt003, L23 residual archives, final RMSNorm inputs and
outputs, norm weights, tied-head row tensors and tokenizer identities. Existing
cutoff/exchanged pairs, coordinate selection (including 62), failures, thresholds
and source/operand/state/KV/lineage gates remain unchanged.

For each selected coordinate, the common exact multiplier is
`(W[left]-W[right])*norm_weight*s_ref`. Its hidden difference is split into
actual-minus-original-FP16 `input_hidden`, attention `stage11`, MLP `stage17`,
actual residual-boundary remainder before output conversion, negative FP16
reference residual-boundary remainder, actual Q24-to-stage18 FP16 conversion,
and original FP16 terminal minus the independent branch terminal. Actual
residual-boundary carry/attention-update/MLP-update and negative reference
attention/MLP residual-boundary subparts are disclosed separately.

The last component is zero for the FP16 branch. For binary64 it explicitly
retains the independently propagated original binary64 terminal difference:
binary64 internal L23 stages are not retained, substituted or reconstructed.
Thus these are FP16-internal residual accounts with a separate binary64
terminal remainder, **not** a binary64 input/attention/MLP stage decomposition.
Neither global reference is reanchored or derived by casting the other.

Signed/absolute component totals and within-coordinate/across-coordinate
cancellation close only selected direct-hidden margin terms. The complete
upstream margin report remains present, preserving global-scale, interaction,
RMSNorm boundary, unselected-coordinate and head-boundary terms unchanged.
Boundary remainders are not rounding-only causes; exact accounting does not
establish causal percentages, upstream root causes or runtime bottlenecks.

The stdout-only command compiles both new Python files in memory and runs
22 focused tests with independent retained-bit and scalar oracles, mutation
refusals and forbidden-dispatch/write probes. Earlier checks, native/decoder/
prefix/admission/reference-producer/RMSNorm/head/row-dot/local-operator replay,
evidence writes, accepted-artifact overwrites, hardware/GPU/RTL/simulation,
ACE2 work and precision/scale expansion remain forbidden. Q24 state remains
wider than FP16; INT4 weights and FP16 operator boundaries/KV are unchanged.
No admission, policy adoption, successor publication, strict-FP16-state W4A16,
new-token or full-model capability follows. Independent Host review is required.

Command:
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_final_head_final_rmsnorm_direct_hidden_residual_margin_bridge_v1 --check`.

## Read-only final RMSNorm logit-margin bridge

`diagnose_q24_s16_final_head_final_rmsnorm_logit_margin_bridge_v1 --check`
connects the retained final RMSNorm hidden-delta accounting to the existing
cutoff/exchanged numeric diagnostic pairs. Reviewed final-head attempt001,
original-input final attempt003 authority, retained stage18 and final RMSNorm
boundaries, norm weight/epsilon, tied-head row tensors and tokenizer identities
are authenticated through the existing read-only chain. Historical failures,
exact thresholds and source/operand/state/KV/lineage gates are unchanged.

For each pair and separate FP16/binary64 reference branch, coordinates are the
union of the existing top eight absolute RMSNorm deltas, pair-margin delta
contributions and tied-head row differences, plus coordinate 62. Rankings break
ties by ascending coordinate; the deduplicated union is emitted in coordinate
order. Repeated selections across controls, pairs and branches remain logical
accounts. Neither independent reference branch is cast from the other.

At each selected coordinate, all four exact hidden-bridge terms are multiplied
by `dW = W[left,i]-W[right,i]`: direct hidden change, global scalar scale change,
interaction and boundary/conversion remainder change. Scalar-anchor conversion
defects and actual Q24-to-stage18 FP16 conversion remain disclosed. Signed and
absolute term totals, within-coordinate and across-coordinate cancellation,
unselected signed/absolute coordinate remainders and the separate head-boundary
remainder close the retained pair-margin change. Boundary terms are combined
implementation/conversion/scalar-anchor accounting, not rounding-only causes.
No causal percentages or upstream/performance attribution follow from closure.

The stdout-only command compiles its two Python files in memory and runs only
its 20 focused tests, including independent retained-bit, scalar and algebra
oracles. Guards forbid native/decoder/prefix/admission/reference-producer,
RMSNorm/head/row-dot/other operator replay, prior checks, evidence writes,
accepted-artifact overwrites, hardware/GPU/RTL/simulation and ACE2 work.
Rational accounting is not precision or scale expansion: Q24 state remains
wider than FP16, with unchanged INT4 weights and FP16 operator boundaries/KV.
This does not establish admission, policy adoption, successor publication,
strict-FP16-state W4A16, new-token or full-model capability. Normal independent
Host Reviewer validation is required for this new diagnostic.

Command:
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_final_head_final_rmsnorm_logit_margin_bridge_v1 --check`.

## Read-only final RMSNorm hidden-delta bridge

`diagnose_q24_s16_final_head_final_rmsnorm_hidden_delta_bridge_v1 --check`
authenticates reviewed final-head attempt001, original-input final attempt003
authority, nine retained L23 terminal archives, the original FP16 stage18
archive and independently propagated binary64 terminal hidden vector. Final
RMSNorm outputs, norm weight, epsilon contract, tokenizer and tied-head tensor
bindings retain their existing authentication and failure/threshold/source/
operand/state/KV/lineage gates.

For each control and each separate FP16/binary64 reference branch, eight
coordinates are selected by absolute retained final RMSNorm delta, breaking
ties by ascending coordinate. Coordinate 62 is also disclosed independently:
144 hotspot accounts and 18 forced-coordinate accounts, including repeats.
With retained hidden `h`, output `y` and weight `w`, the diagnostic uses a
binary64 **scalar accounting anchor**
`s=1/sqrt(float(exact_mean(h*h)+1/1000000))`, not a replayed RMSNorm vector.
All subsequent accounting is exact rational arithmetic:
`delta_y=w*delta_h*s_ref+w*h_ref*delta_s+w*delta_h*delta_s+delta_b`,
where `b=y-w*h*s`. Direct hidden change, global scale change, interaction and
boundary-remainder change remain separate, with signed/absolute totals and
cancellation mass. The global mean-square change is independently split into
`mean(2*h_ref*delta_h)` and `mean(delta_h**2)`; selected-coordinate and all-other
energy changes are disclosed without allocating nonlinear scale effects as
causal percentages.

The radicand's binary64 conversion delta, inverse-square identity defect,
native Q48 epsilon difference, and actual Q24-to-stage18 FP16 conversion are
explicit. Raw Q24 state is not propagated through RMSNorm. Combined output
remainders include normalization, implementation, epsilon, multiply/conversion
and scalar-anchor differences, not rounding alone. Neither branch is cast
from the other. This local identity does not identify upstream causes or
establish admission, strict-FP16-state W4A16, new-token or full-model capability.
Q24 residual state remains wider than FP16; INT4 weights and FP16 operator/KV
boundaries are unchanged.

The stdout-only command compiles its two Python files in memory and runs its
own 18 focused tests, with independent bit-decoding/algebra and scalar-oracle
checks. Dispatch/write guards forbid earlier checks, all operator and reference
replay, native/decoder/prefix/admission execution, evidence writes, hardware/
GPU/RTL/simulation and ACE2 work. Normal independent Host review is required.

Command:
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_final_head_final_rmsnorm_hidden_delta_bridge_v1 --check`.

## Read-only final-head L23 MLP unselected down-input SiLU/product bridge

`diagnose_q24_s16_final_head_l23_mlp_unselected_down_input_silu_product_bridge_v1 --check`
extends the retained local SiLU/product bridge only over the unchanged complement
of its eight selected stage16 coordinates per hotspot. Nine controls, two separate
final-head selection branches and eight hotspots give 144 logical accounts of
4856 unselected inputs: 699264 coordinate accounts and 2797056 weighted terms.
Repeated hotspots count separately. All selected accounts and the previous report
remain present verbatim.

For retained `g=stage14`, `u=stage15`, `y=stage16`, the four weighted terms are
`W*delta_g*u_ref`, `W*g_ref*delta_u`, `W*delta_g*delta_u` and
`W*((y_actual-g_actual*u_actual)-(y_ref-g_ref*u_ref))`. Their net signed
category sums close the original unselected down-input signed remainder exactly.
The raw product is **not SiLU**; separate nonlinear and product-rounding effects
are not identifiable without forbidden replay. Weighted actual/reference
`y-g*u` remainders are disclosed separately. Absolute category accounts preserve
within-coordinate, across-coordinate and between-category cancellation; their
sum need not equal the original absolute input remainder. The unchanged selected
signed sum, unselected signed sum and down-projection boundary remainder close
the retained stage17 delta.

Both final-head selection branches use the same independently propagated
original-input L23 FP16 internal operands, never reconstructed binary64 stages.
Ten retained L23 archives and nine gate/up/down projection tensors use the
existing source/operand/state/KV/lineage authentication. Native G128 asymmetric
INT4 nibble ordering without qzero plus-one, FP16 scales/operator boundaries/KV,
Q24 residual state wider than FP16, exact thresholds and all L21/L22/L23 failures
remain unchanged. Rational arithmetic is accounting, not precision expansion.
This local account does not inform or extend the RMSNorm pair-selection gate,
establish causality or change a token margin. No native/prefix/admission/reference/
operator replay, evidence writes, simulation/RTL/hardware/GPU/ACE2 work,
strict-FP16-state W4A16, new-token or full-model admission is authorized.

The stdout-only check compiles its two files in memory, runs 14 focused tests
including an independent half-bit/native-nibble integer-grid oracle over every
unselected input, validates the strict output schema and emits one JSON document.
Normal independent Host Reviewer validation is required.

Command:
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_final_head_l23_mlp_unselected_down_input_silu_product_bridge_v1 --check`.

## Read-only final-head L23 MLP SiLU/product bridge

`diagnose_q24_s16_final_head_l23_mlp_silu_product_bridge_v1 --check` extends
the retained MLP-down stage16 selections and gate/up projection accounts:
nine controls, two final-head selection branches, eight channel hotspots and
eight input coordinates per hotspot give 1152 logical bridge accounts.
Reviewed final-head attempt001, ten L23 archives (nine terminals and the
original-input FP16 reference), and all nine canonical gate/up/down tensors
are authenticated by the existing read-only chain. Repeated selections count
separately. No earlier check or operator is rerun.

For retained `g=stage14`, `u=stage15`, `y=stage16`, define the **accounting
remainder** `b=y-g*u` independently on each trajectory. The exact identity is
`delta_y = delta_g*u_ref + g_ref*delta_u + delta_g*delta_u + delta_b`.
The raw `g*u` baseline is **not a SiLU output**. The interaction remains
separate; actual/reference boundary remainders jointly contain the nonlinear
SiLU effect and implementation/product/conversion differences. Separate
SiLU and product-rounding effects are not identifiable from these retained
boundaries without replay; no sigmoid, exponential or intermediate SiLU
value is reconstructed. This is exact local closure, not a causal attribution.

All 3456 operand terms, including 1152 interactions, and 1152 boundary-delta
terms have exact rational signed/absolute totals. Absolute rankings break
ties by term index or coordinate. Cancellation mass is the absolute sum minus
the absolute signed sum. Authenticated down weights map all four terms back
to each selected down-input contribution. Per-hotspot totals retain unselected
input signed/absolute remainders and the original down-projection boundary
remainder, closing the retained stage17 delta without operator propagation.

Both selection branches use the same independently propagated original-input
L23 FP16 internal trajectory, not reconstructed binary64 stages. Existing
global references, thresholds, failures, source/operand/state/KV/lineage gates
and non-admission boundaries remain unchanged. Q24 residual state is wider
than FP16; INT4 weights, FP16 operator boundaries/KV and native S16 RTZ are
unchanged. Rational accounting is not precision expansion. No native, prefix,
admission, reference or operator replay, evidence writes, hardware/GPU/RTL/
ACE2 work, strict-FP16-state W4A16, new-token or full-model claim is permitted.

The stdout-only check compiles its two Python files in memory and runs 16
focused tests, including independent half-bit and down-weight nibble oracles.
Normal independent Host Reviewer validation is required.

Command:
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_final_head_l23_mlp_silu_product_bridge_v1 --check`.

## Read-only final-head L23 MLP gate/up unranked stage13 input bridge

`diagnose_q24_s16_final_head_l23_mlp_gate_up_unselected_stage13_input_bridge_v1 --check`
extends the gate/up projection-input report without replacing its ranked
entries, full-input groups or projection-boundary remainders. For each of
2304 gate/up accounts, `unranked_stage13_inputs` excludes the same eight
ranked stage13 coordinates and reports all seven contiguous G128 groups
in input order. Each group retains its unranked/excluded counts, exact net
signed contribution and sum of absolute coordinate contributions. The
888-coordinate totals close the existing `unranked_signed_remainder` and
`unranked_absolute_remainder` exactly: 2045952 logical unranked terms and
16128 group accounts, counting repeated selections separately.

For `t_i=(stage13_actual_i-stage13_original_input_FP16_i)*W_i`, each group
accounts only for its coordinates outside the retained ranked set.
The group signed sums total the unranked signed remainder; group absolute
sums total the unranked absolute remainder, without discarding cancellation.
Ranked signed sum plus unranked signed sum plus the unchanged projection
boundary remainder equals the retained stage14 gate or stage15 up delta.
Ranked absolute sum plus unranked absolute sum equals the original full-input
absolute account. Projection-boundary remainders are not solely rounding.

Both final-head branch selections retain the independently propagated
original-input L23 FP16 internal reference. Binary64 internals are not
reconstructed. Nine controls/P0, down-input selections, original global
references, thresholds, historical failures and source/operand/state/KV/
lineage gates remain unchanged. Native G128 asymmetric packed INT4,
FP16 scales/operator boundaries/KV and native S16 RTZ remain unchanged;
Q24 residual state is wider than FP16. This is local post-RMSNorm accounting,
not a final-RMSNorm pair-selection gate report, dominance claim, executed
counterfactual, replay, admission, strict-FP16-state W4A16, new-token or
full-model result.

The stdout-only check compiles its two Python files in memory and runs 18
focused tests with independent half-bit/native-nibble oracles. Earlier
checks, native/prefix/admission/operator dispatch and writes remain forbidden.
One JSON document retains the complete accounting and dispatch/write audit.
Normal independent Host Reviewer validation is required.

Command:
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_final_head_l23_mlp_gate_up_unselected_stage13_input_bridge_v1 --check`.

## Read-only final-head L23 MLP gate/up input coordinates

`diagnose_q24_s16_final_head_l23_mlp_gate_up_projection_input_coordinates_v1 --check`
follows the existing eight stage16 input coordinates of each L23 MLP-down
hotspot, preserving the nine controls, two final-head reference-branch
selections and eight channel hotspots per branch. The 1152 logical stage16
selections yield 2304 gate/up accounts, 2064384 input-coordinate terms, 18432
ranked terms and 16128 G128 groups, counting repeated selections separately.

The canonical source maps `mlp.gate_proj` from stage13 to stage14 and
`mlp.up_proj` from stage13 to stage15. Each projection binds exactly three
`model.layers.23.mlp.{gate_proj,up_proj}` tensors: INT32 packed qweight
`[896,608]`, INT32 packed qzeros `[7,608]` and FP16 scales `[7,4864]`.
There are six gate/up bindings and no bias bindings. Native asymmetric
G128 INT4 GEMM nibble ordering and no qzero plus-one adjustment are unchanged.

Each retained gate/up output delta closes with all 896 exact
`(stage13_actual-stage13_original_input_FP16)*W` terms and a separate
projection-boundary remainder. Eight terms are ranked by descending absolute
contribution then ascending coordinate; all seven contiguous groups are
reported in input order. Signed/absolute totals and unranked remainders
preserve cancellation. A boundary remainder is not attributed solely to rounding.
Both selections use the independently propagated original-input L23 **FP16**
trajectory. No binary64 internal stages are reconstructed.

This is post-RMSNorm input accounting, not propagation of raw Q24 state
through RMSNorm, a SiLU/product decomposition, or causal attribution of
stage16, down-projection or final-head error. The original down-input/channel
report, global references, exact thresholds, failures and source/operand/
state/KV/lineage gates remain intact. Q24 state is wider than FP16; INT4
weights, FP16 operator boundaries/KV and native S16 RTZ remain unchanged.
There is no admission, strict-FP16-state W4A16, new-token or full-model claim.

The stdout-only check compiles its two files in memory and runs its own 18
focused tests, including independent half-bit/native-nibble oracles over
every logical account. Earlier checks, projection/SiLU/RMSNorm/native/prefix/
admission replay and evidence writes are forbidden. Normal independent Host
Reviewer validation is required.

Command:
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_final_head_l23_mlp_gate_up_projection_input_coordinates_v1 --check`.

## Read-only final-head L23 MLP-down input coordinates

`diagnose_q24_s16_final_head_l23_mlp_down_projection_input_coordinates_v1 --check`
authenticates reviewed final-head attempt001, the original-input L23 FP16
archive, all nine retained L23 terminal archives and the three original
`model.layers.23.mlp.down_proj` tensor bindings. The down projection has no
bias binding. Native G128 asymmetric packed INT4 GEMM nibble ordering,
no qzero plus-one adjustment, and FP16 scales remain unchanged.

For both retained final-head reference-branch selections and their eight
output-channel hotspots, each stage17 delta is accounted for using all 4864
exact `(stage16_actual-stage16_reference)*W_down` input-coordinate terms.
The census is 144 logical accounts, 700416 terms and 5472 contiguous G128
groups, including repeated hotspots. The top eight coordinates are ordered
by descending absolute contribution, with ascending coordinate tie breaks;
all 38 groups are reported in input order. Signed and absolute sums and both
unranked remainders preserve cancellation. A separate down-projection boundary
remainder closes retained stage17 delta minus exact input sum; it is not
attributed solely to rounding.

Both selections use the same independently propagated original-input **FP16
L23** stage16/stage17 trajectory. Binary64 internal stages are not reconstructed.
The unchanged channel report preserves global FP16/binary64 references, exact
thresholds and L21/L22/L23 failures. This local identity is not an MLP, SiLU,
RMSNorm or head replay, propagation of raw Q24 state, or a causal attribution
of final-head error. Source/operand/state/KV/lineage gates remain in force.
Q24 residual state is wider than FP16; INT4 weights, FP16 operator/KV boundaries
and native S16 RTZ are unchanged. No admission, strict-FP16-state W4A16,
new-token or full-model capability is established.

The stdout-only check compiles its two files in memory and runs only its own
focused tests, with independent half-bit/native-AWQ coordinate and group
oracles, exact boundary remainders, strict schema/order/census and deterministic
reconstruction. Earlier diagnostic checks, native/prefix/admission/reference
and local operators and evidence writers are forbidden. Independent Host
Reviewer validation is required.

Command:
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_final_head_l23_mlp_down_projection_input_coordinates_v1 --check`.

## Read-only final-head L23 residual-branch deltas

`diagnose_q24_s16_final_head_l23_residual_branch_delta_v1 --check` authenticates
reviewed final-head attempt001 and the original-input L23 FP16 archive. Nine
controls, two retained final-head reference branches and their eight existing
largest-absolute input-channel hotspots give 144 logical residual accounts
and 576 exact actual-minus-reference boundary deltas. Both hotspot selections
use the original-input **FP16 L23** trajectory; binary64 internal stages are
unavailable and are neither substituted nor reconstructed.

At each coordinate, the retained stage18 hidden delta equals the sum of
input_hidden, stage11 attention-output and stage17 MLP-down-output deltas,
plus the actual-minus-reference boundary remainder. Actual Q24 input carry,
attention/down state-update remainders and final FP16 conversion are reported
separately; the reference remainder is split at its retained stage12 residual
boundary. Exact rational arithmetic preserves signs and cancellation. These
are local accounting identities, not propagation through final RMSNorm,
causal shares of final-head error, or a rounding-only attribution.

The command compiles its two files in memory and runs only its focused tests;
stdout contains one JSON document. No native, prefix, admission, reference or
local operator, earlier check, or evidence writer is dispatched. Retained
L21/L22/L23 failures, exact thresholds, original global references and
source/operand/state/KV/lineage gates remain unchanged. Q24 state remains
wider than FP16; native S16 RTZ, INT4 weights and FP16 operator/KV boundaries
are unchanged. This diagnostic grants no admission, strict-FP16-state W4A16,
new-token or full-model capability.

Command:
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_final_head_l23_residual_branch_delta_v1 --check`.

## Read-only final-head attention V projection unselected stage00 complement

`diagnose_q24_s16_final_head_attention_value_projection_unselected_stage00_complement_v1 --check`
extends the retained V projection-input diagnostic without changing its selected
entries or running earlier diagnostic check commands. Each of the 1152 retained
accounts excludes the same eight ranked stage00 coordinates and partitions the
remaining 888 coordinates into seven contiguous G128 groups in input order:
1022976 logical unselected terms and 8064 complement groups, including repeated
selections across hotspots and separately referenced final-head branches.

Each `unselected_stage00_inputs` account retains the excluded coordinate list,
group ranges, unselected/excluded counts, exact signed contributions and sums of
absolute coordinate contributions. Group signed totals close the existing
`unranked_signed_remainder`; group absolute totals close the full absolute
contribution sum minus the selected absolute terms. These are exact rational
accounts, not absolute values of net group sums: cancellation remains explicit.
Selected signed sum plus unselected signed sum plus the unchanged projection
boundary remainder equals the retained V delta. Multiplication by the original
value-component factor preserves the retained weighted value identity.

The complete original report remains present apart from additive complement
fields. Native AWQ G128 asymmetric INT4 semantics, no qzero plus-one, FP16
scales, authenticated stage00 inputs, stage03/stage07/output-cache lineage and
empty P0 input KV remain unchanged. The attention reference is the original-input
FP16 trajectory, not a reconstructed binary64 attention trajectory; final-head
FP16/binary64 branches retain their separate independent global references,
thresholds, failures and source/operand/state/KV/lineage gates.

This is post-RMSNorm local accounting, not raw Q24 propagation, bridge selection,
dominance, a token-margin or ranking change, or an executed counterfactual.
Q24 residual state remains wider than FP16, with INT4 weights and FP16 operator
boundaries/KV unchanged. No prefix/admission/native/operator/reference replay,
evidence write, hardware/RTL/GPU/ACE2 work, precision/scale expansion,
strict-FP16-state W4A16, new-token or full-model admission is authorized or implied.

From the repository root, the stdout-only command is
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_final_head_attention_value_projection_unselected_stage00_complement_v1 --check`.
It compiles the two new Python files in memory, runs 28 focused tests (including
the inherited 18 V-input regressions), and emits exactly one schema-checked JSON
document. Independent half-bit/native-lane integer oracles check every retained
complement group and total; synthetic cancellation, selection/closure tampering,
retained identities, source/lineage and forbidden-dispatch/write guards remain
covered. Normal independent Host Reviewer validation is required.

## Read-only final-head attention V projection-input coordinates

`diagnose_q24_s16_final_head_attention_value_projection_input_coordinates_v1 --check`
extends the retained source/value attribution without running any earlier check.
For nine controls, two independently referenced final-head branches, eight
selected final-head channels per branch and eight selected value components
per channel, it accounts for 1,152 V projection deltas. Each account ranks all
896 exact `(stage00_actual-stage00_reference)*W_v` terms, reporting the eight
largest coordinates and all seven contiguous G128 groups. Coordinate ordering
is absolute signed contribution descending, then coordinate ascending; group
ordering is absolute net sum descending, then group index ascending. Signed
and absolute sums expose cancellation, and the unranked remainder retains
the complete sum. The census includes 1,032,192 input terms, 9,216 selected
coordinates and 8,064 groups, including repeated accounts across hotspots.

All four L23 `v_proj` weight/zero/scale/bias tensors are authenticated against
the original-reference canonical bindings. Native GEMM nibble ordering,
asymmetric INT4 without qzero plus-one, and FP16 scales/biases remain unchanged;
shared biases cancel. Authenticated stage00 FP16 inputs are paired with the
same retained attention archives, with stage03/stage07 V equality, empty P0
input KV and actual output-cache lineage enforced. The residual is an FP16
post-RMSNorm input difference, not propagation of raw Q24 state through RMSNorm.
Retained V delta minus the exact input sum is a separate projection boundary
remainder, not assigned solely to rounding. The original-reference probability
times the GQA `o_proj` weight sum links both terms to the selected retained
value component. This is a local identity, not a causal final-head decomposition.

The entire prior source/value report remains embedded, preserving the distinct
original-input FP16/binary64 global references, unavailable binary64 attention
trajectory, exact thresholds, all failures, and source/operand/state/KV/lineage
gates. Q24 state remains wider than FP16; INT4 weights and FP16 operator/KV
boundaries are unchanged. No native/prefix/admission/reference/operator replay,
evidence writes, hardware/simulation/GPU or ACE2 work, precision/scale expansion,
strict-FP16-state W4A16, new-token or full-model admission is established.

The stdout-only command is
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_final_head_attention_value_projection_input_coordinates_v1 --check`.
It compiles the two new files in memory and runs only the new focused tests,
including independent half-bit/AWQ integer oracles, exact coordinate/group
sums, projection/value identities, deterministic reconstruction, schema/census,
lineage rejection and forbidden dispatch/write audits. Normal independent
Host Reviewer validation is required for this version.

## Read-only final-head Q/K projection unselected stage00 complement

`diagnose_q24_s16_final_head_qk_projection_unselected_stage00_complement_v1 --check`
extends the Q/K projection-input diagnostic with additive complement accounts.
All 1152 retained query/key accounts keep their eight selected stage00 entries
and full-input group rankings unchanged. Each new `unselected_stage00_inputs`
account partitions the remaining 888 coordinates into seven contiguous G128
groups in input order: 1022976 logical unselected terms and 8064 complement
groups across nine controls, 72 score hotspots and 576 selected dimensions.
Repeated Q/K selections remain separate logical accounts.

Each group reports its range, unselected/excluded counts, exact signed sum and
sum of absolute coordinate contributions. The group accounts subtract selected
signed and absolute terms from their corresponding authenticated full-input
accounts; independent half-bit/native-AWQ-lane integer oracles directly sum the
complement coordinates. Absolute totals are not absolute net sums: cancellation
remains visible. Group totals close `unranked_signed_remainder` and the original
full absolute sum minus selected absolute terms exactly.

Selected plus unselected plus the unchanged projection boundary equals the
retained projection delta. Group and total complement contributions also retain
both reference-partner and actual-partner QK/8 factors. Both Q/K operand-swap
identities and their unallocated interaction remain unchanged. The full prior
report, original-input FP16 attention reference, independently propagated
FP16/binary64 global references, exact thresholds, historical failures and
source/operand/state/KV/lineage gates remain intact. No binary64 internal
attention trajectory is reconstructed.

This is local post-RMSNorm accounting, not raw Q24 propagation, bridge selection,
dominance, a margin-change reversal, token ranking change or an executed
counterfactual. Q24 residual state remains wider than FP16; native asymmetric
G128 INT4 weights, no qzero plus-one, FP16 scales and FP16 operator boundaries/KV
are unchanged. No prefix/admission/native/operator/reference replay, evidence
write, hardware/RTL/GPU/ACE2 work, precision/scale expansion, strict-FP16-state
W4A16, new-token or full-model admission is authorized or implied.

From the repository root, the stdout-only command is
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_final_head_qk_projection_unselected_stage00_complement_v1 --check`.
It compiles the two new Python files in memory, runs 28 focused tests including
the inherited 16 Q/K input regressions, and emits exactly one schema-checked JSON
document. Complement/selected tampering, exact signed/absolute closure, operand
swaps, cancellation and zero ties, source/KV lineage, CLI and forbidden
dispatch/write guards are covered. Normal independent Host Reviewer validation
is required for this version.

## Read-only final-head Q/K projection-input coordinates

`diagnose_q24_s16_final_head_qk_projection_input_coordinates_v1 --check`
extends the retained Q/K logit attribution without running its earlier checks.
For each of its 576 selected dot-product dimensions, both query and key
projection accounts rank the 896 exact `(stage00_actual-stage00_reference)*W`
terms by absolute signed contribution, breaking ties by input coordinate.
The eight largest coordinates and all seven contiguous G128 input-group
sums are reported; groups sort by absolute net sum, then group index. Absolute
sums expose cancellation, and an explicit unranked remainder preserves the
complete signed sum.

The input residual here means an actual-minus-original-reference difference
at the FP16 post-RMSNorm projection input, not propagation of raw Q24 residual
state through nonlinear RMSNorm. All eight Q/K weight, zero, scale and bias
tensors are authenticated against the retained original-reference canonical
bindings. Native GEMM nibble ordering and no qzero plus-one are preserved;
shared biases cancel. Retained projection delta minus the exact input sum is
reported separately as a boundary remainder, not assigned solely to rounding.
Reference-partner and actual-partner QK/8 factors close both retained operand
swap orders without assigning their interaction to one projection.

The census is nine controls, 72 score hotspots, 576 selected dimensions,
1,152 projection accounts, 1,032,192 input terms, 9,216 selected coordinates,
and 8,064 input groups. These counts include repeated accounts where a KV
coordinate serves multiple query heads. The complete prior report preserves
original-input global FP16/binary64 references, exact thresholds, historical
failures and source/operand/state/KV/lineage gates. This is local accounting,
not final-head causality or runtime profiling; singleton probabilities remain
one. Q24 state remains wider than FP16, with INT4 weights and unchanged FP16
operator/KV boundaries. There is no native/prefix/admission replay, evidence
write, hardware/ACE2 work, precision/scale expansion or new admission.

The stdout-only command is
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_final_head_qk_projection_input_coordinates_v1 --check`.
It compiles the two new Python files in memory, runs only its focused
independent bit/integer-oracle tests, and checks JSON schema, census,
deterministic ordering and dispatch/write guards. Normal independent Host
Reviewer validation remains required for this new version.

## Read-only final-head attention Q/K logit attribution

`diagnose_q24_s16_final_head_attention_qk_logit_attribution_v1 --check` extends
the retained score-versus-value diagnostic without running any earlier check.
It selects eight of the 14 query heads per control by absolute retained
actual-minus-original-input-FP16 score delta, breaking ties by head index.
Each selected head has only source position 0/token 9707. All probabilities
remain one, so there is no nonzero probability hotspot or multi-token ranking.
The full prior score/value report and its independent original-input
FP16/binary64 final-head references remain unchanged.

For each selected head, authenticated FP16 `stage04` Q and `stage06` K give
64 exact scaled terms:
`(Q_actual-Q_ref)*K_ref/8`, `Q_ref*(K_actual-K_ref)/8`, and their interaction
`(Q_actual-Q_ref)*(K_actual-K_ref)/8`. Both operand swap orders are reported.
The retained score minus the exact scaled dot has a separate boundary
remainder on each trajectory; their difference closes the retained score
delta without calling a score operator or assigning the remainder solely
to rounding. Components are ranked by absolute signed sum, then dimension;
the report includes all 64, the top eight, and both contiguous 32-dimension
group sums. P0 Q/RoPE and K/RoPE/cache equalities and GQA head mapping are
checked, not recomputed.

The fixed census is nine controls, 126 inherited score pairs, 72 selected
head/source accounts, 4,608 dimension accounts, 576 selected dimensions,
and 144 groups. These are local product identities, not causal shares,
upstream projection provenance, probability changes, propagation to
final-head error, or runtime profiling. Q24 residual state remains wider
than FP16; INT4 weights, native S16 RTZ and FP16 operator/KV boundaries,
source/lineage gates, exact thresholds, historical failures and non-admission
flags are preserved. No native/prefix/admission/reference replay, evidence
write, hardware, ACE2, precision/scale expansion or new-token/full-model
admission is authorized.

The command is
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_final_head_attention_qk_logit_attribution_v1 --check`.
It compiles the two new Python files in memory, runs only its focused tests,
validates strict JSON schema/counts and deterministic ordering, and emits
JSON to stdout under inherited dispatch/write guards. This version requires
normal independent Host Reviewer validation.

## Read-only final-head attention score versus value contrasts

`diagnose_q24_s16_final_head_attention_score_vs_value_v1 --check` extends the
retained source/value attribution with 126 actual/reference L23 P0 score
comparisons (nine controls, 14 query heads, one source token 9707). It reads
authenticated `stage08` scores and `stage09` probabilities; it does not
recompute Q/K, scores, softmax or attention. Score differences are ranked by
absolute magnitude, then ascending query-head index. Singleton softmax has
unit probability for every finite score, so score changes cannot change
probability weighting at this retained boundary.

For the retained final-head attempt001 lineage, eight controls differ from
the original-input FP16 scores in 13 of 14 query heads; `mapped_all` differs
in all 14. All 126 actual/reference probability pairs remain exactly one.
Both fixed-value probability effects and the interaction are exactly zero
in all 144 source accounts. The exact pre-boundary attention-product delta
is therefore value-content-only at these selected coordinates; the separate
AV/projection remainders and downstream final-head error are not assigned
to value content by this observation.

For each of the 144 existing final-head channel hotspots, four exact local
weighted products hold either probability or value content fixed:
`P_ref*V_ref*W`, `P_actual*V_ref*W`, `P_ref*V_actual*W`, and
`P_actual*V_actual*W`. Both swap orders and their interaction are reported,
not allocated as causal percentages. The only source-token account sums all
128 GQA value components; its eight selected components retain the previous
absolute-contribution ranking and coordinate tie breaks (1,152 selected
components total). Explicit retained AV and `o_proj` boundary remainders
still close the local difference. This is exact product accounting, not
counterfactual operator execution, multi-token ranking or propagation of
these effects through residual/MLP/final RMSNorm to final-head errors.

The previous attribution report is preserved in the new JSON, including
independent original-input final-head FP16/binary64 references, the absence
of retained binary64 attention stages, all failures, exact thresholds and
source/operand/state/KV/lineage gates. Q24 residual state is wider than FP16;
native S16 RTZ, G128 asymmetric INT4 GEMM ordering, no qzero plus-one, FP16
scales/operator boundaries/KV and non-admission status are unchanged.
There is no native/prefix/admission/reference replay, evidence mutation,
hardware/ACE2 work, precision/scale expansion or new-token/full-model claim.

The command is
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_final_head_attention_score_vs_value_v1 --check`.
It compiles the two new Python files in memory, runs only its focused tests,
validates the strict JSON schema and emits JSON to stdout. Earlier checks
are forbidden; existing source/value measurement and authentication helpers
are reused. Independent Reviewer validation is required for this version.

## Read-only final-head attention source/value attribution

`diagnose_q24_s16_final_head_attention_source_value_attribution_v1 --check`
maps the eight largest absolute tied-head input-channel contributions for
each of nine retained controls and both original-input final-head reference
branches to the same output coordinates of L23 attention `o_proj`. These
are 144 logical hotspot accounts, each with 896 query-head/value products
aggregated into 128 KV-value components; repeated coordinates reuse the same
exact local account. Source position 0/token 9707 is the only source:
P0 has empty prior KV and unit probabilities, with 14 query heads sharing two
64-channel KV heads in groups of seven. This diagnostic cannot rank multiple
source tokens or establish a probability-driven error at this boundary.

The attention contrast is explicitly **actual versus original-input FP16**.
Original binary64 attention stages were not retained; the binary64 final
hidden and final-head references remain independent, unchanged, and are not
replaced by FP16 attention data. There is no binary64 stage reconstruction.
For each selected `o_proj` coordinate, exact signed components are
`delta(P)*V_ref*W`, `P_ref*delta(V)*W`, and `delta(P)*delta(V)*W`.
Native G128 asymmetric packed INT4 weights use GEMM nibble ordering, no
qzero plus-one, and the authenticated FP16 scales. Signed GQA contributions
are summed before ranking by magnitude, with ascending value-coordinate
tie breaks. Full 128-component rankings and source-token totals are retained.
Explicit AV and projection boundary remainders close the retained local
`o_proj` difference; the projection remainder is not presumed solely rounding.

This is local upstream coordinate accounting, **not** propagation through
residual/MLP/final RMSNorm and not a causal decomposition of final logit
error. Final-head channel products, both global references, nine-control
failure histories, exact thresholds, and source/operand/state/KV/lineage
gates are preserved. Q24 residual state remains wider than FP16; native
S16 RTZ, INT4 weights, and FP16 operator/KV boundaries are unchanged.

The disclosed command is
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_final_head_attention_source_value_attribution_v1 --check`.
It compiles the new module and focused tests in memory, executes only those
tests, validates the strict JSON schema, and emits JSON to stdout.
Dispatch/write guards forbid native, original-prefix, admission, reference,
attention, projection, head, RTL/GPU/hardware/simulation execution and
evidence mutation. No new-token, strict-FP16-state W4A16 or full-model
admission follows. Normal independent Reviewer validation is required for
this new diagnostic.

## Read-only final-head attention source/value unselected-component complement

`diagnose_q24_s16_final_head_attention_source_value_unselected_component_complement_v1 --check`
extends the source/value attribution above using its full exact rankings, without
replaying an earlier check or any attention/projection/head/reference producer.
All 144 logical hotspots retain their selected eight components and add the
120-component complement in ascending value-coordinate order: 1152 selected and
17280 unselected components, with 120960 unselected query-head/value products.
Repeated selections count separately. No MLP or RMSNorm analysis is extended.

For selected, unselected and full sets, the report gives exact rational net signed
totals and sums of absolute net component contributions, plus separate signed and
absolute probability/value/interaction totals. The absolute component account is
**after** each component's seven-query-head GQA aggregation; it is not the sum of
absolute individual query-head products. Source position 0/token 9707 has its
own selected/unselected/full totals. Unit P0 probabilities make probability and
interaction terms and the AV boundary remainder zero in this retained scope.

Selected plus unselected signed accounts equal the unchanged `exact_attention_sum`.
Adding the unchanged `av_boundary_remainder` and
`o_projection_boundary_remainder` equals the unchanged
`retained_o_projection_delta`. Absolute selected plus unselected accounts equal
the full absolute account, without claiming absence of cancellation. The
projection remainder is not presumed to be solely rounding. Full rankings,
source/value identities, final-head rows, both independently propagated original
global references, thresholds, historical failures and all source/operand/state/
KV/lineage gates remain intact. Attention uses only original-input FP16 data;
binary64 attention internals are neither reconstructed nor substituted.

The disclosed command is
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_final_head_attention_source_value_unselected_component_complement_v1 --check`.
It compiles only the new source and focused tests in memory, runs the focused
independent scalar/bit-oracle and mutation tests, validates the strict schema,
and emits exactly one JSON document to stdout, with no evidence files.
Dispatch/write guards retain the zero-native/prefix/admission/reference/operator/
RTL/GPU/hardware boundary. This accounting does not select a bridge, establish a
dominant cause, execute a counterfactual, change rankings or confer admission.
Q24 state is wider than FP16; native S16 RTZ, packed INT4 weights and FP16
operator/KV boundaries are unchanged. Normal independent Reviewer validation
is required.

## Read-only final-head output-projection input-channel hotspots

`diagnose_q24_s16_final_head_output_projection_channel_hotspots_v1 --check`
analyzes the final tied FP16 `lm_head` output projection, not an attention
`o_proj` or a new suffix execution. For each of the nine retained final-head
attempt001 controls and each independently propagated original-input FP16
and binary64 reference, it selects the largest exact absolute retained logit
residual, breaking ties by ascending numeric row ID. This is a distinct
individual-row residual account, not a repetition of pair-margin geometry.

Each of the 18 row/reference accounts ranks all 896 input columns by
`(actual RMSNorm[i] - reference RMSNorm[i]) * W[row,i]`, with deterministic
channel-index tie breaking. Top-eight absolute, positive and negative
contributions retain their signs; coordinate 62 is always reported with
its rank. Exact sums and unranked remainders are explicit. The retained logit
residual minus the coordinate sum is a boundary remainder, not an attribution
solely to rounding or a claim that input error explains the whole residual.
Signed column accounting is not a nonnegative causal share or upstream cause.

For these retained arrays, the selected row is numeric ID 48298 against FP16
and 20806 against binary64 for every control. Channel 62 ranks first by
absolute contribution in all 18 accounts. FP16 top-eight channels are
`[62, 241, 190, 827, 53, 485, 208, 877]` for every control. Binary64 top-eight
channels are `[62, 190, 53, 208, 221, 734, 493, 827]` except for `mapped_all`,
which gives `[62, 190, 53, 221, 208, 734, 827, 292]`. These are descriptive
rankings for the retained attempt001/original-input attempt003 identities,
not admission evidence or a certification of upstream repairs.

The command
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_final_head_output_projection_channel_hotspots_v1 --check`
authenticates the existing reviewed artifacts, source and operand bindings,
original references and failure histories. It compiles the two new Python
files in memory, runs only its focused tests and validates its JSON schema.
Independent bit-level FP16 and scalar rational oracles check row selection,
channel products and ordering. JSON is emitted only to stdout.

There is no row-dot/head/RMSNorm/native/prefix/admission/reference replay,
full-vocabulary output recomputation, evidence write, token selection,
hardware/RTL/GPU dispatch or ACE2 change. All thresholds, source/operand/
state/KV/lineage gates and L21/L22/L23 failures remain unchanged. Old sparse-cut
closure cannot certify these nine-control parents. Q24 state remains wider
than FP16; native S16 RTZ, INT4 weights and FP16 operator/KV boundaries are
unchanged. No strict-FP16-state W4A16, new-token, full-model or admission
claim follows. The new diagnostic requires normal independent Host review.

## Read-only final-head output-projection unselected input-channel complement

`diagnose_q24_s16_final_head_output_projection_unselected_channel_complement_v1 --check`
extends the individual-row hotspot diagnostic above without changing its nine
controls, separate original-input FP16/binary64 references, selected numeric
rows, top-eight channels, complete rankings, coordinate-62 accounts or
head-boundary remainders. It accounts only for the 888 channels outside each
account's eight largest-absolute contributions; positive/negative rankings
do not define additional exclusions.

Each of the 18 row/reference accounts reports seven contiguous G128 groups
in ascending input order, including groups with zero net contribution.
Group coordinate counts include zero terms and exclude only selected channels.
Exact rational signed sums and sums of absolute individual contributions are
reported separately: cancellation is retained, not replaced by the absolute
net group sum. This gives 15984 logical unselected terms and 126 group accounts.
Selected plus unselected equals the full signed channel account; adding the
unchanged boundary remainder closes the retained signed logit residual.
Selected and unselected absolute sums close the full absolute channel account.
These are individual-row identities, not pair-margin bridge selection, a
dominant-cause claim, a token-ranking reversal or an executed counterfactual.

For the retained attempt001/original-input attempt003 arrays, FP16 group
counts are `[126, 125, 128, 127, 128, 128, 126]` for every control. Binary64
counts are `[126, 125, 128, 127, 128, 127, 127]` except for `mapped_all`,
which has `[126, 125, 127, 128, 128, 127, 127]`. Each list sums to 888.
The exact complement totals are:

| Reference / controls | Signed complement | Sum of absolute unselected contributions |
|---|---|---|
| FP16 / other eight controls | `467916873/536870912` | `1904735939/536870912` |
| FP16 / mapped_all | `14729134155/17179869184` | `60444714315/17179869184` |
| binary64 / other eight controls | `6745351574995038887751/75557863725914323419136` | `30477648239690165078729/75557863725914323419136` |
| binary64 / mapped_all | `6348952351451827609415/75557863725914323419136` | `29618817910214260391881/75557863725914323419136` |

The stdout-only command is
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_final_head_output_projection_unselected_channel_complement_v1 --check`.
It compiles the new source and focused tests in memory, checks complement
accounts against independent FP16-bit/binary64-rational product oracles, and
emits exactly one schema-validated JSON document with the unchanged inherited
accounts, all per-group signed/absolute values, exact totals and dispatch audit.
It does not run earlier diagnostics' checks or write evidence.

Original source/operand/state/KV/lineage and terminal-review gates, exact
thresholds and historical L21/L22/L23 failures are unchanged. There is no
RMSNorm/head/row-dot/native/prefix/admission/reference replay, full-vocabulary
output recomputation, hardware/RTL/GPU dispatch or ACE2 change. Q24 state
remains wider than FP16; INT4 weights and FP16 operator boundaries/KV remain
unchanged. This bounded accounting does not establish strict-FP16-state
W4A16, new-token or full-model admission and requires independent Host review.

## Read-only final-head tied-row difference hotspots

`diagnose_q24_s16_final_head_row_difference_hotspots_v1 --check` authenticates
reviewed final-head attempt001, original-input final attempt003 authority,
tokenizer/tied-head identities, retained execution source/test pins and pinned
cutoff/contribution/RMSNorm-delta helpers. It preserves all nine controls'
L21/L22/L23 failures, exact thresholds and source/operand/state/KV/lineage
gates. Only numeric pairs already exposed by the cutoff, exchanged-pair and
coordinate-contribution definitions are analyzed.

For each selected left/right pair, all 896 exact signed
`W[left,i] - W[right,i]` differences are ranked by absolute magnitude, with
ascending coordinate tie breaking. Top-eight absolute, positive and negative
summaries include exact operands, magnitudes, sums and remainders. Coordinate
62 is always force-reported with its 1-based absolute rank, even outside the
top eight, as are all signed/absolute RMSNorm-delta and contribution hotspots
needed to interpret that pair.

Row-hotspot overlaps are compared with retained actual-minus-independent-
reference RMSNorm deltas and exact branch/change coordinate contributions.
Each forced coordinate reports the exact multiplier and product. Expanded
scalar identities independently check subtraction and distributive
contribution relationships without evaluating row-dot or head operators.
Independent bit-level FP16 and integer-ratio scalar test oracles check the
rankings and products. Sign reversals, zero-multiplier erasures and rank
agreement/disagreement are explicit: geometry alone need not reproduce
contribution ordering; even coincident rankings do not establish causation.
Retained margin remainders are not attributed solely to rounding.

The disclosed command is
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_final_head_row_difference_hotspots_v1 --check`.
It compiles both new Python files in memory, runs only focused tests and
emits one JSON document on stdout; test progress uses stderr. No evidence
writes, prior diagnostic checks, RMSNorm/head/native/decoder/prefix/admission/
reference replay, full-vocabulary output recomputation or token decode/
publication/selection is allowed. Old sparse-cut closure remains a separate
lineage and cannot certify, substitute for or propagate the new parents.
Q24 state remains wider than FP16; INT4 weights, native S16 RTZ and FP16
operator/KV boundaries are unchanged. No upstream root-cause, admission,
strict-FP16-state W4A16, new-token or full-model claim follows.
Normal independent Reviewer validation is REQUIRED.

## Read-only final-head RMSNorm delta hotspots

`diagnose_q24_s16_final_head_rmsnorm_delta_hotspots_v1 --check` authenticates
reviewed final-head attempt001, original-input final attempt003 reference
authority, original execution source/test identities, live pinned helpers,
tokenizer bytes and tied-head/embedding identities. All nine L21/L22/L23
failure histories, exact thresholds, source/operand/state/KV/lineage gates
and non-admission flags remain intact.

For each control it ranks all 896 exact signed actual-minus-reference
RMSNorm deltas separately against independent FP16 and binary64 trajectories.
Top-eight absolute, positive and negative summaries include exact sums and
unranked remainders; equal magnitudes use ascending coordinate index.
Coordinate 62 is always reported with its exact operands, signed delta and
1-based absolute rank, even when outside the top eight. Every reported signed
or absolute hotspot of the existing cutoff/exchanged-pair coordinate-product
definition is also force-reported, with its diagnostic pair and reason.

Only exact coordinate products against selected tied-head row differences
are evaluated for alignment; no row-dot, RMSNorm or head operator is rerun.
Per-pair overlap distinguishes identical, partial and disjoint hotspot sets.
All 36 control pairs and each control's two independent reference branches
are compared. Byte-identical RMSNorm and joint RMSNorm/logit groups require
byte equality and matching delta rankings without merging their histories.
Retained pair-margin change minus the coordinate-product sum is a boundary
remainder, not an attribution solely to rounding or an upstream cause.

For these retained arrays, eight controls are byte-identical and `mapped_all`
forms the other RMSNorm and joint-output group. Coordinate 62 has absolute
delta rank 1 in both reference branches for every control. Its signed deltas
are `-53/8` against FP16 and `-75112679630269/70368744177664` against binary64
in the eight-control group; `mapped_all` gives `-13/2` and
`-66316586608061/70368744177664`, respectively. Every control's FP16 top-eight
delta coordinates are `[62, 241, 190, 53, 674, 827, 493, 221]`. The binary64
lists are `[62, 241, 190, 493, 53, 221, 674, 208]` for the eight-control group
and `[62, 190, 241, 221, 53, 674, 493, 439]` for `mapped_all`.
Across each control's three selected diagnostic pairs, hidden-delta top-eight
sets share only four or five coordinates with FP16 pair-delta contribution
top-eight sets, and three or four with binary64 sets. Hidden-vector and
selected-pair contribution hotspots therefore overlap only partially;
largest hidden deltas alone do not determine the retained cutoff geometry.

The disclosed command is
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_final_head_rmsnorm_delta_hotspots_v1 --check`.
It compiles both new Python files in memory, runs only its focused tests,
and emits one JSON document on stdout with test progress on stderr.
Prior diagnostic checks, row-dot/head/RMSNorm/native/decoder/prefix/admission/
reference replay, full-vocabulary head recomputation and evidence writes are
forbidden. Numeric IDs are diagnostic only: no token decode, publication or
selection occurs. The old sparse-cut adjusted closure remains separate and
cannot certify, substitute for or propagate the new nine-control parents.
Q24 state is wider than FP16; native S16 RTZ, INT4 weights and FP16
operator/KV boundaries remain unchanged. No upstream root-cause, admission,
strict-FP16-state W4A16, new-token or full-model claim follows.
Normal independent Reviewer validation is REQUIRED.

## Read-only final-head coordinate contributions

`diagnose_q24_s16_final_head_coordinate_contributions_v1 --check` authenticates
the reviewed final-head attempt001, final attempt003 reference authority,
retained original source/test identities, live validator/helper pins, tokenizer
bytes and tied-head/embedding identities. It preserves all nine L21/L22/L23
failure histories and their exact thresholds, independent original-input
references, source/operand/state/KV/lineage gates and non-admission flags.

For exchanged numeric diagnostic IDs, each trajectory's cutoff-neighbor pair,
and differing IDs' opposite-membership neighbors, it computes
`h[i] * (W[left,i] - W[right,i])` exactly under retained actual, independent FP16
and independent binary64 RMSNorm vectors. It ranks eight largest absolute
signed contributions, positive contributions and negative contributions, with
coordinate-index tie breaking, exact sums and unranked rational remainders.
Independent integer-ratio row dots verify each contribution sum; actual local
row dots must reproduce retained logits after the existing exact Q48-to-FP16
boundary. This is selected-row CPU arithmetic, not a full-vocabulary head run.

Actual-minus-reference coordinate changes plus the difference of retained-head
remainders exactly reconstruct the retained preference-margin change.
Partial-coordinate margins are additive accounting, not executed vector
splices. The remainder includes accumulation and boundary conversion effects;
it is not attributed solely to rounding. Binary64 and FP16 reference vectors
remain separate independent trajectories. Signed contributions do not imply
nonnegative causal shares or identify an upstream root cause.

The disclosed command is
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_final_head_coordinate_contributions_v1 --check`.
It compiles the source and focused tests in memory, runs only those tests,
and emits one JSON document to stdout with test progress on stderr. Guards
prohibit native/decoder/prefix/admission/reference-producer/RTL/GPU/hardware
dispatch and evidence writes. Local row computations are counted separately.
The old sparse-cut adjusted closure cannot certify, substitute for, or propagate
the new nine-control parents. No token is decoded, published or selected;
Q24 state remains wider than FP16 with unchanged INT4 weights and FP16
operator/KV boundaries. No strict-FP16-state W4A16, new-token, full-model or
admission claim follows. Normal independent Reviewer validation is REQUIRED.

## Retained mixed-RMSNorm conversion top-k stability

The actual retained-only check returns **`UNKNOWN` before classification** at
the byte binding for
`ace3/model/candidates/q24_s16_final_head_from_l23_coordinate62_suffix_execution_v1.py`.
The authenticated retained document carries two different source versions at
that same path: `original_execution_sources` requires 22,121 bytes with SHA-256
`5a3a3e3700afef82b149c8cff60e69b0bf0c9ba0784ad35501596fa1f44adb2a`,
whereas `diagnostic_sources` requires 23,207 bytes with SHA-256
`b406b5f3bc89eebadc89156c3a09ad5b7d3a25d3bf1da55f3c81186835e915e1`.
The live source matches the diagnostic version, not the original-execution
version. The classifier checks both groups against their live paths; one
file cannot satisfy these two byte-exact pins. Neither pin nor source was
changed to manufacture authentication.

Consequently, no actual branch/policy top-k or cutoff comparison was reached:
the hypothesis is neither supported nor rejected, and `lane_terminated`
remains false. The single authorized invocation is consumed; this concrete
source-version binding boundary does not authorize another invocation or
producer replay. Normal independent Reviewer validation remains required.

The launcher retains descriptor-target write confinement and uses an
explicit in-attempt pytest log file. Candidate, focused test and launcher
compilation succeeds, and all 27 focused tests pass with zero failures,
errors or skips. Launch, run, compilation, pytest and actual-check audits
record zero forbidden dispatches and zero outside-attempt evidence writes;
the diagnostic itself records zero filesystem-write events. Byte-exact
stdout, empty stderr, whole-command framing and command/environment/source/test
bindings are retained for the nonzero-exit `UNKNOWN`.

Earlier failures remain unchanged: the implementation-caused
`branch command/environment identity` `UNKNOWN`, the launcher's rejected
in-attempt atomic-write descriptor, and pytest's default `/dev/null` logging
write rejected before tests. The earlier pytest audit still records one
**prevented** outside-attempt write event, not zero. Those failures are not
scientific negative results and are not replaced by the passing focused tests.

`diagnose_q24_s16_final_head_mixed_rmsnorm_conversion_topk_stability_v1 --check`
implements the conversion-policy relevance test using only the authenticated byte-exact
mixed-RMSNorm capture's `check.stdout` JSON. It binds the outer launcher to
the inner run, checks command/environment/stream framing and source/test pins,
and preserves the original references, thresholds, nine failed control
histories and non-admission flags. It does not import or execute the producer,
RMSNorm, prefix, head matvec or full-vocabulary computation.
Inner command identity compares shell-tokenized argv and environment
assignments, independently of JSON object key ordering; the original command
and complete stream bytes still require their exact retained hashes.

For each of the three retained input branches, the contract requires all five policies to disclose
top-10 diagnostic IDs, exact cutoff gap/tie, IDs 319/34319 presence and ranks,
and the retained FP16/binary64 exchanged-pair accounts. An excluded rank is
derived only when the retained maximum cutoff-distance constraint uniquely
determines it; absence of a unique binding is `UNKNOWN`, not an invented rank.

The six same-matvec forward/reverse PyTorch-versus-direct comparisons test
membership, order and exchanged-pair/cutoff classification equality despite
nonzero conversion-word mismatches. Exact-RNE comparisons are separately
diagnostic, not a conversion-only causal contrast. Positive gap magnitudes
and full retained rational accounts are also compared and disclosed; a
positive-to-positive gap change alone is not a changed cutoff classification.
`SUPPORTED` or `REJECTED` terminates this conversion-policy relevance lane;
rejection includes the exact branch, policies, differing IDs/order and cutoff
accounts. Invalid or missing bindings yield nonzero-exit `UNKNOWN` with only
the binding fact and never authorize producer replay. Q24-wide state, INT4
weights and FP16 operator/KV boundaries remain unchanged. No policy adoption,
strict-FP16-state W4A16, new-token or full-model admission follows. Normal
independent Reviewer validation remains required.

## Read-only mixed-RMSNorm final-head counterfactual

`diagnose_q24_s16_final_head_mixed_rmsnorm_counterfactual_v1 --check`
authenticates the exact reviewed final-head attempt001 and original-input
final attempt003 reference authority, original execution source/test pins,
live reviewed validator and reference producer sources/tests, checkpoint,
tied head/embedding tensors and tokenizer bytes. All nine controls retain
their L21/L22/L23 failures, thresholds and source/operand/state/KV/lineage
gates. No RMSNorm or original prefix is recomputed.

Only retained actual and independent FP16 RMSNorm vectors enter the
counterfactual head. Byte-identical actual vectors share computation without
merging their control histories. The fixed exact head uses bounded Q48 sums
and one FP16 RNE. Controlled alternatives use CPU binary64 matvec with the
reviewed 2048-row chunking, forward or reversed coordinate order, and the
frozen PyTorch FP16 conversion. Direct NumPy FP16 conversions of those same
matvec results distinguish conversion effects from accumulation/order effects.
The retained binary64 RMSNorm/logits remain independent comparison anchors,
not inputs to regenerated reference trajectories.

The diagnostic requires all exact-head actual logits and all forward
PyTorch-head independent FP16 logits to reproduce the retained words. It
reports full-vocabulary top-10 numeric IDs, exact rational cutoff and
exchanged-pair margins, mixed-input comparisons, rounding/order mismatch
counts and exact additive pair decompositions. A retained pair's relative
change equals the RMSNorm-vector effect under a fixed exact head plus the
difference of the two head accumulation/rounding effects. Signed shares
are not nonnegative causal percentages. A pair reversal under a controlled
vector swap is local final-head evidence, not an upstream root-cause or
runtime-bottleneck claim. Ties and zero denominators remain explicit.

The mechanism is `SUPPORTED` only for exact reproduction of all retained
actual and independent FP16 logits and the complete ordered nine-control,
three-branch census with one exchanged pair per control, nine exact additive
closures and nine strict positive-to-negative exact-head vector-swap
reversals. An absent census, closure or reversal is `REJECTED`, as is a
computed logit/policy counterexample against an authenticated retained vector.
The latter reports the exact mismatch count and first differing word rather
than misclassifying a scientific negative as missing evidence. Malformed
pair orientations, flags contradicting rational margins, authority failures
and forbidden execution attempts terminate with nonzero-exit `UNKNOWN`,
without a fabricated zero-dispatch audit. A captured `SUPPORTED` result
terminates this exact-head vector-swap lane; `REJECTED` terminates the
mixed-RMSNorm mechanism. `UNKNOWN` records only the concrete unavailable or
invalid fact, preserves non-admission flags and does not authorize producer
replay.

On this retained surface, all 151,936 actual logits per control and all
151,936 independent FP16 forward-head logits reproduce their retained words.
All nine pair accounts close and reverse under the fixed exact head,
supporting the bounded mixed-RMSNorm mechanism pending independent review.
Across the three unique input branches, forward and reverse unrounded CPU
matvecs equal the exact Q48 sums, with no order-induced FP16 word changes.
PyTorch versus direct NumPy conversion differs at 15 words for the shared
`frozen_inherited` branch, eight for `mapped_all`, and 12 for
`independent_fp16`, in either coordinate order. These are separate local
conversion effects, not new references, operator policies or upstream
attributions.

The task-native entry point is the focused test module's
`--launch build/mixed-rmsnorm-final-head-<fresh-attempt>` command, using the
isolated Python interpreter and `PYTHONPATH`. It exclusively reserves a fresh
ignored-build attempt, checks branch/account/interpreter and the same-scope
process census, and holds an in-scope stable lock across validation and capture.
Its fixed command budget is one `py_compile` of the two Python files, one
focused synthetic pytest target, and exactly one diagnostic `--check`.
Standalone tests do not recompute live model outputs: the 24 focused tests
inside `--check` reuse that invocation's measured evidence, with independent
Fraction/sorting oracles and explicit negative controls.

The launcher uses `diagnostic_capture_v1` for file-backed byte-exact stdout,
stderr, command/argv/environment, whole-command framing, source/interpreter
identity, exit/timeout status and distinct SHA-256 bindings, including an outer
launcher capture. Failed attempts are retained, never overwritten or promoted
to successful measurements. Successful test progress is part of the diagnostic
JSON instead of uncaptured stderr. Capture is not independent review and does
not relax model/service budgets or source/reference gates. Dispatch/write
guards prohibit native/decoder/prefix/admission/RTL/GPU/hardware and original
reference-producer execution, and prohibit evidence writes. Counterfactual
head computations are counted separately rather than represented as zero
computation. No tokens are decoded, published or selected. Q24 residual state
is wider than FP16; INT4 weights and FP16 operator/KV boundaries are unchanged.
No candidate/policy/successor, strict-FP16-state W4A16, new-token or full-model
admission follows. Normal independent Reviewer validation remains REQUIRED.

## Read-only final-head cutoff-margin diagnostic

`diagnose_q24_s16_final_head_cutoff_margin_v1 --check` authenticates the
reviewed final-head attempt001 result, original execution source/test
identities, live reviewed validator/test pins, all retained arrays, and
original-input global references through the existing pinned read-only
authenticator. It does not rerun the earlier census check or any operator.
All nine controls retain their L21/L22/L23 failures, exact thresholds,
source/operand/state/KV/lineage gates and non-admission flags.

For actual FP16 logits and both independent reference trajectories, the
diagnostic reports 1-based ranks, top-10 numeric diagnostic IDs, the lowest
included and highest excluded logits, and their exact rational cutoff gap.
Differing IDs include signed margins to both cutoff neighbors and to the
nearest opposite-membership neighbor. Ordering remains descending finite
value, then ascending numeric ID; zero gaps explicitly disclose ties.
No tokens are decoded, published or selected for feedback.

Each exchanged pair reports the exact identity: relative actual-minus-reference
value change equals the reference preference margin plus the reversed actual
preference margin. The finite near-boundary classification requires the
observed 10/10 versus 9/10 overlap, exchanged ranks 10 and 11 in both compared
vectors, and strictly positive preference margins. Nonadjacent exchanges and
ties receive separate classifications, not an invented numerical threshold.
This describes retained-logit order geometry, not an upstream cause or a claim
that the independent FP16 reference is obtained by casting binary64 logits.

All nine retained controls preserve 10/10 binary64 and 9/10 FP16 overlap;
none meets the stricter adjacent-in-both-vectors classification. For
`frozen_inherited` and `inherited_native`, numeric ID 34319 has actual,
binary64 and FP16 ranks 10, 10 and 11; ID 319 has ranks 15, 14 and 10.
The actual cutoff gap is `1/64`, the binary64 gap is
`8512889453605/281474976710656`, and the FP16-reference gap is `3/64`.
The FP16 reference favors ID 319 over 34319 by `3/64`; the actual logits
favor 34319 by `29/128`. Their relative value change is exactly
`35/128 = 3/64 + 29/128`. These are finite, non-tied rank reversals, but
not a simple rank-10/rank-11 exchange in both trajectories. No upstream
cause follows from these retained-array measurements.

The disclosed command is
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_final_head_cutoff_margin_v1 --check`.
It compiles both new Python files in memory and runs only its focused tests;
stdout is one JSON document and test progress goes to stderr. Dispatch and
filesystem-mutation guards prohibit RMSNorm/head/decoder/native/prefix/reference
replay and evidence writes. Normal independent Reviewer validation remains
required. Q24 state is wider than FP16; unchanged INT4 weights and FP16
operator/KV boundaries do not establish strict-FP16-state W4A16, new-token,
full-model or candidate admission.

## Read-only cross-control final-head impact census

`diagnose_q24_s16_final_head_impact_census_v1 --check` compares only retained
final-head attempt001 arrays. It authenticates the resolved round-2 Reviewer
terminal record and backlog completion, exact result and artifact hashes,
the original execution source/test pins, and the separately pinned reviewed
validator/test revision. Its nine controls and 18 arrays are not new model
executions. The original-input binary64/FP16 final reference manifests and
input pins remain separate from actual outputs.

The census groups byte-identical RMSNorm/logit artifacts and joint outputs,
reports per-control mismatch counts, exact rational maximum absolute errors
and worst indices against both references, and compares all 36 control pairs.
Top-10 overlap and exact order equality are distinct statistics; ordering is
descending finite value with ascending diagnostic ID on ties. FP16 mismatch
counts compare words (including signed zero); binary64 counts compare values.
No token is decoded, published or selected for feedback.

For these retained arrays, eight controls share identical RMSNorm and logit
artifacts; `mapped_all` forms the other class for both outputs. Against the
reviewed FP16 trajectory, RMSNorm mismatch counts range from 895 to 896 and
logit mismatch counts from 151036 to 151071. Binary64 worst-error indices are
62 for RMSNorm and 20806 for logits in every control. All nine controls have
10/10 binary64 top-10 overlap with identical order, but only 9/10 FP16
overlap and different order. These statistics do not erase any retained
L21/L22/L23 failure or introduce a final-head acceptance threshold.

The disclosed command is
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_final_head_impact_census_v1 --check`.
It compiles the diagnostic and focused tests in memory, runs those tests,
and emits one JSON document to stdout, with test progress on stderr.
Dispatch and filesystem-mutation guards prohibit operator replay and evidence
writes. Retained gate/state archives are hash-authenticated, not replayed;
operand and ancestral gate conclusions remain scoped retained facts.
All nine L23 failures and the L21/L22 failures, exact thresholds and
non-admission flags remain unchanged. Q24 state is wider than FP16, with
unchanged INT4 weights and FP16 operator/KV boundaries. Neither artifact
equivalence nor top-k stability establishes a cause, threshold PASS,
strict-FP16-state W4A16, new-token or full-model admission. This new census
requires normal independent Reviewer validation.

## Nearest-passable S18 final-margin bridge

`diagnose_q24_s16_l23_p0_nearest_passable_final_margin_bridge_v1 --check`
is stdout-only CPU-software accounting of the reviewed nearest-passable envelope,
its S18 reference-boundary parent and retained combined-operand baseline.
Run with the repository on `PYTHONPATH` and Python `-B`; no output directory
or evidence packet is created. Parent result/review pins and current
source/test, operand, reference, state/KV and lineage file hashes are checked.
No parent implementation is imported or executed.

The accounting basis uses **2^24 / retained root_q24**, separately for the
baseline and intervened suffix. These are exact ratios of already retained
integer RMSNorm details, not new binary64 inverse-norm captures, recovered
unrounded operands, or the mixed-factor classifier's scalar anchors.
For hidden values `h0,h1`, retained normalized values `y0,y1`, norm weight `g`,
selected head-row difference `v`, and accounting anchors `s0,s1`, each
coordinate closes exactly as:

```
v*(y1-y0) =
  v*g*(h1-h0)*s0
  + v*g*h0*(s1-s0)
  + v*g*(h1-h0)*(s1-s0)
  + v*((y1-g*h1*s1) - (y0-g*h0*s0))
```

The last term includes every difference between retained outputs and this
declared rational basis; it is not a rounding-only attribution. Individual
selected rows 13, 319 and 34319 separately retain exact dot products and
logit-minus-dot boundaries. Their differences close the ordered-pair margins
without rerunning RMSNorm, selected-head computation or an intervention.

The bounded check establishes 24,192 coordinate identities and 27 ordered-pair
closures across nine controls, with zero closure residuals. Each control retains
110 changed coordinates and 786 unchanged coordinates. The unchanged-coordinate
direct-hidden term is zero, but its normalized/head-weighted contribution need
not be zero: global scale and boundary terms remain visible. For the
`frozen_inherited` control, the following components sum to the retained margin
movement:

| Ordered pair | Changed direct-hidden | Unchanged-coordinate contribution | Changed scale/interaction/boundary remainder | Head-boundary delta | Total |
| --- | --- | --- | --- | --- | --- |
| 319 -> 34319 | -310417467/42327646208 | 1327913243/137438953472 | -26545194483999/2773984621887488 | -78521883/137438953472 | -1/128 |
| 34319 -> 319 | 310417467/42327646208 | -1327913243/137438953472 | 26545194483999/2773984621887488 | 78521883/137438953472 | 1/128 |
| 34319 -> 13 | -25256719765/84655292416 | -1451246359/137438953472 | 46258298278565/5547969243774976 | 507716247/137438953472 | -19/64 |

The original-input FP16 and independently propagated binary64 reference margins
are authenticated separately and never reanchored to either actual vector.
The existing excess budget remains exactly 1/8. Historical FAIL/UNKNOWN facts,
closed middle/outer intervention outcomes, Q24-wide residual state, native S16
RTZ, INT4 weights and FP16 operator/KV boundaries remain unchanged. The
in-process filesystem/external-dispatch audit and model-call guard observed
zero writes or forbidden dispatches. This arithmetic closure does not identify
a causal or performance bottleneck, rescue the original trajectory, or establish
strict-FP16-state W4A16, new-token or full-model admission. All three
non-admission markers remain false; final rescue remains `NOT_DEFINED`.
Normal independent Host Reviewer closure is still required.

## L23/P0 nearest-passable S18 envelope

`q24_s16_l23_p0_s18_nearest_passable_envelope_v1` consumes the reviewed
combined-operand S18 reference-boundary diagnostic, not a replay of the original
native trajectory. For each of the nine controls, only words failing the existing
binary64 excess-over-nearest-FP16 budget of 1/8 are replaced. Selection uses the
recorded inclusive finite-word interval, minimizing absolute signed change,
then exact FP16 value, then unsigned word. All equal-distance words are disclosed;
passing words, including signed zero, remain bit-identical.

The fresh `intervened_stage18` field feeds only the existing final RMSNorm/tied-head
diagnostic suffix. The original S18 words and Q24 integers/signed-zero tags remain
separate and unchanged, as do all earlier state, operands and FP16 KV. The new
vector is not certified as a native Q24 state projection. Per-coordinate acceptance
uses the unchanged independent scalar policy and original-input binary64 reference.
Both original-input final reference branches remain separate; paired margins use
the retained combined-operand final output as baseline. Final-head rescue remains
`NOT_DEFINED`: there is no final-head threshold or trajectory-admission conclusion.
This counterfactual does not validate unchanged native operators, strict-FP16-state
W4A16, new-token execution, or full-model admission.

The bounded nine-control execution changed 110 failing coordinates per control
and preserved all 786 passing coordinates. All 896 intervened words per control
satisfied the scalar gate: maximum excess was exactly 1/8 and minimum threshold
margin was zero. No selection ties occurred; the largest absolute word-value
change was 9/4. This is feasibility evidence for the explicit word intervention,
not rescue of the historical trajectory or a causal attribution.

All nine controls shared the following final diagnostic pair margins. The
retained baseline here is the combined-operand output; neither reference branch
is reanchored to it. These are diagnostic numeric pair preferences, not token
publication or final-head acceptance.

| Ordered pair | Retained actual | Intervened actual | Original FP16 reference | Original binary64 reference |
| --- | --- | --- | --- | --- |
| 319 -> 34319 | 3/64 | 5/128 | 3/64 | -105261404943879/562949953421312 |
| 34319 -> 319 | -3/64 | -5/128 | -3/64 | 105261404943879/562949953421312 |
| 34319 -> 13 | 7/64 | -3/16 | 7/64 | 8512889453605/281474976710656 |

## L23/P0 S18 exact binary64 reference-boundary diagnostic

This section records the bounded diagnostic and its measured reference-boundary
limitation, not an accepted capability or a successful admission run.

`diagnose_q24_s16_l23_p0_s18_binary64_reference_boundary_v1` is read-only
CPU-software accounting of the reviewed combined immediate-operand evidence.
It compares all 896 coordinates in each of nine controls, explicitly marking
the retained 110 binary64 failures and their immediate coordinate neighbors.
The independently decoded exact FP16 S12+S17 sum, Q24 integer/signed-zero
state, rational-oracle RNE word, retained substituted word, and separate
original-input FP16 and binary64 terminal references remain distinguishable.
No native S18 or earlier stage, final head, reference producer, or prior
counterfactual is executed.

The original gate is unchanged: absolute error minus the nearest-FP16
representation floor must be <= 1/8. Every coordinate reports exact error,
floor, excess, signed decomposition, threshold margin, all nearest reference
words, and the inclusive interval of passable finite FP16 words. The interval
uses ascending exact value then unsigned word order (including both signed
zeros); endpoint indices and count specify the complete set without omitting
small-magnitude words. Nearest passable words and their signed changes from
the actual output are hypothetical scalar comparisons, not executed changes.

Agreement of the actual output with the exact fixed-operand RNE result and
the FP16 reference, while that result fails the binary64 gate, identifies a
**fixed-FP16-operand reference-boundary incompatibility**, not a representational
impossibility: a nearest-binary64 FP16 word always has zero excess and passes.
This diagnosis neither reconstructs binary64 internal stages nor identifies
an upstream root cause. Local arithmetic/state or reference-boundary
discrepancies are reported explicitly with UNKNOWN, as are authentication,
source, operand, state/KV and lineage defects; none authorize replay.

For all nine retained combined-operand controls, all 896 coordinates agree
with the independent rational Q24 state, projected FP16 word and original-input
FP16 S18 reference. Each control retains 110 binary64 excess failures; all 110
unrounded fixed-operand sums also lie outside the unchanged binary64 error
radius (representation floor plus 1/8). No local S18 word/state discrepancy
is observed. Correct S18 rounding with these same operands therefore cannot
rescue the failures. This scoped result does not identify an upstream cause
or imply that other FP16 words cannot pass the original binary64 gate.

Final-head rescue remains NOT_DEFINED and diagnostic-only. Historical FAILs,
original independent global references, Q24-wide residual non-admission,
native S16 RTZ, INT4 weights and FP16 operator/KV boundaries remain unchanged.
Fresh evidence requires normal independent Host review.

## L23/P0 combined S18 immediate-operand intervention

`q24_s16_l23_p0_s18_immediate_operand_combined_sufficiency_v1` substitutes
the independently authenticated original-input FP16 L23 stage12 and stage17
vectors only as the immediate operands to S18. The stage12 vector is lifted
exactly into separate `stage18_operand_i/z/h` Q24 fields, with signed zeros
preserved and independently checked by the rational state oracle. The actual
input and scratch I/Z/H, stage00-stage16 and input/output FP16 KV remain
byte-preserved. Original stage17 and output state are retained separately.
No upstream stage or original reference producer is dispatched.

Only S18 and the existing final RMSNorm/tied-head diagnostics run for each
of the nine controls. S18 retains the original-input binary64
excess-over-nearest-FP16 budget <= 1/8; its FP16 comparison is diagnostic.
Full coordinate gate rows, failure summaries, separate FP16/binary64 paired
final margins, operand provenance and exact dispatch/write counts are retained.
Final-head rescue remains `NOT_DEFINED`, with no new acceptance predicate.

The retained combined intervention has 110/896 binary64-authority coordinate
failures in each of the nine controls, despite zero FP16 diagnostic failures.
The worst excess is 19/8 at coordinate 62 against the unchanged 1/8 budget
(threshold margin -9/4). The independent rational S18 transition agrees with
the computed Q24 state. These observations reject all-nine rescue only for
this intervention; matching the FP16 diagnostic does not satisfy the
independently propagated binary64 authority. Acceptance of this new evidence
remains subject to normal independent Host review.

All-nine S18 PASS would establish only conditional rescue for this combined
intervention, not a unique root cause or certification of the original
trajectory. Any numerical S18 failure rejects all-nine rescue and leaves
the S18 local integration/reference-boundary hypothesis unresolved.
Authentication, source, reference, state/KV, lineage or execution defects
instead yield UNKNOWN and must not trigger automatic replay.
Historical failures, Q24-wide residual state, native INT4/FP16 operator/KV
boundaries and non-admission markers remain unchanged. Normal independent
Host review is required before accepting the new evidence.

## L23/P0 inherited-residual contrast

`q24_s16_l23_p0_inherited_residual_contrast_v1` substitutes the authenticated
original-input FP16 L23 `input_hidden` only as the inherited-residual operand
of stage12. The FP16 vector is lifted exactly into paired Q24 I/Z/H, including
signed-zero tags, without changing the Q24 arithmetic or rounding its later
residual sums. The retained actual input I/Z/H remains in the raw payload;
the distinct `residual_operand_h/i/z` fields disclose the intervention.
The independent rational state oracle checks the substituted stage12
transition and its own scratch-to-stage18 transition.

All actual stage00-stage11 outputs, input state and input/output FP16 KV
are byte-preserved. This includes value content, Q/K, unit P0 softmax,
attention product and output projection. Only stage12-stage18 and the
existing final RMSNorm/tied-head layer run. No prefix, attention, original
reference producer, admission run or accepted evidence is replayed.

The affected stages retain the existing local gates and original-input
binary64 stage18 excess-over-nearest-FP16 budget <= 1/8. Final-head
FP16/binary64 comparisons and paired margins remain diagnostic-only;
there is no final-head admission threshold to add or infer. All-nine
PASS means conditional rescue of this fresh cone, not certification
of the historical trajectory or a unique inherited-residual root cause.
Any numeric failure rejects all-nine rescue. Authentication, lineage
and state/KV defects cannot be classified as a scientific result.
The nine-control raw payloads retain preservation checks, local oracles,
acceptance metrics, original reference pins, exact dispatch/write counts
and separate retained/substituted final margins.

Q24 residual state remains wider than FP16. INT4 weights, FP16 operator
boundaries and FP16 KV are unchanged; historical failures, non-admission,
non-certification and normal independent Reviewer requirements remain.

## L23/P0 value-content sufficiency intervention

`q24_s16_l23_p0_value_content_sufficiency_v1` implements a single isolated
CPU-software intervention across the nine retained controls. It substitutes
the authenticated original-input FP16 L23 stage07 vector (128 components)
only in the working dependency-cone inputs. The retained stage03 source,
actual K/V caches, stage00-stage09 fields other than stage07, input Q24
I/Z/H, checkpoint operands, reference lineage and historical failures are
protected. Actual and reference P0 probabilities must all be exactly one.
This is an explicit stage07 operand intervention, not a native V-operator
validation or a cache update.

Only stage10-stage18 and the existing final RMSNorm/tied-head arithmetic
are recomputed. Stage10-stage17 retain the existing independently computed
same-operand local gates: finite and absolute error <= 0.125, or relative
error < 0.001 and ordered FP16 ULP <= 1. Stage18 retains the independently
propagated original-input binary64 excess-over-nearest-FP16 budget <= 1/8.
Final-head FP16/binary64 comparisons remain diagnostic-only: the existing
surface defines no final-head admission threshold, and this intervention
does not create one.

The raw output includes the substituted and retained value vectors,
protected fields, affected stage outputs, local oracle arrays, final
outputs, complete acceptance reports, paired final margins and separate
FP16/binary64 reference lineage. Margin change is actual minus reference
for the same ordered pair; the paired intervention delta is substituted
minus retained actual margin.

All-nine acceptance would establish only conditional rescue of the new
outputs, not a unique root cause, failure-creation sufficiency, prefix
correctness or full-model admission. Any numerical failure rejects the
all-nine rescue claim; authentication, source, state/KV, lineage or
execution defects instead yield UNKNOWN. Q24 residual state remains wider
than FP16, with INT4 weights and FP16 operator/KV boundaries unchanged.
Normal independent review remains necessary before accepting this surface.

## Isolated final-head execution boundary

`q24_s16_final_head_from_l23_coordinate62_suffix_execution_v1` binds the
reviewed revision-3 preflight and its exact original-input attempt003 final
reference authority. It permits only nine final RMSNorm/tied-head/top-k
CPU-software controls, in the reviewed raw failed L23 parent order.
The existing integer RMSNorm uses Q48 epsilon `281474977`, integer square
root and Q24 RNE. The tied head uses exact FP16 products and a Q47.48 sum
with one FP16 RNE; a checked integer bound rejects any int64 overflow.
Top-k uses descending finite rounded FP16 values and ascending token IDs
on ties. These are diagnostic IDs, not published or feedback tokens.

Execution exclusively creates a fresh ignored versioned direct child of
`build/`, retaining partial failures rather than retrying or overwriting.
It authenticates the official checkpoint, tied tensors, tokenizer, all raw
parent state/KV/lineage gates and L21/L22/L23 failure histories. Stored arrays
are compared against both reviewed binary64 and FP16 final trajectories,
without introducing a final-head admission threshold. Existing decoder
thresholds and original-input references remain unchanged.
`--validate` authenticates the stored evidence and comparisons without
rerunning RMSNorm, the head, decoder or original reference computation.
It writes nothing and reports normal independent Host review as REQUIRED.
The file census uses direct set equality, rejecting missing or extra files
independently of directory enumeration order. The retained execution is
authenticated by its exact result and original executor/test source pins;
the repaired validator reports its current source bindings separately.
Every other execution source binding must still match the live isolated
source, and other output attempts retain live executor/test authentication.

Q24 state remains wider than FP16. INT4 weights and FP16 operator/KV
boundaries are unchanged. This surface cannot establish strict-FP16-state
W4A16, new-token, full-model or candidate admission, policy adoption or
successor publication. It performs no decoder/prefix/admission replay,
RTL, hardware, GPU, simulation or external-service invocation.

## Final-head asset-binding boundary

Asset-binding revision 3 of
`q24_s16_final_head_from_l23_coordinate62_suffix_preflight_v1` uses the
canonical project-local official tokenizer copy in
`build/host_dialogue_audit_20260829T182819Z/tokenizer/`. Both tokenizer files
must match the existing fixed-revision byte hashes; no decoding, download,
symlink following or alternative-file fallback is authorized. The tokenizer
identity is bound alongside the authenticated checkpoint and its final
RMSNorm and tied embedding/head tensors, not inferred from a directory name.

Final numerical references require the exact isolated
`q24_s16_final_original_reference_v1_attempt003/freeze.json#final` authority,
not an extension of the unchanged original-input `references/freeze.json`
used by the nine reviewed L23 parents. The bundle manifest SHA256 is
`87b4746306ecaba3c106f0d6f3ec6c112c2ad3856386458a23ef463c04a98feb`.
Its pinned `eff468ba33e5` round-1 Reviewer terminal record must resolve through
the current handoff and be corroborated by backlog `status=done` and
`outcome.review_status=done`. Historical evidence-local `REQUIRED` and
`PENDING_REVIEW` labels do not override that terminal review.
The final binding must bind history `[9707]`, position zero, original
L23 binary64 and FP16 inputs, unchanged independent reference policy,
checkpoint, operator tensors and tokenizer pins. Its four canonical NPY
arrays are `rmsnorm_binary64` (`896`, binary64), `rmsnorm_fp16` (`896`, FP16
words), `logits_binary64` (`151936`, binary64) and `logits_fp16` (`151936`,
FP16 words). Hash, optional recorded size, exact array shape/dtype, finite
values and absence of trailing bytes are mandatory.

With this authority and all assets authenticated, the check reports
`PREFLIGHT_READY`. Without the reviewed manifest or arrays the check reports
`BLOCKED_MISSING_FINAL_REFERENCE`, the exact missing entry and array paths,
and each required array's missing or present-but-unbound status. Merely
finding another `final_norm.npy` or `full_vocab_logits.npy` does not
authenticate original-input propagation. The check cannot extend the
retained manifest, reanchor or recompute references, or certify available
unbound arrays. Unreviewed, corrupt, substitute, reanchored or noncanonical
authority is refused; the stale reference-producer default `attempt001` is
never selected as a fallback. The historical revision-2 consumer source pin
is authenticated as part of the reviewed manifest, not compared against the
changed revision-3 consumer. Producer source pins remain byte-checked.

This remains preparation only: all nine raw L23 states, L21/L22/L23 failures,
thresholds and source/operand/state/KV/lineage gates are retained. Final-head,
native, prefix/admission, RTL/hardware/GPU/simulation execution and evidence
writes remain zero; admission, policy adoption and successor publication
remain false. Q24 state is wider than FP16; INT4 weights and FP16 operators
and KV are unchanged. The changed revision requires independent Host review,
not a restamp of the previous revision or a new-token/full-model claim.

## L23/P0 raw failed-parent preparation boundary

`q24_s16_l23_from_l22_coordinate62_suffix_preflight_v1` is a read-only
preparation surface for exactly nine complete raw L22 I/Z/H states from the
reviewed L22 execution `attempt001`, in certified control order. It binds
the resolved terminal Reviewer record and corroborating backlog completion,
not stale evidence-local review-required strings. The raw L21 failures and
all nine L22 S18 coordinate-62 failures remain unchanged; the older adjusted
sparse-cut closure is not a source of replacement parents.

The `--check` command compiles its two Python files in memory, runs focused
non-native tests, and emits JSON only to stdout. It authenticates retained
source/operand/state/KV/lineage reports, freshly checks full Q24 I/Z/H
projection and exact original-reference L22 metrics, and binds the original
L23 binary64/FP16 references through the unchanged original L22 references.
Missing L23 reference material yields `BLOCKED_MISSING_L23_REFERENCE`;
corrupt or re-anchored material is rejected, not silently replaced.

Future output must be a fresh, ignored, canonical versioned direct child of
`build/`; check does not create it. Each possible L23/P0 consumer has its own
empty `(0, 128)` FP16-word K/V, never L22 KV. No native execution, prefix or
admission replay, RTL, hardware, GPU, simulation or evidence writing is
performed. INT4 and FP16 operator/KV boundaries remain unchanged, while Q24
residual state remains wider than FP16. Candidate admission, policy adoption
and successor publication remain false. Preparation is not L23 numerical
evidence or strict-FP16-state, token or full-model admission. The new
preflight requires independent Host review before acceptance; execution
requires a separate reviewed surface and current operational preflights.

## L22/P0 raw failed-parent execution boundary

`q24_s16_l22_from_l21_coordinate62_suffix_execution_v1` is separate from the
read-only preparation surface. It permits exactly nine native L22/P0 S0-S18
CPU-software consumers in certified census order, using the entire unchanged
raw L21 I/Z/H state. Every raw L21 S18 coordinate-62 failure remains recorded;
the older adjusted sparse-cut state is never substituted. Each native consumer
creates its own empty `(0, 128)` FP16-word K/V arrays.

The execution contract discloses the `--execute --out` command. It compiles
the two new Python files in memory and runs focused non-native guard tests
before exclusively creating a fresh ignored versioned direct-child `build/`
output. Original-input L22 reference recurrence, reviewed preflight/source
pins, official L22 tensors, local operand references, exact Q24 transitions,
own-layer KV and S16 RTZ remain mandatory. Native dispatch is restricted to
the nine L22 controls; prefix, admission and external execution are forbidden.
Stored-evidence validation follows execution without native replay, and
`--validate --out` exposes that read-only verification for independent review.
Files are never overwritten, and an execution error preserves its audit and
completed controls rather than retrying.

This surface records per-control native and oracle/gate timing separately,
without attributing a bottleneck from total time. Numerical PASS or FAIL is
not admission: Q24 state remains wider than FP16, INT4 weights and FP16
operator/KV boundaries remain unchanged, and candidate admission, policy
adoption and successor publication remain false. No strict-FP16-state
W4A16, new-token or full-model claim follows. New execution evidence requires
normal independent Host Reviewer validation before acceptance.

For these nine raw failed-parent states, the bounded L22 consumers retain
S0-S17 PASS and S18 FAIL at coordinate 62 alone: 162 passing and nine failing
stage reports. For `frozen_inherited`, the L22 output word is `52d4`, nearest
original-reference FP16 word `52eb`, and excess error
`759194225823/1099511627776` exceeds the unchanged `1/8` budget.
This is a downstream threshold miss, not evidence of a unique producer cause
or a certified replacement state.

## L22/P0 failed-parent preparation boundary

`q24_s16_l22_from_l21_coordinate62_suffix_preflight_v1` prepares exactly nine
raw L21 output I/Z/H states in the certified census order for possible isolated
L22/P0 consumers. All nine retain their S18 coordinate-62 failure: excess
`7/8`, budget `1/8`, margin `-3/4`. No cut, parent replacement or admission
occurs. The separately reviewed sparse-cut localization remains a comparator,
not a certification or source of replacement states for these nine parents.
The preparation authenticates both reviewed lineages and checks each raw
parent's full portable Q24 state against its independent rational FP16 view.

The original-input L22 binary64 and FP16 references are bound through the
retained reference manifest, with inputs equal to the original L21 references,
never the candidate L21 states. Missing reference material yields explicit
`BLOCKED_MISSING_L22_REFERENCE`; corrupt or re-anchored evidence is rejected.
Each future consumer requires its own empty `(0, 128)` FP16-word K/V arrays,
not the preceding layer's KV. Retained source/operand/state/KV/RTZ gates and
historical L15/L18/L20 failures remain intact; preparation does not freshly
execute those numerical gates.

The contract's `command_sidecar` invokes `--check`, which compiles the two new
Python files in memory, authenticates evidence and runs only the focused
preparation tests. JSON goes to stdout. `--out` declares, but never creates, a
fresh ignored canonical direct-child `build/` directory matching the versioned
attempt pattern. Occupied, symlinked, nonignored, nested and external outputs
are rejected. Future execution must independently recheck freshness and all
operational gates and create its output exclusively; this surface has no
`--execute` interface. Native, prefix, RTL, hardware, GPU and simulation
invocations and evidence writes are zero. Admission, adoption and successor
publication remain false. This is not L22 numerical evidence, strict-FP16-state
W4A16, new-token or full-model admission.

## Read-only L21 sparse-cut localization matrix

`q24_s16_l21_sparse_cut_localization_matrix_v1` contrasts the nine certified
L21-from-L20 census controls with the separately reviewed revision-2,
attempt002 L13/L15/L21 sparse-cut suffix. The nine raw coordinate-62 margins
remain `-3/4` (excess `7/8`, budget `1/8`). The composed suffix is a different
trajectory: its raw L21 coordinate has excess `3/4` and margin `-5/8`; its
adjusted coordinate has excess `1/8` and margin `0`, with the adjusted full
L21 vector and retained L22/L23 mandatory gates passing. Its raw L15 and L21
failures remain failures. A shared comparator is not nine paired interventions
or evidence that each census control closes after a cut; arithmetic margin
contrasts do not identify a unique cause or establish a native arithmetic repair.

The diagnostic binds the certified census source, independent review records,
closure result/validation/contract/source origins, recorded input files, raw
stage/local-reference/state/KV artifacts and separate adjusted states. Exact
scalar errors are checked from the retained FP16 words and binary64 reference.
Original independently propagated global references and thresholds stay fixed.
The census and closure retain distinct original-reference archive paths; their
L20/L21 binary64 and FP16 payloads are byte-identical. Both path sets remain
authenticated and disclosed, rather than equating their different state parents.
It imports no native execution module and emits JSON only to stdout through
`python -B -m ace3.model.candidates.diagnose_q24_s16_l21_sparse_cut_localization_matrix_v1 --check`
from this worktree with its repository on `PYTHONPATH`. This compiles the two
repository Python files in memory and runs only its 22 focused tests.
There is no native execution, prefix replay, hardware/simulation or evidence
overwrite. Source/operand/state/KV/RTZ gates are authenticated retained facts.
Admission, policy adoption and successor publication remain false; Q24 is
wider than FP16, with unchanged INT4 weights and FP16 operator/KV boundaries.
No strict-FP16-state W4A16, token or full-model admission follows.

## Read-only L21/P0 S18 failure census

`q24_s16_l21_from_l20_s18_failure_census_v1` summarizes the nine reviewed
L21-from-L20 controls without importing native execution modules. It pins the
terminal independent review, result and validation, authenticates all 37 L21
artifacts and their retained source/history bindings, and reads all 896 stored
S18 coordinate reports per control. Original L20/L21 binary64 and FP16 reference
files remain hash-bound and linked; no reference is recomputed or re-anchored.
The full ancestral input-binding manifest is retained, not replayed or claimed
to have been freshly re-executed.

All nine controls retain S0-S17 PASS and S18 FAIL at coordinate 62 alone.
The stored exact absolute error is `3915202785233/4398046511104`, with FP16
representation floor `66912088017/4398046511104`: excess `7/8` exceeds the
unchanged `1/8` budget by `3/4`. The census also reports the nearest-threshold
coordinates and exact signed margins across each complete stored S18 vector;
these are arithmetic summaries of existing metrics, not new layer evaluations.
Source/operand/state/KV/RTZ and historical parent PASS facts are explicitly
**authenticated retained evidence**, not freshly executed scientific gates.
Historical L15/L18 and the four excluded L20 failures remain intact.

The contract's `command_sidecar` records the byte-for-byte check command:
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_l21_from_l20_s18_failure_census_v1 --check`.
It compiles the two actual repository Python files in memory, runs 18 focused
tests and emits the census to stdout only. Native/RTL invocation counts are
zero, with no prefix replay, hardware/simulation or successor publication.
Candidate admission, policy adoption and successor publication remain false.
Q24 residual state is wider than FP16; INT4 weights and FP16 operator/KV
boundaries are unchanged. This establishes neither a unique cause nor
strict-FP16-state W4A16, new-token or full-model admission. Independent Host
Reviewer acceptance is required for this bounded non-admission diagnostic.

The native W4A16 profile uses FP16 activations and KV. Its finite interstage
trajectory policy is
`finite AND (abs_error <= 0.125 OR (relative_error < 0.001 AND ordered_FP16_ULP <= 1))`,
with relative denominator `max(abs(reference), 2^-14)`. The independently
propagated binary64 layer-output comparison is a separate absolute-error
contract with limit 0.125. The interstage relative/ULP alternative does not
apply to that binary64 contract.

For a fixed finite reference r, the smallest possible FP16 output error is
`min(abs(h - r))` over all finite FP16 values h. If that minimum exceeds the
absolute tolerance, changing an FP16-producing implementation alone cannot
make that coordinate satisfy the contract. For example, FP16 spacing in
the interval [1024, 2048) is 1, so a fixed 0.125 absolute limit is not feasible
for every binary64 reference in that interval.

An impossible minimum can coexist with avoidable excess error:
`abs(actual - r) - min(abs(h - r))`. The lower bound does not excuse this
excess, prove correct internal arithmetic, or identify a unique upstream
producer. Conversely, a representable reference has zero representation
lower bound; a trajectory failure against it cannot be attributed to output
unrepresentability.

Same-input arithmetic agreement and conditional operand substitutions are
not independent whole-trajectory acceptance. A retained-output evaluation
also does not certify an unexecuted endpoint, full-driver state compatibility,
or token selection. Original FAIL results remain failures even when a
representation limit is established. Changing precision, tolerance, or
reference quantization requires an explicit operator decision; a diagnosis
or bounded review does not itself authorize that change.

Retained canonical-suffix controls are a narrower diagnostic: replace a
complete operator output with its independently computed same-input reference,
then recompute its reachable suffix with canonical operators. Nested cuts
do not predict isolated native-RTL patches. At P0, with one visible cache
entry and unit softmax probability, Q/K cannot affect that layer's final
value path; this does not remove their effects on future cached state.
The actual incoming hidden stays fixed in these controls, while the original
global reference stays separate and unchanged. A failing canonical suffix
does not prove that every output permitted by the local error tolerances
must fail. The retained-control entry point is
`ace3/model/candidates/diagnose_retained_local_rounding_v3.py`; it performs
no capture, admission, or RTL execution.

The operator has authorized a separately versioned prospective W4A16
layer-final-output profile,
`ace3-w4a16-layer-final-binary64-fp16-excess-v1`, specified in
`ace3/contracts/candidates/binary64_fp16_excess_v1.json`. It requires valid
finite operands, reference magnitude at most 65504, and exact
`abs(actual-reference) - q(reference) <= 1/8`. Its nearest-value selection is
round-to-nearest, ties-to-even, with gradual underflow and signed-zero
preservation; out-of-range references are rejected rather than saturated.
The candidate computes and reports exact rational errors to avoid comparison
cancellation. Negative excess is an evaluator defect, not a clamping case.

The v1 definition alone is not adoption into a live gate.
Independent Host Reviewer acceptance must precede retrospective assessment
of authenticated retained trajectories. Later continuation adoption still
requires all unchanged gates and source/state/binary compatibility. The
legacy absolute-error policy and its FAIL evidence remain intact. The new
profile excludes internal stages, logits, the separately bound head contract,
other precision modes, and model-fidelity or dialogue certification.

Conformance to the declared independently propagated FP16-interstage reference
and fidelity to the independent binary64 trajectory are distinct claims.
Applying v1 to the FP16 reference itself is a separately labeled reference
control, never a replacement for evaluating actual RTL. A reference rejection
at a bit-identical actual/reference coordinate cannot establish an RTL deviation
from that reference; it can establish an additional fidelity demand. Such a
rejection alone proves neither a defective policy nor impossibility of meeting
both gates, since the stage gate permits some nonidentical values. Actual-only
binary64 failures must remain distinguishable from shared reference failures.

## Explicit decoder layer-final authority policy

`ace3-w4a16-layer-final-binary64-authority-v2` is a separate, opt-in
gate-applicability policy, specified in
`ace3/contracts/candidates/decoder_gate_policy_v2.json`. It does not change
the v1 numerical profile, its exact excess budget, or either independent
reference recurrence. S0-S17 retain the original mandatory FP16 predicate.
Only decoder S18 uses binary64-v1 as its mandatory numerical accuracy gate;
the original FP16 S18 comparison remains a truthful trajectory-conformance
diagnostic, including failures and errors. Finite/shape/dtype requirements
remain mandatory. Missing original binary64 evidence is BLOCKED.

The single-round residual runner accepts explicit `--gate-policy` binding
for its software screen and corresponding actual RTL-result comparisons.
Omitting that argument preserves the legacy dual-gate continuation behavior.
Its `--reevaluate-software` mode additionally requires the retained manifest
SHA256 and genuine retained review path. This mode cannot be combined with
an RTL continuation: it reuses authenticated software arrays, threads their
actual candidate hidden vectors from the accepted L4 hidden parent, keeps
each layer's own P0 KV empty, and computes only a missing software suffix
after all mandatory prefix gates pass. It emits a fresh policy-bound freeze
and result without changing old artifacts. A supported software screen is
not RTL admission; normal independent Host review is still required.

No v2 override applies to final RMSNorm, head, logits, tokens, whole-model
checks, general per-stage comparisons elsewhere, other dtypes, or state and
source authentication. Compatible arithmetic ancestors are not invalidated
merely by this policy binding.

## Isolated CPU Q24 residual experiment

The `q24_s16_l21_from_l20_coordinate62_suffix_v1` preparation surface binds
the independently reviewed `dc9d3eb787bd` L20/P0 execution. Its future L21
schedule contains only the nine complete frozen output I/Z/H parents whose
S0-S18 classifications all passed: `frozen_inherited`, `frozen_inherited_o`,
`frozen_inherited_down`, `frozen_inherited_o_down`, `scratch`, `scratch_down`,
`mapped62`, `mapped_all`, and `inherited_native`. Authentication still covers
all 13 L20 controls, including the unchanged `actual`, `frozen_o`,
`frozen_down`, and `frozen_o_down` S18 coordinate-62 failures and their
historical L15/L18 failures. Those four are not future L21 inputs.

From `/home/argustest/ace3-argus`, its disclosed check is
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_l21_from_l20_coordinate62_suffix_v1 --check`.
It compiles actual repository sources, authenticates candidate/test/contract
origins, reviewed L20 lineage, complete parents and original-input L21
binary64/FP16 references, and requires 32 focused tests with no failures,
errors or skips. It reports `L21_status=NOT_EXECUTED`, zero native/RTL
invocations, exactly nine planned L21/P0 controls, all admission/adoption/
publication flags false, and `normal_host_review=REQUIRED`.

The declared `--execute --out
build/q24_s16_l21_from_l20_coordinate62_suffix_v1_fresh` surface is reserved
for a separate non-admission task: exactly nine L21/P0 S0-S18 invocations,
no L0-L20 or accepted-prefix replay. Output must be a fresh, ignored,
versioned direct child of `build`, created exclusively; existing paths,
symlinks, traversal, wrong prefixes and non-ignored paths are rejected.
Each consumer has its own empty P0 FP16 KV, never L20 KV. References remain
independently propagated from the original input through the original L20
references, without recomputation or re-anchoring. No actual L21 baseline
is computed or inferred; any future cross-control comparison is explicitly
against the first scheduled control, not an actual baseline or global oracle.
Q24 state remains wider than FP16, with unchanged native G128 asymmetric
INT4 packing, native GEMM nibble order, no qzero plus-one adjustment, FP16
scales/operator boundaries/KV, S16 RTZ, exact thresholds and source/operand/
state/KV/lineage gates. Preparation is not L21 numerical evidence,
strict-FP16-state W4A16, new-token or full-model admission.

The `q24_s16_l20_from_l19_coordinate62_suffix_v1` surface binds the independently
reviewed `cac153e063be` L19/P0 execution: exactly 13 complete frozen L19 output
I/Z/H parents in control order, each with all 19 S0-S18 classifications passing.
The actual baseline, `frozen_o`, `frozen_down`, and `frozen_o_down` retain their
historical L15/L18 coordinate-62 failures; L16/L17/L19 passes do not erase them.

From `/home/argustest/ace3-argus`, the disclosed preparation command is
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_l20_from_l19_coordinate62_suffix_v1 --check`.
It compiles repository sources and authenticates candidate/test/contract origins,
the pinned independent L19 review, frozen parents and original-input L20
references. It requires exactly 34 focused tests without failures, errors or
skips, and reports `L20_status=NOT_EXECUTED`, zero native/RTL dispatch, exactly 13
planned L20/P0 controls, `candidate_admitted=false`, `policy_adopted=false`,
`successor_published=false`, and `normal_host_review=REQUIRED`.

Its declared `--execute --out
build/q24_s16_l20_from_l19_coordinate62_suffix_v1_fresh` interface is reserved
for a separate non-admission execution task: one native L20/P0 S0-S18 invocation
per frozen parent, no L0-L19 or accepted-prefix replay. It requires an exclusive,
fresh, ignored direct-child versioned build directory, rejecting existing paths,
symlinks, traversal, wrong prefixes and paths outside that scope. Each consumer
uses its own empty P0 FP16 KV, never L19 KV. The frozen independently propagated
original-input L20 binary64/FP16 references remain linked through the original
L19 references, without recomputation or control-derived re-anchoring.
All thresholds and source/operand/state/KV/lineage gates remain unchanged.
Q24 residual state is wider than FP16; native G128 asymmetric packed INT4 weights,
native GEMM nibble ordering, no qzero plus-one, FP16 scales/operator boundaries/KV
and S16 RTZ remain unchanged. Preparation establishes no L20 numerical result,
strict-FP16-state W4A16, new-token or full-model admission. Normal independent
Host review remains required.

An observed bounded execution of this L20/P0 surface completed all 13 controls
and 247 S0-S18 reports: 243 reports passed and four failed. Nine controls passed
every stage; the actual baseline, `frozen_o`, `frozen_down`, and `frozen_o_down`
did not. Those four also retain their historical L15/L18 failures and
`conditional_acceptance_retained=false`. Native L0-L19 and RTL invocation counts
were zero. Original-input global references and source/operand/state/KV/lineage
gates remained unchanged, and candidate admission, policy adoption, and successor
publication remained false. This is bounded non-admission evidence requiring
normal independent Host review, not a unique-cause attribution or
strict-FP16-state W4A16, new-token, or full-model approval.

The `q24_s16_l19_from_l18_coordinate62_suffix_v1` surface preserves all 13
complete frozen L18 output I/Z/H parents in control order from independently
reviewed `e1c2af4f8e0b`. Nine controls pass all L18 gates. The actual baseline,
`frozen_o`, `frozen_down`, and `frozen_o_down` pass S0-S17 but fail S18 at
coordinate 62. These failures, the historical L15 failures, and the all-pass
L16/L17 classifications remain distinct and unchanged.

From `/home/argustest/ace3-argus`, its preparation command is
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_l19_from_l18_coordinate62_suffix_v1 --check`.
It compiles actual repository sources, authenticates candidate/test/contract
origins, reviewed ancestry, frozen parents and original-input L19 references,
and requires exactly 32 focused tests with zero failures, errors or skips.
It reports `L19_status=NOT_EXECUTED`, zero native/RTL dispatch, exactly 13
planned controls, all three non-admission flags false, and
`normal_host_review=REQUIRED`.

The declared `--execute --out
build/q24_s16_l19_from_l18_coordinate62_suffix_v1_fresh` interface is for a
separate bounded execution task: exactly one native L19/P0 S0-S18 consumer
per frozen parent, never L0-L18 or an accepted-prefix replay. It rejects
existing paths, symlinks, traversal, non-ignored paths, and paths outside the
versioned direct-child build scope before creating an exclusive directory.
Each consumer uses its own empty P0 FP16 KV, not L18 KV. The frozen original-input
L19 binary64 global reference and FP16 trajectory remain independently
propagated through the original L18 references, with no recomputation or
control-derived re-anchoring. Thresholds, source/operand/state/KV/lineage gates,
native G128 asymmetric packed INT4 weights and nibble ordering, no qzero
plus-one, FP16 scales/operator boundaries/KV and S16 RTZ remain unchanged.
Q24 residual state is wider than FP16; this preparation establishes no L19
numerical result, admission, strict-FP16-state W4A16, new-token or full-model
approval and publishes no successor.

An observed bounded execution of this L19/P0 surface produced 13 passing
immediate-consumer controls and 247 passing S0-S18 reports, with no S18 failure
indices. This is independently reviewed non-admission evidence, not admission.
The actual baseline, `frozen_o`, `frozen_down`, and `frozen_o_down` still have
`conditional_acceptance_retained=false`: their L19 passes do not erase their
historical L15/L18 coordinate-62 failures. The other nine controls retain that
conditional flag. Native L0-L18 and RTL invocation counts are zero; the
original-input global references and all source/operand/state/KV/lineage gates
remain unchanged. Candidate admission, policy adoption and successor
publication remain false. These results do not establish strict-FP16-state
W4A16, new-token or full-model approval.

The `q24_s16_l18_from_l17_coordinate62_suffix_v1` surface authenticates the
independently reviewed L17/P0 execution as exactly 13 frozen complete output
I/Z/H parents in control order, including the actual baseline. All retained
L16/L17 passing classifications and the four historical L15 coordinate-62
failures remain intact; no accepted prefix is replayed.

From `/home/argustest/ace3-argus`, its preparation command is
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_l18_from_l17_coordinate62_suffix_v1 --check`.
The check compiles actual repository sources, authenticates candidate/test/
contract origins, frozen parents and the independently propagated original-input
L18 binary64/FP16 references, and requires exactly 34 focused tests with zero
failures, errors or skips. Historical tests are imported only for origin binding.
Execution-path tests mock native dispatch. The preparation result remains
`L18_status=NOT_EXECUTED`, `native_layer_invocations=0`, `rtl_invocations=0`,
`candidate_admitted=false`, `policy_adopted=false`, `successor_published=false`
and `normal_host_review=REQUIRED`.

The declared execution template replaces `--check` with `--execute --out
build/q24_s16_l18_from_l17_coordinate62_suffix_v1_fresh`. A separate bounded
execution task is required for exactly 13 future native L18/P0 S0-S18 immediate
consumers, one per parent. The output must be a fresh git-ignored direct child
of repository `build/` with the versioned prefix and a nonempty suffix.
Existing files/directories, symlinks or linked ancestors, traversal, nested/
outside paths, wrong prefixes and non-ignored paths are rejected before dispatch.
Declaring the interface does not execute, publish or queue a successor.

Each L18 control uses its own empty P0 FP16 KV, never L17 KV. References remain
linked through the original L17 reference, never actual or control-derived state.
All numerical thresholds and source/operand/state/KV/lineage gates are unchanged.
Q24 residual state remains wider than FP16; native G128 asymmetric INT4 packing,
GEMM nibble ordering, no qzero plus-one, FP16 scales/operator boundaries/KV and
S16 RTZ remain unchanged. Preparation is not L18 numerical evidence, repair,
admission, strict-FP16-state W4A16, new-token, full-model or hardware PASS.

The `q24_s16_l17_from_l16_coordinate62_suffix_v1` surface authenticates the
independently reviewed L16/P0 execution as 13 frozen complete L16 output I/Z/H
parents in control order. All 13 L16 controls pass their retained stage gates;
the four historical L15 coordinate-62 failures (`actual`, `frozen_o`,
`frozen_down`, `frozen_o_down`) and historical `retained_status=FAIL` remain
unchanged. L16's bounded acceptance is not revoked by those historical failures,
nor does it repair or admit their trajectories.

From `/home/argustest/ace3-argus`, the disclosed preparation command is
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_l17_from_l16_coordinate62_suffix_v1 --check`.
It compiles actual repository files, authenticates candidate/test/contract
origins and frozen lineage, and executes exactly 32 focused tests with no
failures, errors or skips. Historical tests are imported for origin checks
only; execution-path tests mock native dispatch. The result remains
`L17_status=NOT_EXECUTED`, `native_layer_invocations=0`, `rtl_invocations=0`,
`candidate_admitted=false`, `policy_adopted=false`, `successor_published=false`
and `normal_host_review=REQUIRED`.

The declared execution template uses the same context with `--execute --out
build/q24_s16_l17_from_l16_coordinate62_suffix_v1_fresh` instead of `--check`.
It requires a separate bounded task and exactly 13 future L17/P0 S0-S18
immediate-consumer invocations, one per frozen parent, with no native L0-L16,
RTL or successor invocation. The output must be a fresh git-ignored direct
child of repository `build/`, with the versioned prefix and a nonempty suffix.
Existing paths, symlinks and linked ancestors, traversal, nested/outside paths,
wrong prefixes and non-ignored paths are rejected before dispatch. Declaring
the interface does not execute, publish or queue a task.

Only the frozen independently propagated original-input L17 binary64 global
reference and FP16 trajectory are used, linked through original L16 references,
never actual or control-derived references. Numerical thresholds and
source/operand/state/KV/lineage gates are unchanged. Each L17 control starts
with its own empty P0 FP16 KV, not L16 KV. Q24 residual state remains wider than
FP16; native G128 asymmetric INT4 packing, GEMM nibble ordering, no qzero
plus-one, FP16 scales/operator boundaries/KV and S16 RTZ remain unchanged.
Surface validation is not L17 numerical evidence, repair, admission,
strict-FP16-state W4A16, new-token, full-model or hardware PASS.

The `q24_s16_l16_from_l15_coordinate62_suffix_v1` surface separates zero-dispatch
`--check` from a declared bounded non-admission `--execute --out` interface.
It authenticates the reviewed L15/P0 execution, all 13 complete output I/Z/H
parents in frozen control order, and the original-input independently propagated
L16 binary64 global reference and FP16 trajectory. The nine passing L15 controls
and four coordinate-62 failures (`actual`, `frozen_o`, `frozen_down`,
`frozen_o_down`) remain distinct and unchanged. Its plan names exactly 13 future
L16/P0 S0-S18 immediate consumers with own-empty-P0 FP16 KV, never L15 KV.
The execution template uses the command context below with `--execute --out
build/q24_s16_l16_from_l15_coordinate62_suffix_v1_fresh` instead of `--check`.
Execution requires a separate task; declaring this surface does not execute,
publish or queue one. Output must be a fresh, git-ignored direct child of the
repository `build/` directory with the versioned diagnostic name plus a nonempty
suffix. Existing files/directories, symlinks (including dangling links or linked
ancestors), traversal, nested/outside paths, wrong prefixes and non-ignored paths
are rejected before validation or dispatch. Execution authenticates again before
dispatch, retains all 13 controls and records stage gates, operands, paired state,
own-empty-P0 KV and actual-baseline comparisons. No prefix replay, L0-L15
recomputation, reference recomputation, RTL or successor publication is permitted.
From `/home/argustest/ace3-argus`, the disclosed command is
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_l16_from_l15_coordinate62_suffix_v1 --check`.
This compiles actual repository files, authenticates candidate/test/contract
origins and retained lineage, and requires exactly 46 focused tests with zero
failures, errors or skips. Historical tests are imported for origin checks only.
Execution-path tests mock native dispatch. The check result remains
`L16_status=NOT_EXECUTED`, `native_layer_invocations=0`, `rtl_invocations=0`,
`execution_surface_declared=true`, with `candidate_admitted=false`,
`policy_adopted=false`, `successor_published=false`, and
`normal_host_review=REQUIRED`. Numerical thresholds and source/operand/state/KV
gates are unchanged; Q24 residual state is wider than FP16, with unchanged
INT4 weights and FP16 scales/operator boundaries/KV. This is not L16 numerical
evidence, strict-FP16-state W4A16, new-token or full-model PASS.

The versioned `q24_s16_l15_from_l14_coordinate62_suffix_v1` surface authenticates
the independently reviewed frozen L14 execution and all 13 complete L14 output
I/Z/H parents, including the actual baseline, without recomputing L0-L14.
Its disclosed `--execute --out <fresh-directory>` schedule is exactly 13 native
L15/P0 S0-S18 consumers, one per frozen control in contract order. Each uses
own-empty-P0 FP16 KV, never L14 KV, and the frozen independently propagated
original-input L15 binary64 global reference linked through the original L14
reference. Thresholds, native-S16-RTZ arithmetic, INT4 weights, FP16 operator
boundaries/KV, paired Q24 state and source/operand/lineage gates are unchanged.
Q24 residual state remains wider than FP16.

From `/home/argustest/ace3-argus`, the repo-bound command is
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_l15_from_l14_coordinate62_suffix_v1 --check`.
It compiles actual repository sources, authenticates candidate/test origins and
frozen parent lineage, and requires exactly 40 focused tests with zero failures,
errors or skips. Historical suites are imported for origin checks only, never
replayed. Execution-path tests mock native dispatch; `--check` performs no native
or RTL invocation and is not L15 numerical evidence. Persist its JSON stdout
and test stderr in a fresh ignored build directory for independent review.
The execution template uses the identical context with `--execute --out
build/q24_s16_l15_from_l14_coordinate62_suffix_v1_fresh` instead of `--check`;
existing paths and symlinks are rejected. Execution records all stage gates,
operands, paired state, KV, original references and actual-baseline comparisons.
Neither surface changes historical failures or accepted evidence.
`candidate_admitted`, `policy_adopted` and `successor_published` remain false;
`normal_host_review=REQUIRED` and `rtl_invocations=0`. No repair, admission,
strict-FP16-state W4A16, new-token or full-model PASS is implied, and an execution
template does not publish or queue a successor.

The isolated `q24_s16_l12_coordinate62_producer_cone_v3` contract separates a zero-dispatch, repo-bound 30-test `--check` from bounded non-admission `--execute`: exactly 13 L13/P0 controls and two necessary L12 invocations (15 total), using the hash-pinned v2 authentication closure and frozen reviewed v3/L12 inputs; execution requires a separate bounded Planner task and normal independent Host review, with unchanged thresholds/global reference/state/KV/lineage gates and zero RTL dispatch.

The versioned `q24_s16_l14_from_l12_v3_coordinate62_suffix_v1` CPU-software surface
separates zero-dispatch `--check` from bounded non-admission `--execute --out
<fresh-directory>`. Its contract pins one frozen L12-v3 result and authenticates
all 13 complete L13 control outputs as separate immediate L14/P0 I/Z/H parents,
without consuming L13 KV. It preserves the retained L13 failures, original-input
global reference, unchanged numerical thresholds and source/operand/state/KV
checks. Execution is exactly 13 native L14/P0 invocations, one complete S0-S18
consumer per frozen control in contract order, including the retained actual
baseline. There is no L12/L13 recomputation, L0-L8 invocation, reference
recomputation, RTL dispatch or successor publication. Each control uses its own
empty P0 KV and the same authenticated, independently propagated original-input
L14 global reference, never a control-derived reference.
From `/home/argustest/ace3-argus`, the disclosed check command is
`PYTHONPATH=/home/argustest/ace3-argus PYTHONDONTWRITEBYTECODE=1 /home/argustest/miniconda3/bin/python -B -m ace3.model.candidates.diagnose_q24_s16_l14_from_l12_v3_coordinate62_suffix_v1 --check`.
It compiles actual repository sources and requires exactly 40 focused tests with
zero failures, errors or skips; execution-path tests use mocked dispatch only.
Historical suites are imported for origin authentication only. `--check` verifies
the declared execution bounds without running the experiment.
The separately selected execution command uses the identical command context,
replacing `--check` with `--execute --out
build/q24_s16_l14_from_l12_v3_coordinate62_suffix_v1_fresh`; that directory must
not already exist or be a symlink. Output stays under the versioned repository
build prefix and records all stage gates, operands, paired state, FP16 KV,
lineage, baseline comparisons and conditional-rescue classifications.
`candidate_admitted`, `policy_adopted` and `successor_published` remain false;
normal independent Host review remains required. Surface validation is not L14
numerical evidence, repair, admission, strict-FP16-state W4A16, new-token or
full-model evidence. Q24 residual state remains wider than FP16, with unchanged
INT4 weights and FP16 operator boundaries/KV.

The versioned L10/P0 coordinate-62 producer-cone input path
`ace3/model/candidates/diagnose_q24_s16_l10_coordinate62_producer_cone_v2.py`
authenticates the selected frozen L9 actual I/Z/H parent against the current
L9 entry source and the retained L9-output/L10-input linkage. It reuses the
v1 producer cuts and original-reference capture unchanged. The historical
L10 v1 result remains distinct: its old entry-source binding is rejected,
not replaced or silently grandfathered into the current path.

Its adjacent v2 contract exposes a repository-bound `--check` only:
compilation, focused tests and read-only authentication, with zero native
dispatch. This is not a new numerical result or execution authorization.
Accepted L0-L8 evidence is consumed read-only, never replayed. Independent
Host review is still required; neither this source-binding preflight nor
wide-Q24 software evidence implies strict-FP16-state W4A16, new-token,
full-model or hardware acceptance.

`ace3/model/candidates/q24_software_root_v3.py` is a software-only P0
root-to-L8 experiment, not an RTL runtime adapter. Its separately versioned
contract is `ace3/contracts/candidates/q24_software_root_v3.json`. Residual
state is signed int64 Q24 plus a zero-sign tag; INT4 weights and FP16 operator
boundaries/scales/KV are unchanged. This is not strict-FP16-state W4A16.

Each layer consumes its immediate actual CPU-produced I/Z/H parent. S12's
independent rational reference consumes authenticated I/Z and actual O,
rather than relifting H. S13 consumes rounded S12. Exact state checks retain
the unrounded residual through both additions and across layers. Every layer
owns an empty P0 prior cache and its own FP16 K/V output. The runner checks
each stage before dispatching its consumer and rejects FP16-only or invented
parents. A passing software parent is explicitly non-RTL, never a simulator
save or an accepted runtime import.

Other local references and original-input binary64-v1 global references are
unchanged. All original FP16 trajectory comparisons remain diagnostics.
The entry point requests explicit v3 opt-in, stops on the first mandatory
failure, and requires normal independent Host review. It neither resumes
the RTL backlog nor changes production interfaces, defaults, or token claims.

## Opt-in local-operator/global-trajectory v3

`ace3-w4a16-local-operator-global-binary64-authority-v3` changes the
mandatory S0-S17 reference inputs to authenticated actual producer DATA.
The local reference is independently implemented from canonical model
weights and operator semantics. The original exact FP16 predicate and
relative denominator do not change. Wrong source, tensor selection,
layout, history or KV identity is not legitimized by local arithmetic
agreement.

S18 still requires the original independently propagated binary64-v1
trajectory. It must never consume actual hidden/KV or local-reference
outputs as reference inputs. Original FP16 S0-S18 trajectory comparisons
remain separately reported diagnostics; their historical failures are not
rewritten. Legacy/v2 definitions and default behavior remain untouched.

The separate `reevaluate_local_operator_v3` entry point targets retained
single-round P0/L5-L8 software rooted in accepted actual L4 hidden. Its
independent reference currently supports P0 only and rejects other
positions. Native AWQ G128, canonical scales/biases, nibble ordering,
RMSNorm epsilon, GQA mapping and P0 RoPE/causal semantics are fixed in
`decoder_gate_policy_v3.json`. This bounded support does not admit a
whole model, tail, token, another position or another arithmetic history.

The actual-result adapter first runs `runtime_admission_v3.validate_runtime`.
It requires an actual-runtime manifest artifact and a separately supplied
manifest SHA256 from the owning controller's independently authenticated
receipt. A digest copied from the candidate result, a caller's `PASS` flag,
or a software archive is not an authority. The controller/Host must establish
this trust input; the evaluator does not mint it.

The read-only validator binds the retained single-round source closure,
exact public header and layer parameters, compile binary/generated save ABI,
pre-execution launch, actual transaction and operand archive, hidden parent,
canonical serialized tensors, P0 controls, complete ordered raw trace and
final output. It checks each layer's empty prior KV, cache producer identity,
and the idle saved-state layer, valid map and actual K/V contents without
restoring the state. Original FP16/binary64 references are authenticated
separately from actual operands. A missing or inconsistent receipt blocks
before numerical evaluation; evaluator no-execution is not an RTL failure.
Only an admitted actual transaction reaches the unchanged v3 numerical gates.
Normal independent Host review and downstream state-import requirements
remain separate. Synthetic receipt regressions are not simulator evidence.

The P0 raw trace ABI is token/position/stage/index/FP16. RoPE emission order
is half-split paired, not canonical tensor order. Capture and runtime admission
assemble vector stages by their explicit coordinates while preserving the
independently authenticated raw causal layout. S8/S9's repeated context index
zero denotes successive query heads, each followed by its own S10 block; it
must not be collapsed by a dictionary keyed only by raw index. Retained-data
recovery binds a new operand archive and corrected decoder sources separately
from the unchanged original execution, raw outputs and saved state. It neither
replays RTL nor supplies its own Host authentication or numerical acceptance.

A fresh continuation plan can reuse a recovered L5 admission through the
existing parent-result lookup while retaining the original runtime receipts
and historical helper identities separately from current decoder sources.
The continuation consumes only actual L5 hidden; its saved state belongs to
L5 and cannot initialize L6. Each P0 suffix layer owns empty prior KV.
Capture-only monitor values and original independent global references keep
their distinct roles. Binding preparation and public-interface compilation
establish neither a new numerical PASS nor independent review acceptance.

## Remaining P0 decoder binding

The separate CPU entry point
`ace3.model.candidates.run_q24_s16_toward_zero_l3_l8_v1` resumes the reviewed
native-S16-toward-zero Q24 L2 paired I/Z/H parent through L3-L8/P0, under
`q24_s16_toward_zero_l3_l8_v1.json`. It does not extend the original root
entry point's L0-L2 dispatch or replay that accepted prefix. Parent admission
binds the genuine root review, retained stage reports and actual residual/KV
outputs, and checks that the extracted arithmetic is unchanged from its
accepted source snapshot. The prior layer's KV is never restored into the
next layer. All local, original-input global and lineage gates remain
mandatory; the first failure stops dispatch without a failed-layer successor.
This interface supplies CPU evidence for normal Host review, not RTL, token,
production or full-model admission.

`tests.test_q24_s16_toward_zero_l3_l8_v1` exercises resume authentication with
synthetic local review, freeze, result, paired-state and KV records. Invalid
records must block in authentication before root lifting, layer dispatch,
model loading or numerical evaluation. The valid fixture stops immediately
after authentication; these regressions do not replay an accepted prefix,
read accepted run evidence or establish numerical or independent Host admission.

Post-authentication dispatch fixtures also use local authenticated records,
mocked model tensors, stage producers and numerical gate results. They check
mandatory-failure stopping before successor publication, immediate-predecessor
paired-state lineage, each layer's own empty P0 KV input and software-parent
metadata. Both passing and failing results retain L3-L8/P0 scope, zero RTL
and L9 invocations, no policy adoption and required independent Host review.
These are orchestration regressions, not new numerical admission evidence.
The repository-local `tests` package supports the module command
`python -m unittest tests.test_q24_s16_toward_zero_l3_l8_v1` with the repository
root on Python's module search path.

`remaining_layers_v3` extends the explicit v3 binding surface to L9-L23/P0,
history `[9707]`. It retains the accepted single-round RTL, public ports,
capture-only harness and saved-state ABI. The first executable parent is
the original admitted actual L8 hidden; each later layer consumes only its
immediately preceding admitted actual output. L8 saved state is evidence,
not an initializer. Every suffix layer starts with its own empty P0 KV.

The reference extension has a separate, reference-only graph. Its FP16
diagnostics continue the retained original FP16 L8 output using the frozen
`propagated_reference`; its mandatory binary64 references continue the
retained original binary64 L8 output using the frozen
`_reference_layer_step`. Only the missing L9-L23 references are computed.
Canonical tensors come from the independently bound whole official checkpoint;
native G128 packing, zero adjustment, scales and original numerical policies
are unchanged. No actual hidden, actual KV or local-reference result seeds
either original trajectory.

The extension records exact per-layer compile/capture/admission commands and
fresh output roots. Runtime admission requires the extension to match the
Host-authenticated manifest, while original L5-L8 plans retain their existing
scope. Missing extensions, unsupported positions/layers, changed controls,
state restoration and broken reference-parent links fail closed. The corrected
coordinate decoder is shared, including half-split RoPE and head-occurrence
decoding of repeated P0 S8/S9 indices.

The historical `decoder_gate_policy_v3` source identity and L5-L8 transaction
scope remain unchanged. L9-L23 admission explicitly selects
`remaining_decoder_gate_policy_v3`, bound as
`ace3-v3-remaining-decoder-p0-evaluator-v1` in the plan, Host-authenticated
runtime manifest and admission result. It reuses the historical numerical
stage predicates and independent local operators; it is not the historically
reviewed evaluator revision or a new numerical policy. Missing, changed or
cross-version evaluator bindings block before suffix numerical evaluation.

Reference suffixes may be reused without oracle recomputation. Their original
input freeze, arrays and generator identities stay immutable; when a generator
source has changed, a byte-identical preserved source record is required in the
new preexecution closure rather than treating current source as its old version.

Public-interface compilation and binding regressions do not establish
candidate L9-L23 numerical correctness. Ordered capture/admission remains a
parent-Host action after independent review, with the existing 7200-second
per-capture timeout. Final RMSNorm/head/logits/token checks, full-model
admission, later positions, generation and other precision modes are outside
this binding capability.
