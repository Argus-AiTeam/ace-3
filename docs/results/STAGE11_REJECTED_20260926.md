# Reviewed Stage11 CPU results - September 26, 2026

**Execution recovered; both tested hypotheses were rejected.** These are
completed CPU numerical experiments, not successful method qualification,
new-token/full-model admission, FPGA results or performance measurements.

| Experiment | Native reviewed completion (UTC) | Result | Actual computation |
| --- | --- | --- | --- |
| `b4978b898600` | 06:23:54 | REJECTED | One check; 63 local oracles, 27 projections, 9 final norms and 9 selected-pair heads |
| `6738b75d0cc8`, v2 S18 boundary localizer | 06:41:57 | Boundary-only explanation REJECTED | 9 final suffixes; 0 of 18 terminal directions recovered |

The v2 intervention tests whether changing only the S18 boundary removes the
opposite terminal response. It does not recover the predicted directions.
This rejects that bounded explanation; it does not establish the full cause.
Both results retain zero forbidden/prefix/reference/producer/service calls
within their recorded Python-scoped audits, not an OS-wide attestation.

The scope remains layer23/position0, full width896, frozen pair `[34319,13]`,
native S16 RTZ, G128 INT4, FP16 operator/KV boundaries and wider Q24 residual.
The numerical thresholds, original-input references and lineage are unchanged.
Normal independent review accepted the evidence and honest result, not the
rejected scientific hypothesis. Later `bb4f1b293dda` was UNKNOWN_NOT_RUN and is
not counted as a third numerical experiment.

[The structured summary](STAGE11_REJECTED_20260926.json) contains exact rational
result rows, invocation counts and SHA-256 references to host-only source
outputs and normal review records. It is not an original authoritative receipt.
Raw arrays, model weights, live authority/state and credentials are excluded.

## Published source boundary

The release module/tests, candidate/tests and capture helper/tests are copied
byte-for-byte from the independently reviewed `287d994f2fe4` release.
The v2 candidate, its test and v1 retained helper match hashes in the v2 result.
The v1 helper's companion regression file is also included as supporting source.
The [incremental manifest](../continuation/source-manifest-2026-09-26.json)
identifies every file. Later live release-module changes are not retroactively
part of these experiments and are not substituted for this reviewed basis.

The [host publisher source](../continuation/host-source-20260926/stage11_owner_claim_v6.py)
preserves the operational fix that authenticates immutable native completion
rather than hashing a mutable backlog row. It is a hash-checked live source
capture, not a member of the six-file scientific seal. Its original host-only
v4 helper and authority dependencies are not exported. It is archival source,
not a new execution entrypoint or permission to replay the publisher.

The separate [Planner patch](../continuation/argus-planner-format-retry-20260926.patch)
records the reviewed bounded format-repair fix against the previous host
runtime, including regression tests. It does not modify the active daemon or
promise compatibility with arbitrary Argus versions.

Completed temporary recovery steering subsequently caused a release-preparation
loop. It was withdrawn and the actual Planner started evidence-led research
analysis at 13:00 UTC. This publication is a checkpoint, not a claim that all
future scheduling is permanently reliable.
